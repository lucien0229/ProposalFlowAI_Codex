{
  "name": "proposalflow-ai",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "smoke": "turbo run smoke"
  },
  "devDependencies": {
    "turbo": "2.5.6",
    "typescript": "5.9.2"
  }
}
