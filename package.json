{
  "name": "proposalflow-ai",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "smoke": "playwright test"
  },
  "devDependencies": {
    "@proposalflow/shared-config": "workspace:*",
    "@proposalflow/shared-schemas": "workspace:*",
    "@proposalflow/shared-types": "workspace:*",
    "@playwright/test": "1.55.0",
    "react": "19.1.1",
    "react-dom": "19.1.1",
    "turbo": "2.5.6",
    "typescript": "5.9.2"
  }
}
