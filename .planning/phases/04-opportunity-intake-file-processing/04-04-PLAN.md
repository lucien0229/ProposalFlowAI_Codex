---
phase: 04-opportunity-intake-file-processing
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - apps/api/alembic/versions/0006_phase4_opportunity_files.py
  - apps/api/app/opportunity_models.py
  - apps/api/app/opportunity_repository.py
  - apps/api/app/opportunity_file_service.py
  - apps/api/app/opportunity_file_routes.py
  - apps/api/app/file_storage.py
  - apps/api/app/file_queue.py
  - apps/api/app/product.py
  - apps/worker/main.py
  - apps/worker/tasks/file_processing.py
  - tests/api/test_opportunity_file_processing_api.py
autonomous: true
requirements:
  - INTAKE-02
must_haves:
  truths:
    - "The file lifecycle must be explicit: upload-url, complete, poll detail, ready/failed, and retry."
    - "Retry must create a new processing job rather than overwrite the prior failure."
  artifacts:
    - path: "apps/api/app/opportunity_file_routes.py"
      provides: "D-30 file lifecycle routes"
    - path: "apps/worker/tasks/file_processing.py"
      provides: "PDF extraction worker task"
    - path: "tests/api/test_opportunity_file_processing_api.py"
      provides: "Verification for upload, processing, ready, failed, and retry"
  key_links:
    - from: "apps/api/app/opportunity_file_routes.py"
      to: "apps/api/app/file_storage.py"
      via: "upload-url and object verification"
      pattern: "/files/upload-url|/complete|/retry"
    - from: "apps/api/app/opportunity_file_service.py"
      to: "apps/worker/tasks/file_processing.py"
      via: "enqueued extraction and terminal states"
      pattern: "processing|ready|failed"
---

<objective>
Implement the asynchronous file-processing backend and worker seam.

Purpose: the overview page cannot show real file states or retry behavior until the upload/complete/detail/retry contract and worker extraction flow exist.
Output: file asset/job persistence, explicit file routes, worker extraction task, and retryable terminal states.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
@.planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
@.planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@tests/support/file_processing_fakes.py
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement file upload-url, complete, detail, and retry endpoints</name>
  <files>apps/api/alembic/versions/0006_phase4_opportunity_files.py, apps/api/app/opportunity_models.py, apps/api/app/opportunity_repository.py, apps/api/app/opportunity_file_service.py, apps/api/app/opportunity_file_routes.py, apps/api/app/file_storage.py, apps/api/app/file_queue.py, apps/api/app/product.py, tests/api/test_opportunity_file_processing_api.py</files>
  <read_first>
    - apps/api/app/opportunity_models.py
    - apps/api/app/opportunity_repository.py
    - apps/api/app/product.py
    - tests/api/test_opportunity_file_processing_api.py
    - tests/support/file_processing_fakes.py
    - packages/shared-config/index.ts
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: upload URL creation returns an `uploaded` file asset.
    - Test 2: complete verifies object existence and returns `file_status = processing`.
    - Test 3: detail returns `file_status` plus `latest_job_status`.
    - Test 4: retry creates a new job record.
  </behavior>
  <action>
    Run the file-processing API tests from RED first, then implement the explicit D-30 file surface:
    - `POST /api/v1/opportunities/{opportunity_id}/files/upload-url`
    - `POST /api/v1/opportunities/{opportunity_id}/files/{file_asset_id}/complete`
    - `GET /api/v1/opportunities/{opportunity_id}/files/{file_asset_id}`
    - `POST /api/v1/opportunities/{opportunity_id}/files/{file_asset_id}/retry`

    Use `file_assets` and `file_processing_jobs`. Persist only `uploaded`, `processing`, `ready`, and `failed` on the file surface; do not invent a stored `complete` status.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_opportunity_file_processing_api.py -k 'upload_url or complete or detail or retry' -q</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/app/opportunity_file_routes.py` exposes all four D-30 file routes.
    - `apps/api/alembic/versions/0006_phase4_opportunity_files.py` creates `file_assets` and `file_processing_jobs`.
    - The targeted file-route pytest command exits `0`.
  </acceptance_criteria>
  <done>
    The backend now exposes the full file lifecycle required by the overview UI.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement worker extraction and terminal file-state transitions</name>
  <files>apps/api/app/opportunity_file_service.py, apps/api/app/file_queue.py, apps/worker/main.py, apps/worker/tasks/file_processing.py, tests/api/test_opportunity_file_processing_api.py</files>
  <read_first>
    - apps/api/app/opportunity_file_service.py
    - apps/api/app/file_queue.py
    - apps/worker/main.py
    - tests/api/test_opportunity_file_processing_api.py
    - .planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: successful extraction produces `ready` and extracted preview text.
    - Test 2: non-extractable PDFs produce `failed` with the MVP OCR-out-of-scope message.
    - Test 3: retry moves the file back to `processing` via a fresh job attempt.
  </behavior>
  <action>
    Run the ready/failed/retry tests from RED first, then implement the worker path.

    1. Replace the stub `apps/worker/main.py` entry with a queue-backed worker boot.
    2. Implement `apps/worker/tasks/file_processing.py` using `pypdf` only.
    3. Keep the exact failure message `No extractable text found. OCR is not part of the MVP pipeline.`
    4. Preserve prior failure history when retrying; do not mutate the original job into success.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_opportunity_file_processing_api.py -k 'ready or failed or retry' -q</automated>
  </verify>
  <acceptance_criteria>
    - `apps/worker/main.py` is no longer an idle print stub.
    - `apps/worker/tasks/file_processing.py` contains the exact OCR-out-of-scope failure message.
    - The targeted ready/failed/retry pytest command exits `0`.
  </acceptance_criteria>
  <done>
    File extraction and retry are durable backend behaviors rather than planned placeholders.
  </done>
</task>

</tasks>

<verification>
The file backend is ready only when upload, complete, detail, ready, failed, and retry tests are all green.
</verification>

<success_criteria>
- The full D-30 file route surface exists.
- Worker extraction drives terminal file states.
- Retry semantics are explicit and job-based.
</success_criteria>

<output>
After completion, create `.planning/phases/04-opportunity-intake-file-processing/04-04-SUMMARY.md`
</output>
