---
phase: 04-opportunity-intake-file-processing
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - apps/api/alembic/versions/0005_phase4_opportunity_intake.py
  - apps/api/app/opportunity_models.py
  - apps/api/app/opportunity_repository.py
  - apps/api/app/opportunity_service.py
  - apps/api/app/opportunity_routes.py
  - apps/api/app/opportunity_input_routes.py
  - apps/api/app/product.py
  - tests/api/test_opportunity_intake_api.py
  - tests/api/test_opportunity_inputs_api.py
  - tests/api/test_lead_brief_gate_api.py
autonomous: true
requirements:
  - OPP-02
  - INTAKE-01
  - INTAKE-03
must_haves:
  truths:
    - "The opportunity overview API must expose a richer container model than the Phase 3 list/detail shape."
    - "D-30 inputs CRUD-lite endpoints must exist as first-class opportunity-scoped resources, not be hidden inside a single PATCH detail route."
  artifacts:
    - path: "apps/api/app/opportunity_input_routes.py"
      provides: "Opportunity-scoped inputs CRUD-lite routes"
    - path: "apps/api/app/opportunity_routes.py"
      provides: "Overview detail/update and lead-brief generate routes"
    - path: "tests/api/test_opportunity_inputs_api.py"
      provides: "Verification of explicit inputs CRUD-lite behavior"
  key_links:
    - from: "apps/api/app/opportunity_input_routes.py"
      to: "apps/api/app/opportunity_repository.py"
      via: "GET/POST/PATCH inputs CRUD-lite calls"
      pattern: "/inputs|input_id|raw_input"
    - from: "apps/api/app/opportunity_service.py"
      to: "tests/api/test_lead_brief_gate_api.py"
      via: "shared server-side gate logic"
      pattern: "missing_fields|missing_source|file_processing|save_failed"
---

<objective>
Implement the opportunity overview backend, explicit inputs CRUD-lite routes, and the server-enforced generate gate.

Purpose: the overview page cannot be a real opportunity container until the API exposes both the rich detail resource and the separate input-resource surface required by D-30.
Output: persisted intake schema, overview detail/update routes, inputs CRUD-lite endpoints, and synchronous lead-brief generate semantics.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
@.planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
@.planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@apps/api/app/opportunity_service.py
@apps/api/app/opportunity_repository.py
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement overview detail/update and opportunity-scoped inputs CRUD-lite</name>
  <files>apps/api/alembic/versions/0005_phase4_opportunity_intake.py, apps/api/app/opportunity_models.py, apps/api/app/opportunity_repository.py, apps/api/app/opportunity_service.py, apps/api/app/opportunity_routes.py, apps/api/app/opportunity_input_routes.py, apps/api/app/product.py, tests/api/test_opportunity_intake_api.py, tests/api/test_opportunity_inputs_api.py</files>
  <read_first>
    - apps/api/app/opportunity_models.py
    - apps/api/app/opportunity_repository.py
    - apps/api/app/opportunity_service.py
    - apps/api/app/product.py
    - tests/api/test_opportunity_intake_api.py
    - tests/api/test_opportunity_inputs_api.py
    - packages/shared-config/index.ts
    - packages/shared-types/index.ts
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: `GET /api/v1/opportunities/{opportunity_id}` returns a rich overview container with workflow summary, step readiness, restriction context, and input/file summaries.
    - Test 2: `PATCH /api/v1/opportunities/{opportunity_id}` persists minimum opportunity context without swallowing raw-input resource behavior.
    - Test 3: `GET/POST /api/v1/opportunities/{opportunity_id}/inputs` and `PATCH /api/v1/opportunities/{opportunity_id}/inputs/{input_id}` persist and return manual source records.
    - Test 4: optional `contact_email` validates only when present.
  </behavior>
  <action>
    Implement D-29 and D-30 exactly.

    1. Run `tests/api/test_opportunity_intake_api.py` and `tests/api/test_opportunity_inputs_api.py` from their RED state first.

    2. Add `apps/api/alembic/versions/0005_phase4_opportunity_intake.py` and extend `apps/api/app/opportunity_models.py` with the `opportunity_inputs` table for manual raw-input records.

    3. Extend repository and service code so overview detail returns:
       - base opportunity fields
       - current workflow summary
       - step readiness
       - restriction context
       - summarized current input/file state for the overview workspace

    4. Create `apps/api/app/opportunity_input_routes.py` and mount it from `apps/api/app/product.py` with the exact D-30 endpoints:
       - `GET /api/v1/opportunities/{opportunity_id}/inputs`
       - `POST /api/v1/opportunities/{opportunity_id}/inputs`
       - `PATCH /api/v1/opportunities/{opportunity_id}/inputs/{input_id}`

    5. Keep raw input as its own resource. Do not fold D-30 inputs CRUD-lite back into a single detail PATCH.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_opportunity_intake_api.py -q && PYTHONPATH=apps/api pytest tests/api/test_opportunity_inputs_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/app/opportunity_input_routes.py` exists and exposes the exact D-30 input routes.
    - `apps/api/alembic/versions/0005_phase4_opportunity_intake.py` creates `opportunity_inputs`.
    - `apps/api/app/opportunity_service.py` returns workflow summary, step readiness, and restriction context in overview detail.
    - Both targeted pytest commands exit `0`.
  </acceptance_criteria>
  <done>
    The API now exposes both the overview container and the separate manual-input resource surface required by the frozen contract.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement server-enforced lead-brief gate semantics</name>
  <files>apps/api/app/opportunity_service.py, apps/api/app/opportunity_routes.py, apps/api/app/product.py, tests/api/test_lead_brief_gate_api.py</files>
  <read_first>
    - apps/api/app/opportunity_service.py
    - apps/api/app/opportunity_routes.py
    - tests/api/test_lead_brief_gate_api.py
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - .planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: generate stays blocked when minimum business context is missing.
    - Test 2: generate stays blocked when neither meaningful manual input nor usable extracted text exists.
    - Test 3: manual raw input can unlock generate even when a file is absent or failed.
    - Test 4: success returns a redirect to `/opportunities/{id}/lead-brief`.
  </behavior>
  <action>
    Implement the server gate once and reuse it in both detail serialization and the generate endpoint.

    1. Run `tests/api/test_lead_brief_gate_api.py` from its RED state first.
    2. Keep the gate reasons exactly: `missing_fields`, `missing_source`, `file_processing`, `save_failed`.
    3. Implement `POST /api/v1/opportunities/{opportunity_id}/lead-brief/generate` in `apps/api/app/opportunity_routes.py`.
    4. Update opportunity status and redirect to `/opportunities/{id}/lead-brief` on success, but do not implement Phase 5 content in this plan.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_lead_brief_gate_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The generate route exists and uses shared server-side gate logic.
    - Success responses contain `redirect_to` ending in `/lead-brief`.
    - `PYTHONPATH=apps/api pytest tests/api/test_lead_brief_gate_api.py -q` exits `0`.
  </acceptance_criteria>
  <done>
    Phase 4 now enforces lead-brief readiness on the server instead of relying on browser-only heuristics.
  </done>
</task>

</tasks>

<verification>
The overview backend is ready only when overview detail/update, D-30 inputs CRUD-lite, and lead-brief gate tests are all green.
</verification>

<success_criteria>
- Overview detail is richer than the Phase 3 list item.
- Inputs CRUD-lite endpoints exist explicitly and pass tests.
- Generate gating is enforced server-side and returns a direct Phase 5 handoff seam.
</success_criteria>

<output>
After completion, create `.planning/phases/04-opportunity-intake-file-processing/04-03-SUMMARY.md`
</output>
