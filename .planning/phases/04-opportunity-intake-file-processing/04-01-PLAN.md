---
phase: 04-opportunity-intake-file-processing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/index.ts
  - packages/shared-schemas/index.ts
  - packages/shared-config/index.ts
  - apps/web/package.json
  - package.json
  - pnpm-lock.yaml
  - apps/api/pyproject.toml
  - apps/worker/pyproject.toml
  - infra/compose/docker-compose.local.yml
  - tests/conftest.py
  - tests/support/file_processing_fakes.py
autonomous: true
requirements:
  - OPP-02
  - INTAKE-01
  - INTAKE-02
  - INTAKE-03
must_haves:
  truths:
    - "Phase 4 must start from a stable contract and tooling baseline before any failing behavior specs are written."
    - "Wave 0 is incomplete until `pnpm exec playwright test --list` succeeds, the supported API smoke command `PYTHONPATH=apps/api pytest ...` imports the app cleanly, and queue/object-storage fakes are available."
  artifacts:
    - path: "packages/shared-types/index.ts"
      provides: "Phase 4 opportunity, input, file, and generation DTOs"
    - path: "packages/shared-config/index.ts"
      provides: "Frozen route builders for opportunity detail, inputs CRUD-lite, files, and lead-brief generate"
    - path: "tests/conftest.py"
      provides: "Reusable API bootstrap and authenticated session fixtures for the supported `PYTHONPATH=apps/api pytest ...` contract"
    - path: "tests/support/file_processing_fakes.py"
      provides: "Inline queue and in-memory object-storage fakes"
  key_links:
    - from: "packages/shared-config/index.ts"
      to: "docs/web/ProposalFlow AI｜API 设计 v1.0.md"
      via: "exact D-30 endpoint builders"
      pattern: "/inputs|/files/upload-url|/lead-brief/generate"
    - from: "tests/conftest.py"
      to: "apps/api/app/main.py"
      via: "importable FastAPI app fixture"
      pattern: "create_app|api_client|authenticated_web_session"
---

<objective>
Repair the Phase 4 execution baseline before RED-state test authoring begins.

Purpose: separate harness/tooling repair from the later failing-spec capture so TDD proves behavior failures rather than broken imports or missing binaries.
Output: frozen shared contracts, installed Phase 4 dependencies, local service baseline, reusable fixtures, and deterministic queue/object-storage fakes.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
@.planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
@.planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
@.planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@infra/compose/docker-compose.local.yml
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze Phase 4 contracts and install the researched dependency baseline</name>
  <files>packages/shared-types/index.ts, packages/shared-schemas/index.ts, packages/shared-config/index.ts, apps/web/package.json, package.json, pnpm-lock.yaml, apps/api/pyproject.toml, apps/worker/pyproject.toml</files>
  <read_first>
    - packages/shared-types/index.ts
    - packages/shared-schemas/index.ts
    - packages/shared-config/index.ts
    - apps/web/package.json
    - package.json
    - apps/api/pyproject.toml
    - apps/worker/pyproject.toml
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - .planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
  </read_first>
  <action>
    Implement the Wave 0 contract baseline per D-02, D-15, D-18, D-30, and D-31.

    1. Extend `packages/shared-types/index.ts` with explicit Phase 4 types for:
       - opportunity overview/detail response
       - opportunity input records
       - file assets and processing jobs
       - generation gate reasons
       - lead-brief generate response

    2. Extend `packages/shared-schemas/index.ts` with matching schemas for:
       - overview detail/update
       - `GET /api/v1/opportunities/{opportunity_id}/inputs`
       - `POST /api/v1/opportunities/{opportunity_id}/inputs`
       - `PATCH /api/v1/opportunities/{opportunity_id}/inputs/{input_id}`
       - file detail and generate response payloads

    3. Extend `packages/shared-config/index.ts` with exact D-30 route builders and constants:
       - `GET /api/v1/opportunities/{opportunity_id}`
       - `PATCH /api/v1/opportunities/{opportunity_id}`
       - `GET /api/v1/opportunities/{opportunity_id}/inputs`
       - `POST /api/v1/opportunities/{opportunity_id}/inputs`
       - `PATCH /api/v1/opportunities/{opportunity_id}/inputs/{input_id}`
       - `POST /api/v1/opportunities/{opportunity_id}/files/upload-url`
       - `POST /api/v1/opportunities/{opportunity_id}/files/{file_asset_id}/complete`
       - `GET /api/v1/opportunities/{opportunity_id}/files/{file_asset_id}`
       - `POST /api/v1/opportunities/{opportunity_id}/files/{file_asset_id}/retry`
       - `POST /api/v1/opportunities/{opportunity_id}/lead-brief/generate`
       - `OPPORTUNITY_FILE_POLL_INTERVAL_MS = 2000`

    4. Update dependency manifests to the researched stack:
       - frontend: `@tanstack/react-query`, `react-hook-form`, `zod`, `@hookform/resolvers`, `lucide-react`
       - backend/worker: `redis`, `rq`, `pypdf`, `boto3`
       - refresh `pnpm-lock.yaml` so `pnpm exec playwright` resolves in this workspace

    Do not add shadcn or any third-party UI registry.
  </action>
  <verify>
    <automated>grep -q 'opportunities/{opportunity_id}/inputs' packages/shared-config/index.ts && grep -q 'OpportunityInput' packages/shared-types/index.ts && grep -q '@tanstack/react-query' apps/web/package.json && grep -q 'boto3>=' apps/api/pyproject.toml && pnpm exec playwright test --list >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared-config/index.ts` contains exact D-30 input/file/generate route builders.
    - `packages/shared-types/index.ts` exports an opportunity-input DTO and file-status DTO.
    - `apps/web/package.json` declares the researched Phase 4 frontend libraries.
    - `apps/api/pyproject.toml` and `apps/worker/pyproject.toml` declare `redis`, `rq`, `pypdf`, and `boto3`.
    - `pnpm exec playwright test --list` exits `0`.
  </acceptance_criteria>
  <done>
    Phase 4 has a frozen contract surface and installed dependency baseline that later plans can target directly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repair local fixtures and deterministic async-service test doubles</name>
  <files>infra/compose/docker-compose.local.yml, tests/conftest.py, tests/support/file_processing_fakes.py</files>
  <read_first>
    - infra/compose/docker-compose.local.yml
    - tests/conftest.py
    - .planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
    - .planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
  </read_first>
  <action>
    Repair the harness without writing Phase 4 failing specs yet.

    1. Update `infra/compose/docker-compose.local.yml` to keep the existing `postgres`, `redis`, and `minio` services but add healthchecks for `redis` and `minio`.

    2. Extend `tests/conftest.py` so the supported API command `PYTHONPATH=apps/api pytest ...` can import the API app and provide:
       - `api_client`
       - `authenticated_web_session`
       - shared payload builders for opportunities, inputs, and file requests
       - default fake queue/object-storage fixtures

    3. Create `tests/support/file_processing_fakes.py` with:
       - `InlineQueue`
       - `InMemoryObjectStore`

    4. Keep API tests off live Redis and MinIO by default; compose-backed services remain optional for local parity, not required for the quick smoke loop.
  </action>
  <verify>
    <automated>python3 -c "from tests.support.file_processing_fakes import InlineQueue, InMemoryObjectStore; print(InlineQueue.__name__, InMemoryObjectStore.__name__)" | grep -q 'InlineQueue InMemoryObjectStore' && PYTHONPATH=apps/api pytest --collect-only -q tests/api/test_dashboard_opportunities_api.py && grep -q 'healthcheck' infra/compose/docker-compose.local.yml</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` exposes `api_client` and `authenticated_web_session`.
    - `tests/support/file_processing_fakes.py` exports `InlineQueue` and `InMemoryObjectStore`.
    - `infra/compose/docker-compose.local.yml` contains healthchecks for `redis` and `minio`.
    - `PYTHONPATH=apps/api pytest --collect-only -q tests/api/test_dashboard_opportunities_api.py` exits `0`.
  </acceptance_criteria>
  <done>
    Harness repair is complete and the next plan can capture a real RED state instead of a broken-environment failure.
  </done>
</task>

</tasks>

<verification>
Wave 0 repair is complete when contracts are frozen, `pnpm exec playwright test --list` succeeds, `PYTHONPATH=apps/api pytest ...` imports the API app cleanly, and deterministic async-service fakes are available.
</verification>

<success_criteria>
- Shared Phase 4 contracts are explicit and D-30-compliant.
- The workspace has the required frontend/backend dependencies installed.
- `PYTHONPATH=apps/api pytest ...` and `pnpm exec playwright test --list` are runnable before failing behavior specs are authored.
</success_criteria>

<output>
After completion, create `.planning/phases/04-opportunity-intake-file-processing/04-01-SUMMARY.md`
</output>
