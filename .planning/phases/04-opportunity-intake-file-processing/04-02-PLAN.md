---
phase: 04-opportunity-intake-file-processing
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - tests/api/test_opportunity_intake_api.py
  - tests/api/test_opportunity_inputs_api.py
  - tests/api/test_opportunity_file_processing_api.py
  - tests/api/test_lead_brief_gate_api.py
  - tests/e2e/opportunity-overview-shell.spec.ts
  - tests/e2e/opportunity-intake.spec.ts
  - tests/e2e/helpers/opportunity-overview.ts
autonomous: true
requirements:
  - OPP-02
  - UI-02
  - INTAKE-01
  - INTAKE-02
  - INTAKE-03
must_haves:
  truths:
    - "TDD in Phase 4 must prove a real RED state after harness repair, not just file existence or test collection."
    - "The RED state must cover opportunity inputs CRUD-lite, file processing, shared route-level product states, and browser behavior before production code begins."
  artifacts:
    - path: "tests/api/test_opportunity_inputs_api.py"
      provides: "Failing D-30 CRUD-lite input endpoint coverage"
    - path: "tests/api/test_opportunity_file_processing_api.py"
      provides: "Failing file lifecycle coverage"
    - path: "tests/e2e/opportunity-intake.spec.ts"
      provides: "Failing browser workflow coverage"
    - path: "tests/e2e/opportunity-overview-shell.spec.ts"
      provides: "Failing route-state and opportunity-existence coverage"
  key_links:
    - from: "tests/api/test_opportunity_inputs_api.py"
      to: "packages/shared-config/index.ts"
      via: "exact D-30 input routes"
      pattern: "/inputs|/inputs/{input_id}"
    - from: "tests/e2e/opportunity-intake.spec.ts"
      to: "04-UI-SPEC.md"
      via: "locked copy, shared-state, and layout assertions"
      pattern: "Opportunity intake|Generate lead brief|Retry extraction|ProductStateBlock|loading|empty|error|blocked|retry|success"
---

<objective>
Author the Phase 4 failing specs and capture an explicit RED state.

Purpose: separate behavior-failure capture from harness repair so implementation later starts from real unmet product behavior.
Output: failing API and browser specs with RED-state verification commands that must return non-zero until later plans implement the feature.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
@.planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
@.planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
@.planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@tests/conftest.py
@tests/support/file_processing_fakes.py
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author failing API specs for overview, inputs CRUD-lite, files, and generate gate</name>
  <files>tests/api/test_opportunity_intake_api.py, tests/api/test_opportunity_inputs_api.py, tests/api/test_opportunity_file_processing_api.py, tests/api/test_lead_brief_gate_api.py</files>
  <read_first>
    - tests/conftest.py
    - tests/api/test_dashboard_opportunities_api.py
    - packages/shared-config/index.ts
    - packages/shared-types/index.ts
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - .planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: overview detail/update is missing the nested intake workspace semantics.
    - Test 2: `GET/POST /api/v1/opportunities/{opportunity_id}/inputs` and `PATCH /api/v1/opportunities/{opportunity_id}/inputs/{input_id}` fail because D-30 CRUD-lite inputs are not implemented yet.
    - Test 3: file upload-url, complete, detail, and retry fail because the file lifecycle is not implemented yet.
    - Test 4: lead-brief generate fails because the server gate and redirect seam are not implemented yet.
    - Test 5: missing opportunities still fail existence/not-found assertions because the API contract is not implemented yet.
  </behavior>
  <action>
    Create the failing API specs only; do not implement production code in this plan.

    1. Add `tests/api/test_opportunity_intake_api.py` for overview detail/update behavior plus explicit `404` handling when the opportunity record does not exist.
    2. Add `tests/api/test_opportunity_inputs_api.py` with explicit D-30 cases for:
       - `GET /api/v1/opportunities/{opportunity_id}/inputs`
       - `POST /api/v1/opportunities/{opportunity_id}/inputs`
       - `PATCH /api/v1/opportunities/{opportunity_id}/inputs/{input_id}`
    3. Add `tests/api/test_opportunity_file_processing_api.py` for upload-url, complete, detail, ready/failed, and retry.
    4. Add `tests/api/test_lead_brief_gate_api.py` for blocked reasons and successful `/lead-brief` redirect.
    5. Capture the RED state with the exact commands below and record the non-zero exits in task notes or summary output.
  </action>
  <verify>
    <automated>! PYTHONPATH=apps/api pytest tests/api/test_opportunity_intake_api.py -q && ! PYTHONPATH=apps/api pytest tests/api/test_opportunity_inputs_api.py -q && ! PYTHONPATH=apps/api pytest tests/api/test_opportunity_file_processing_api.py -q && ! PYTHONPATH=apps/api pytest tests/api/test_lead_brief_gate_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/test_opportunity_inputs_api.py` exists and names the exact D-30 input endpoints.
    - The RED verification command exits `0` because each targeted pytest command fails as expected.
    - No production files outside the test tree are modified in this task.
  </acceptance_criteria>
  <done>
    The API side of Phase 4 has a real RED state proving missing behavior rather than missing tooling.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author failing browser specs for shell continuity, editing, file states, and handoff</name>
  <files>tests/e2e/opportunity-overview-shell.spec.ts, tests/e2e/opportunity-intake.spec.ts, tests/e2e/helpers/opportunity-overview.ts</files>
  <read_first>
    - tests/e2e/dashboard-opportunities-integration.spec.ts
    - tests/e2e/helpers/opportunities.ts
    - .planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - .planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: the placeholder overview route fails shell/stepper/layout assertions.
    - Test 2: the route fails shared `loading`, `empty`, `error`, `blocked`, `retry`, and `success` state assertions because no opportunity-level state surface exists yet.
    - Test 3: the current page fails opportunity existence/not-found and no-source assertions because the route does not yet present a `ProductStateBlock` or equivalent state surface.
    - Test 4: the current page fails raw-input editing, save-trust, file-state, retry, and blocked-to-ready assertions.
    - Test 5: the current page fails successful `/lead-brief` handoff assertions.
  </behavior>
  <action>
    Create failing browser specs and helper wiring only.

    1. Add `tests/e2e/opportunity-overview-shell.spec.ts` with explicit assertions for:
       - eyebrow `Opportunity intake`
       - stepper labels `Lead Intake`, `Lead Brief`, `Discovery`, `Proposal Draft`, `Follow-up`
       - header -> stepper -> working surface -> recovery-notice hierarchy
       - route-level `loading`, `empty`, `error`, `blocked`, `retry`, and `success` surfaces using the existing `ProductStateBlock` pattern or a direct extension of it
       - opportunity existence/not-found handling and the no-source empty workspace state from `04-UI-SPEC.md`

    2. Add `tests/e2e/opportunity-intake.spec.ts` with explicit assertions for:
       - raw-input editing and save trust copy
       - file states `uploaded`, `processing`, `ready`, `failed`
       - `Retry extraction`
       - blocked reasons from the UI contract
       - route-level retry and success surfaces that remain visible above inline file-state UI
       - successful redirect to `/opportunities/{id}/lead-brief`

    3. Add `tests/e2e/helpers/opportunity-overview.ts` with deterministic selectors and helper flows for the new workspace.

    4. Capture the RED state by running the targeted browser commands against the existing app state and recording the non-zero exits.
  </action>
  <verify>
    <automated>grep -q 'Opportunity intake' tests/e2e/opportunity-overview-shell.spec.ts && grep -q 'ProductStateBlock' tests/e2e/opportunity-overview-shell.spec.ts && grep -q 'loading' tests/e2e/opportunity-overview-shell.spec.ts && grep -q 'No source material yet.' tests/e2e/opportunity-overview-shell.spec.ts && grep -q 'Retry extraction' tests/e2e/opportunity-intake.spec.ts && grep -q 'Generate lead brief' tests/e2e/opportunity-intake.spec.ts && ! pnpm exec playwright test tests/e2e/opportunity-overview-shell.spec.ts && ! pnpm exec playwright test tests/e2e/opportunity-intake.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/opportunity-overview-shell.spec.ts` and `tests/e2e/opportunity-intake.spec.ts` exist and assert the locked Phase 4 labels.
    - `tests/e2e/opportunity-overview-shell.spec.ts` names `loading`, `empty`, `error`, `blocked`, `retry`, `success`, and opportunity not-found handling.
    - The RED Playwright verification command exits `0` because both targeted specs fail against the pre-implementation UI.
    - No production app files are modified in this task.
  </acceptance_criteria>
  <done>
    The browser side of Phase 4 has a real RED state before any UI implementation begins.
  </done>
</task>

</tasks>

<verification>
Wave 0 TDD is complete only when the failing API and browser specs are authored and their RED-state commands succeed by proving non-zero test exits for D-30 APIs and shared route-state behavior.
</verification>

<success_criteria>
- API RED state is captured for overview, inputs CRUD-lite, file lifecycle, and generate gate.
- Browser RED state is captured for shell, shared route states, opportunity existence/not-found handling, editing, file-state, and handoff behavior.
- Later plans can move directly into implementation without reworking the test harness.
</success_criteria>

<output>
After completion, create `.planning/phases/04-opportunity-intake-file-processing/04-02-SUMMARY.md`
</output>
