---
phase: 04-opportunity-intake-file-processing
plan: 05
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
  - apps/web/components/opportunities/opportunity-overview-shell.tsx
  - apps/web/components/opportunities/opportunity-stepper.tsx
  - apps/web/components/opportunities/intake-source-panel.tsx
  - apps/web/components/opportunities/intake-context-panel.tsx
  - apps/web/lib/opportunities-api.ts
  - apps/web/lib/opportunity-intake-state.ts
  - apps/web/app/globals.css
  - tests/e2e/opportunity-overview-shell.spec.ts
  - tests/e2e/opportunity-intake.spec.ts
autonomous: true
requirements:
  - OPP-02
  - UI-02
  - INTAKE-01
must_haves:
  truths:
    - "The overview page must feel like the top of one opportunity container inside the Phase 3 shell."
    - "Frontend client wiring must call explicit opportunity detail and inputs CRUD-lite endpoints rather than inventing page-local state."
    - "Opportunity-level `loading`, `empty`, `error`, `blocked`, `retry`, `success`, and not-found handling must use `ProductStateBlock` or an explicit extension of it rather than hiding inside the file module."
  artifacts:
    - path: "apps/web/components/opportunities/opportunity-stepper.tsx"
      provides: "Stable five-step opportunity workflow chrome"
    - path: "apps/web/components/opportunities/intake-source-panel.tsx"
      provides: "Source-material workspace"
    - path: "apps/web/components/opportunities/intake-context-panel.tsx"
      provides: "Minimum-context workspace"
    - path: "apps/web/lib/opportunities-api.ts"
      provides: "Explicit overview and input CRUD-lite client calls"
    - path: "apps/web/app/opportunities/[opportunityId]/[step]/page.tsx"
      provides: "Route-level opportunity existence and shared-state orchestration"
  key_links:
    - from: "apps/web/lib/opportunities-api.ts"
      to: "apps/api/app/opportunity_input_routes.py"
      via: "GET/POST/PATCH inputs client wiring"
      pattern: "/inputs|input_id"
    - from: "apps/web/app/opportunities/[opportunityId]/[step]/page.tsx"
      to: "apps/web/components/product-shell.tsx"
      via: "Phase 3 shell continuity"
      pattern: "Opportunity intake|Lead Intake"
    - from: "apps/web/app/opportunities/[opportunityId]/[step]/page.tsx"
      to: "apps/web/components/product-state-block.tsx"
      via: "route-level loading/empty/error/blocked/retry/success and not-found surfaces"
      pattern: "ProductStateBlock|loading|empty|error|blocked|retry|success"
---

<objective>
Implement the overview shell, explicit inputs client wiring, and draft-preserving editing UI.

Purpose: the page must productize the opportunity container while consuming the real overview and inputs endpoints rather than hiding everything behind one local form.
Output: browser-verified opportunity shell, stepper, source/context split, explicit input CRUD-lite client calls, and inline save trust states.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
@.planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
@.planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
@.planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
@apps/web/components/product-shell.tsx
@apps/web/components/product-state-block.tsx
@apps/web/app/globals.css
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the placeholder route with the real overview container shell</name>
  <files>apps/web/app/opportunities/[opportunityId]/[step]/page.tsx, apps/web/components/opportunities/opportunity-overview-shell.tsx, apps/web/components/opportunities/opportunity-stepper.tsx, apps/web/app/globals.css, tests/e2e/opportunity-overview-shell.spec.ts</files>
  <read_first>
    - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
    - apps/web/components/product-shell.tsx
    - apps/web/components/product-state-block.tsx
    - apps/web/app/globals.css
    - tests/e2e/opportunity-overview-shell.spec.ts
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - .planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: the overview route shows `Opportunity intake` and the five workflow steps in order.
    - Test 2: the layout order is header, stepper, working surface, then recovery notices.
    - Test 3: the route preserves Phase 3 shell continuity.
    - Test 4: the route uses `ProductStateBlock` or an explicit extension for `loading`, `empty`, `error`, `blocked`, `retry`, `success`, and opportunity not-found handling instead of collapsing those states into inline file UI.
  </behavior>
  <action>
    Run the overview-shell spec from RED first, then replace the placeholder overview branch.

    1. Create the real overview branch in `apps/web/app/opportunities/[opportunityId]/[step]/page.tsx`.
    2. Create `apps/web/components/opportunities/opportunity-overview-shell.tsx` and `apps/web/components/opportunities/opportunity-stepper.tsx`.
    3. Keep the five step labels exact: `Lead Intake`, `Lead Brief`, `Discovery`, `Proposal Draft`, `Follow-up`.
    4. Use `apps/web/components/product-state-block.tsx` or a direct extension of it for route-level `loading`, `empty`, `error`, `blocked`, `retry`, `success`, and not-found surfaces. Keep the no-source state heading exact: `No source material yet.`
    5. Update `apps/web/app/globals.css` for the approved desktop/laptop asymmetric desk layout only; no mobile/tablet redesign and no shadcn.
  </action>
  <verify>
    <automated>grep -q 'ProductStateBlock' apps/web/app/opportunities/[opportunityId]/[step]/page.tsx && pnpm exec playwright test tests/e2e/opportunity-overview-shell.spec.ts -g 'Opportunity intake|loading|empty|error|blocked|retry|success|not found'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/opportunities/[opportunityId]/[step]/page.tsx` renders `Opportunity intake` for the overview route.
    - `apps/web/app/opportunities/[opportunityId]/[step]/page.tsx` references `ProductStateBlock` or its direct extension for route-level state handling.
    - `apps/web/components/opportunities/opportunity-stepper.tsx` contains all five step labels.
    - The targeted overview-shell Playwright command exits `0`.
  </acceptance_criteria>
  <done>
    The overview route now feels like the top of the opportunity workflow container rather than a Phase 3 placeholder.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire explicit detail and inputs CRUD-lite clients into the editable intake workspace</name>
  <files>apps/web/components/opportunities/intake-source-panel.tsx, apps/web/components/opportunities/intake-context-panel.tsx, apps/web/lib/opportunities-api.ts, apps/web/lib/opportunity-intake-state.ts, apps/web/app/globals.css, tests/e2e/opportunity-intake.spec.ts</files>
  <read_first>
    - apps/web/lib/opportunities-api.ts
    - apps/web/app/globals.css
    - tests/e2e/opportunity-intake.spec.ts
    - packages/shared-config/index.ts
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - .planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: users can edit minimum-context fields and manual raw input, then refresh and see persisted values.
    - Test 2: save trust states `Saving…`, `Saved just now`, and `Save failed` are visible inline.
    - Test 3: the source workspace uses explicit inputs CRUD-lite client calls, not an overloaded detail PATCH for raw input.
    - Test 4: the empty/no-source route state keeps the working layout visible and routes the next action into raw input or PDF upload.
  </behavior>
  <action>
    Run the editing/autosave browser spec from RED first, then implement the real workspace wiring.

    1. In `apps/web/lib/opportunities-api.ts`, add explicit client methods for:
       - overview detail read/update
       - `GET /api/v1/opportunities/{opportunity_id}/inputs`
       - `POST /api/v1/opportunities/{opportunity_id}/inputs`
       - `PATCH /api/v1/opportunities/{opportunity_id}/inputs/{input_id}`

    2. Use those client methods from:
       - `apps/web/components/opportunities/intake-source-panel.tsx` for manual raw-input persistence
       - `apps/web/components/opportunities/intake-context-panel.tsx` for minimum-context editing

    3. Keep the exact field set from D-07 and the UI contract. `contact_email` validates only when present.

    4. In `apps/web/lib/opportunity-intake-state.ts`, centralize meaningful-manual-text detection, inline save-state copy, and the route-level empty/no-source decision so the page can show the correct `ProductStateBlock` variant without losing the working layout.
  </action>
  <verify>
    <automated>grep -q '/inputs' apps/web/lib/opportunities-api.ts && grep -q 'No source material yet.' apps/web/app/opportunities/[opportunityId]/[step]/page.tsx && pnpm exec playwright test tests/e2e/opportunity-intake.spec.ts -g 'edit|autosave|save failed|no source'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/opportunities-api.ts` contains explicit input CRUD-lite client calls.
    - `apps/web/components/opportunities/intake-source-panel.tsx` includes `Raw input`.
    - `apps/web/components/opportunities/intake-context-panel.tsx` includes the minimum-context field set.
    - The targeted browser editing/autosave/no-source command exits `0`.
  </acceptance_criteria>
  <done>
    The overview workspace now uses the real D-30 input surface and preserves edits with inline trust states.
  </done>
</task>

</tasks>

<verification>
The overview UI is ready only when shell continuity, route-level shared states, explicit input client wiring, and editing/save trust behavior are green in the browser.
</verification>

<success_criteria>
- The overview page preserves Phase 3 shell continuity.
- The route uses `ProductStateBlock`-style opportunity state surfaces for `loading`, `empty`, `error`, `blocked`, `retry`, `success`, and not-found handling.
- Source material and minimum context are visibly separate.
- The frontend explicitly uses the D-30 input routes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-opportunity-intake-file-processing/04-05-SUMMARY.md`
</output>
