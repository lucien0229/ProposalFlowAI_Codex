---
phase: 04-opportunity-intake-file-processing
plan: 06
type: execute
wave: 5
depends_on:
  - "04"
  - "05"
files_modified:
  - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
  - apps/web/components/opportunities/intake-file-panel.tsx
  - apps/web/components/opportunities/extracted-preview.tsx
  - apps/web/components/opportunities/intake-action-bar.tsx
  - apps/web/lib/opportunities-api.ts
  - apps/web/lib/opportunity-intake-state.ts
  - apps/web/app/globals.css
  - tests/e2e/opportunity-intake.spec.ts
  - tests/e2e/opportunity-overview-shell.spec.ts
autonomous: false
requirements:
  - OPP-02
  - UI-02
  - INTAKE-02
  - INTAKE-03
must_haves:
  truths:
    - "File state must stay visible in-page as uploaded, processing, ready, or failed with retry recovery."
    - "Generate lead brief must explain blocked reasons and redirect directly into `/lead-brief` when it succeeds."
    - "Route-level error, retry, success, blocked, and not-found handling must remain visible through `ProductStateBlock` or an equivalent shared state surface, not only through inline file-state UI."
  artifacts:
    - path: "apps/web/components/opportunities/intake-file-panel.tsx"
      provides: "File lifecycle UI and retry controls"
    - path: "apps/web/components/opportunities/intake-action-bar.tsx"
      provides: "Primary/secondary action hierarchy and blocked messaging"
    - path: "apps/web/app/opportunities/[opportunityId]/[step]/page.tsx"
      provides: "Final route-level state wiring and opportunity existence handling"
    - path: "tests/e2e/opportunity-intake.spec.ts"
      provides: "Browser verification for file lifecycle and handoff"
  key_links:
    - from: "apps/web/components/opportunities/intake-file-panel.tsx"
      to: "apps/web/lib/opportunities-api.ts"
      via: "upload-url, complete, detail polling, and retry calls"
      pattern: "upload-url|complete|retry|ready|failed"
    - from: "apps/web/components/opportunities/intake-action-bar.tsx"
      to: "apps/web/app/opportunities/[opportunityId]/[step]/page.tsx"
      via: "generate redirect into /lead-brief"
      pattern: "Generate lead brief|Save opportunity|lead-brief"
    - from: "apps/web/app/opportunities/[opportunityId]/[step]/page.tsx"
      to: "apps/web/components/product-state-block.tsx"
      via: "final route-level retry/success/error/blocked/not-found surfaces"
      pattern: "ProductStateBlock|retry|success|error|blocked|not found"
---

<objective>
Finish Phase 4 with visible file recovery UX, generate-gate messaging, and final verification.

Purpose: the phase is not done until file lifecycle UI, blocked-to-ready transitions, and the lead-brief handoff work in the browser and survive human review.
Output: launch-grade file-state UI, action bar, browser regressions, and a final human verification gate.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
@.planning/phases/04-opportunity-intake-file-processing/04-RESEARCH.md
@.planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
@.planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
@apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
@apps/web/lib/opportunities-api.ts
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement visible file states, extracted preview, and retry recovery</name>
  <files>apps/web/components/opportunities/intake-file-panel.tsx, apps/web/components/opportunities/extracted-preview.tsx, apps/web/lib/opportunities-api.ts, apps/web/lib/opportunity-intake-state.ts, apps/web/app/globals.css, tests/e2e/opportunity-intake.spec.ts</files>
  <read_first>
    - apps/web/lib/opportunities-api.ts
    - apps/web/lib/opportunity-intake-state.ts
    - tests/e2e/opportunity-intake.spec.ts
    - .planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
    - .planning/phases/04-opportunity-intake-file-processing/04-CONTEXT.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: upload shows `uploaded`, then `processing`, then `ready` with extracted preview.
    - Test 2: failed extraction shows `Retry extraction` and does not block manual raw-input editing.
    - Test 3: retry starts a fresh processing attempt.
    - Test 4: file-level retry never replaces the route-level `retry` or `error` surface when the entire opportunity fetch fails.
  </behavior>
  <action>
    Run the file-state browser spec from RED first, then implement the file UI.

    1. Create `apps/web/components/opportunities/intake-file-panel.tsx` and `apps/web/components/opportunities/extracted-preview.tsx`.
    2. Use `apps/web/lib/opportunities-api.ts` for upload-url, complete, detail polling, and retry.
    3. Keep the exact UI copy:
       - `Processing your PDF. Keep editing raw input while extraction runs.`
       - `Processing failed. Retry extraction or continue with manual source material.`
    4. Stop polling only on `ready` or `failed`.
    5. Keep file-level state inside the file module; do not replace route-level `ProductStateBlock` handling when the entire record is unavailable.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/opportunity-intake.spec.ts -g 'file states|retry'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/opportunities/intake-file-panel.tsx` contains `Retry extraction`.
    - `apps/web/components/opportunities/extracted-preview.tsx` exists.
    - The targeted file-state browser command exits `0`.
  </acceptance_criteria>
  <done>
    File processing is now visible and recoverable from the overview page.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement generate gate messaging and direct lead-brief handoff</name>
  <files>apps/web/app/opportunities/[opportunityId]/[step]/page.tsx, apps/web/components/opportunities/intake-action-bar.tsx, apps/web/lib/opportunities-api.ts, apps/web/lib/opportunity-intake-state.ts, apps/web/app/globals.css, tests/e2e/opportunity-intake.spec.ts, tests/e2e/opportunity-overview-shell.spec.ts</files>
  <read_first>
    - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
    - apps/web/lib/opportunities-api.ts
    - apps/web/lib/opportunity-intake-state.ts
    - tests/e2e/opportunity-intake.spec.ts
    - tests/e2e/opportunity-overview-shell.spec.ts
    - .planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: `Generate lead brief` is primary and `Save opportunity` is secondary.
    - Test 2: blocked messages are explicit, not tooltip-only or silent disabled states.
    - Test 3: success redirects to `/opportunities/{id}/lead-brief`.
    - Test 4: route-level `error`, `retry`, `success`, and opportunity not-found surfaces remain visible and distinct from file-state messaging.
  </behavior>
  <action>
    Run the blocked-to-ready browser spec from RED first, then implement the action area.

    1. Create `apps/web/components/opportunities/intake-action-bar.tsx`.
    2. Keep the exact blocked messages from the UI contract.
    3. In `apps/web/app/opportunities/[opportunityId]/[step]/page.tsx`, finish the route-level state orchestration using `apps/web/components/product-state-block.tsx` or its direct extension so `error`, `retry`, `success`, `blocked`, and not-found surfaces remain explicit above inline file-state UI.
    4. Wire the generate action to the server endpoint from Plan 03 and redirect directly into `/opportunities/{id}/lead-brief`.
    5. Keep the `lead-brief` route branch as a Phase 4 handoff seam only; do not pull Phase 5 structured brief UI forward.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_lead_brief_gate_api.py -q && grep -q 'ProductStateBlock' apps/web/app/opportunities/[opportunityId]/[step]/page.tsx && pnpm exec playwright test tests/e2e/opportunity-intake.spec.ts -g 'blocked to ready|lead brief|retry|success|not found'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/opportunities/intake-action-bar.tsx` contains `Generate lead brief` and `Save opportunity`.
    - `apps/web/app/opportunities/[opportunityId]/[step]/page.tsx` contains route-level `ProductStateBlock` or direct-extension usage for shared product states.
    - The exact blocked messages are present in app code.
    - The targeted gate + browser handoff command exits `0`.
  </acceptance_criteria>
  <done>
    The overview page now explains readiness clearly and hands users into the next workflow step directly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify the intake desk and recovery clarity</name>
  <files>apps/web/app/opportunities/[opportunityId]/[step]/page.tsx, apps/web/components/opportunities/intake-file-panel.tsx, apps/web/components/opportunities/intake-action-bar.tsx, tests/e2e/opportunity-overview-shell.spec.ts, tests/e2e/opportunity-intake.spec.ts</files>
  <read_first>
    - .planning/phases/04-opportunity-intake-file-processing/04-UI-SPEC.md
    - .planning/phases/04-opportunity-intake-file-processing/04-VALIDATION.md
    - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
    - apps/web/components/opportunities/intake-file-panel.tsx
    - apps/web/components/opportunities/intake-action-bar.tsx
  </read_first>
  <action>
    Perform the final human verification pass after the targeted API and Playwright commands are green.

    Confirm the page still reads as a restrained desktop/laptop intake desk, not a marketing upload page or a mobile/tablet layout.
  </action>
  <what-built>Complete overview workspace with source/context split, visible file states, retry recovery, blocked messaging, and lead-brief handoff</what-built>
  <how-to-verify>
    1. Open an opportunity overview at roughly `1280x800` and `1440x900`.
    2. Confirm the visible hierarchy is header, stepper, working surface, then recovery notices.
    3. Exercise route-level `loading`, `empty/no-source`, `error`, `blocked`, `retry`, `success`, and not-found states; confirm they use the shared `ProductStateBlock` vocabulary or its direct extension and stay distinct from file-state UI.
    4. Exercise `processing`, `failed`, `retry`, and `ready`; confirm file state stays visible in-page and never collapses into toast-only feedback.
    5. Trigger each blocked generation reason and confirm the text is understandable before the CTA becomes enabled.
    6. Generate the lead brief and confirm the browser lands on `/opportunities/{id}/lead-brief`.
  </how-to-verify>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api -q && pnpm exec playwright test tests/e2e/opportunity-overview-shell.spec.ts tests/e2e/opportunity-intake.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Visual review confirms the asymmetric desk layout and Phase 3 shell continuity at `1280px` and `1440px`.
    - Visual review confirms route-level shared states remain distinct from file-state messaging.
    - File states are operationally clear during manual review.
    - The full targeted API + Playwright regression command exits `0`.
  </acceptance_criteria>
  <done>
    Human review confirms the page is credible enough to hand into Phase 5.
  </done>
  <resume-signal>Type `approved` if the page matches the contract, or list the issues that still need to be fixed.</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete only when route-level shared states, file-state recovery, generate-gate messaging, and the `/lead-brief` handoff are green in automation and visually confirmed by a human.
</verification>

<success_criteria>
- File states are visible and recoverable in the browser.
- Route-level `loading`, `empty`, `error`, `blocked`, `retry`, `success`, and not-found surfaces are explicit and use shared product-state vocabulary.
- Generate gating is explicit and accurate.
- Users are handed directly into the Lead Brief step.
- Human review confirms the page still feels like a restrained intake desk.
</success_criteria>

<output>
After completion, create `.planning/phases/04-opportunity-intake-file-processing/04-06-SUMMARY.md`
</output>
