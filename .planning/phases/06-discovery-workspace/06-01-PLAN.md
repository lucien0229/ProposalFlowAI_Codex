---
phase: 06-discovery-workspace
plan: 01
type: execute
wave: 1
depends_on:
  - "05"
files_modified:
  - tests/api/test_discovery_routes_api.py
  - tests/api/test_discovery_api.py
  - packages/shared-types/index.ts
  - packages/shared-schemas/index.ts
  - packages/shared-config/index.ts
autonomous: true
requirements:
  - DISC-01
  - DISC-02
must_haves:
  truths:
    - "Phase 6 starts with red tests and frozen contracts. No backend implementation should be attempted before the discovery contract is explicit."
    - "Discovery UI/UX must follow `.planning/phases/06-discovery-workspace/06-UI-SPEC.md` and `docs/design/DESIGN.md`; landing-page motifs, chat bubbles, and analytics visuals are out of bounds."
    - "This phase is web-only and desktop/laptop-only. No mobile/tablet design work is allowed."
    - "TDD is mandatory: the first visible work in this phase is failing contract coverage, not implementation shortcuts."
  artifacts:
    - path: "tests/api/test_discovery_routes_api.py"
      provides: "Canonical route slug and endpoint drift coverage"
    - path: "tests/api/test_discovery_api.py"
      provides: "Current-resource, version history, restore, conflict, and weak-evidence coverage"
    - path: "packages/shared-config/index.ts"
      provides: "Discovery route builders and API path templates"
  key_links:
    - from: "tests/api/test_discovery_api.py"
      to: ".planning/phases/06-discovery-workspace/06-RESEARCH.md"
      via: "contract expectations for evidence gating and restore semantics"
      pattern: "expected_revision_no|Needs more evidence|409|reload_hint"
    - from: "packages/shared-config/index.ts"
      to: ".planning/phases/06-discovery-workspace/06-CONTEXT.md"
      via: "Discovery route and API path canonicalization"
      pattern: "discovery|/discovery|versions|restore"
---

<objective>
Freeze the Discovery contract before implementation by making route drift, DTO drift, weak-evidence behavior, and restore/concurrency rules visible in red tests.

This wave does not build the workspace UI or backend yet. It makes the API and shared contract expectations precise enough that later waves can implement without re-opening scope.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-discovery-workspace/06-CONTEXT.md
@.planning/phases/06-discovery-workspace/06-RESEARCH.md
@.planning/phases/06-discovery-workspace/06-UI-SPEC.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
@/Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
@docs/design/DESIGN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Discovery route and API contract tests first</name>
  <files>tests/api/test_discovery_routes_api.py, tests/api/test_discovery_api.py</files>
  <read_first>
    - tests/api/test_discovery_api.py
    - tests/api/test_discovery_routes_api.py
    - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
    - apps/api/app/opportunity_service.py
    - packages/shared-config/index.ts
    - packages/shared-types/index.ts
    - .planning/phases/06-discovery-workspace/06-CONTEXT.md
    - .planning/phases/06-discovery-workspace/06-RESEARCH.md
    - .planning/phases/06-discovery-workspace/06-UI-SPEC.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test the canonical public route segment `discovery` and the route helper / API path pairing.
    - Test `GET /api/v1/opportunities/{opportunity_id}/discovery` returns the current resource plus version history.
    - Test `PATCH /api/v1/opportunities/{opportunity_id}/discovery` requires `expected_revision_no` and persists the current working copy only.
    - Test `POST /api/v1/opportunities/{opportunity_id}/discovery/save-version` creates an immutable snapshot.
    - Test `GET /api/v1/opportunities/{opportunity_id}/discovery/versions` and `/versions/{version_no}` return list/detail payloads.
    - Test `POST /api/v1/opportunities/{opportunity_id}/discovery/versions/{version_no}/restore` replaces the current working copy and rejects stale revisions with `409 Conflict`.
    - Test weak-evidence behavior returns an explicit `Needs more evidence` style notice and a conservative gating payload instead of a silent block.
  </behavior>
  <action>
    Start from RED. Create the discovery route/API contract tests first, encode the expected paths and payload shapes literally, and make the missing backend behavior impossible to miss.

    1. Add `tests/api/test_discovery_routes_api.py` for route-slug and path canonicalization coverage.
    2. Add `tests/api/test_discovery_api.py` for current resource, save current, save version, version list/detail, restore, conflict, and weak-evidence contract coverage.
    3. Keep the assertions specific to discovery; do not reuse Lead Brief test names or prose.
    4. Keep browser-facing recovery language explicit so later UI work cannot collapse it into generic error copy.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_discovery_routes_api.py tests/api/test_discovery_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The two discovery API test files exist and encode the route, current-resource, version, restore, conflict, and weak-evidence expectations.
    - The targeted pytest command is red before backend implementation.
    - The test names and assertions explicitly mention `discovery`, `expected_revision_no`, `409 Conflict`, and `Needs more evidence`.
  </acceptance_criteria>
  <done>
    Discovery behavior is frozen in failing tests before implementation starts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend shared Discovery contracts and route helpers</name>
  <files>packages/shared-types/index.ts, packages/shared-schemas/index.ts, packages/shared-config/index.ts</files>
  <read_first>
    - packages/shared-types/index.ts
    - packages/shared-schemas/index.ts
    - packages/shared-config/index.ts
    - .planning/phases/05-lead-brief-workspace/05-CONTEXT.md
    - .planning/phases/06-discovery-workspace/06-CONTEXT.md
    - .planning/phases/06-discovery-workspace/06-RESEARCH.md
    - .planning/phases/06-discovery-workspace/06-UI-SPEC.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Add the Discovery shared contract layer in one pass so later backend and web waves can depend on a stable DTO surface.

    1. Extend `packages/shared-types/index.ts` with:
       - `DiscoverySourceNote`
       - `DiscoverySourceNotes`
       - `DiscoveryFieldState`
       - `DiscoveryFieldKey`
       - `DiscoveryFieldValue`
       - `DiscoveryFields`
       - `DiscoveryCurrentResource`
       - `DiscoveryVersion`
       - `DiscoveryCurrentResourceResponse`
       - `DiscoveryVersionListResponse`
       - `DiscoveryVersionDetailResponse`
       - `DiscoverySaveCurrentRequest`
       - `DiscoverySaveVersionRequest`
       - `DiscoveryRestoreRequest`
       - `DiscoveryConflictResponse`
       - `DiscoveryGenerateResponse`
       - `DiscoverySourceNotesRequest`
    2. Extend `packages/shared-schemas/index.ts` with matching schemas for all of the above.
    3. Extend `packages/shared-config/index.ts` with exact builders and route templates for:
       - `GET /api/v1/opportunities/{opportunity_id}/discovery`
       - `PATCH /api/v1/opportunities/{opportunity_id}/discovery`
       - `POST /api/v1/opportunities/{opportunity_id}/discovery/save-version`
       - `GET /api/v1/opportunities/{opportunity_id}/discovery/versions`
       - `GET /api/v1/opportunities/{opportunity_id}/discovery/versions/{version_no}`
       - `POST /api/v1/opportunities/{opportunity_id}/discovery/versions/{version_no}/restore`
    4. Keep the DTOs field-oriented and version-oriented; do not introduce free-form prose blobs.
    5. Include a source-notes contract that can persist user-authored discovery notes in the current resource and version snapshots.
  </action>
  <verify>
    <automated>pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Shared Discovery types, schemas, and route helpers exist with the exact discovery path segment.
    - The shared contract names are consistent with the Lead Brief versioning pattern and do not introduce a second vocabulary for the same semantics.
    - The shared contract includes a source-notes shape that later waves can render and persist from the left-hand source pane.
    - `pnpm lint` exits successfully after the contract files are updated.
  </acceptance_criteria>
  <done>
    The Discovery contract surface is explicit and ready for backend implementation.
  </done>
</task>

</tasks>

<verification>
Wave 1 is complete only when Discovery route drift is test-covered, the shared DTO/route layer is explicit, and the UI contract plus design baseline are referenced for later implementation.
</verification>

<success_criteria>
- Discovery route drift is impossible to miss in tests.
- Discovery DTOs and route helpers are frozen before backend implementation.
- The phase explicitly preserves TDD, shipping UI quality, and web-only scope.
</success_criteria>
