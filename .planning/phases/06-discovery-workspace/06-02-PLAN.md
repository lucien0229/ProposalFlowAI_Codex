---
phase: 06-discovery-workspace
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/api/app/discovery_models.py
  - apps/api/app/discovery_repository.py
  - apps/api/app/discovery_service.py
  - apps/api/app/discovery_routes.py
  - apps/api/alembic/versions/0008_phase6_discovery_workspace.py
  - apps/api/app/main.py
  - tests/api/test_discovery_api.py
autonomous: true
requirements:
  - DISC-01
  - DISC-02
must_haves:
  truths:
    - "Backend implementation must satisfy the red contract tests from Wave 1 and preserve current-resource/version-history semantics."
    - "All write paths must use optimistic concurrency and explicit overwrite awareness."
    - "Restore must replace the current working copy without silently creating a new history row."
    - "Weak evidence must be a first-class recoverable state, not a generic API failure."
  artifacts:
    - path: "apps/api/app/discovery_service.py"
      provides: "Discovery generation, save, version, restore, and gating semantics"
    - path: "apps/api/alembic/versions/0008_phase6_discovery_workspace.py"
      provides: "Current resource and immutable version snapshot persistence"
    - path: "apps/api/app/discovery_routes.py"
      provides: "Discovery HTTP contract and error mapping"
  key_links:
    - from: "apps/api/app/discovery_service.py"
      to: "apps/api/app/lead_brief_service.py"
      via: "current-resource and restore semantics"
      pattern: "current_revision_no|save_version|restore|409"
    - from: "apps/api/app/discovery_routes.py"
      to: ".planning/phases/06-discovery-workspace/06-RESEARCH.md"
      via: "evidence gate and thin-input recovery expectations"
      pattern: "Needs more evidence|reload_hint|expected_revision_no"
---

<objective>
Implement the Discovery backend so the red contract tests from Wave 1 turn green with a real current-resource, version history, restore, and weak-evidence workflow.

This wave covers the data model and HTTP contract. It does not finish the customer UI.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-discovery-workspace/06-CONTEXT.md
@.planning/phases/06-discovery-workspace/06-RESEARCH.md
@.planning/phases/06-discovery-workspace/06-UI-SPEC.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Discovery data model, repository, service, and migration</name>
  <files>apps/api/app/discovery_models.py, apps/api/app/discovery_repository.py, apps/api/app/discovery_service.py, apps/api/alembic/versions/0008_phase6_discovery_workspace.py</files>
  <read_first>
    - apps/api/app/lead_brief_models.py
    - apps/api/app/lead_brief_repository.py
    - apps/api/app/lead_brief_service.py
    - apps/api/app/opportunity_models.py
    - apps/api/app/opportunity_repository.py
    - apps/api/app/opportunity_service.py
    - packages/shared-types/index.ts
    - packages/shared-schemas/index.ts
    - .planning/phases/06-discovery-workspace/06-CONTEXT.md
    - .planning/phases/06-discovery-workspace/06-RESEARCH.md
    - .planning/phases/06-discovery-workspace/06-UI-SPEC.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Implement Discovery as a real current-resource workspace with immutable version snapshots.

    1. Add Discovery persistence models for the current resource and version history.
    2. Add repository helpers for create/read/update/list/restore behavior.
    3. Add service methods for generate, regenerate, save current, save version, version list/detail, and restore.
    4. Add optimistic concurrency with `expected_revision_no`.
    5. Encode the weak-evidence gate so thin source material returns a visible recovery state instead of a silent failure.
    6. Persist user-authored discovery source notes alongside the current resource and version snapshots.
    7. Add the Phase 6 Alembic migration that creates the Discovery tables and necessary indexes.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_discovery_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Discovery current-resource and version-snapshot tables exist in the migration.
    - The service layer can save current, save version, list versions, fetch version detail, and restore a selected version.
    - Discovery source notes can be saved and reloaded with the current resource.
    - Stale `expected_revision_no` returns `409 Conflict` with a reload hint.
    - Weak evidence returns an explicit gated response instead of an unhelpful generic error.
  </acceptance_criteria>
  <done>
    Discovery persistence and business logic are real and the contract tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the Discovery HTTP routes and route into the opportunity workflow</name>
  <files>apps/api/app/discovery_routes.py, apps/api/app/main.py, apps/api/app/opportunity_service.py</files>
  <read_first>
    - apps/api/app/discovery_routes.py
    - apps/api/app/main.py
    - apps/api/app/opportunity_service.py
    - apps/api/app/lead_brief_routes.py
    - apps/api/app/lead_brief_service.py
    - .planning/phases/06-discovery-workspace/06-CONTEXT.md
    - .planning/phases/06-discovery-workspace/06-RESEARCH.md
    - .planning/phases/06-discovery-workspace/06-UI-SPEC.md
  </read_first>
  <action>
    Wire the Discovery backend into the API surface and the opportunity workflow.

    1. Register the Discovery router in the FastAPI app with the exact `/api/v1/opportunities/{opportunity_id}/discovery` family of paths.
    2. Map API errors into the same conservative style used by Lead Brief, including explicit 404, 409, and blocked-state payloads.
    3. Make opportunity workflow serialization expose Discovery readiness and dependency context so the web app can render the correct blockers and handoffs.
    4. Keep the public route segment canonical as `discovery`; do not introduce a snake_case public URL.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_discovery_routes_api.py tests/api/test_discovery_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The FastAPI app exposes the Discovery route family under the discovery slug.
    - API responses include conservative conflict and not-found handling.
    - The discovery API tests from Wave 1 now pass.
  </acceptance_criteria>
  <done>
    The backend contract is live and the opportunity workflow can surface Discovery state.
  </done>
</task>

</tasks>

<verification>
Wave 2 is complete only when the Discovery API tests pass and the route is registered cleanly in the opportunity workflow.
</verification>

<success_criteria>
- Discovery generation, save, version, and restore are real backend behaviors.
- The backend preserves optimistic concurrency and explicit overwrite protection.
- Discovery readiness can be surfaced to the web app without ad hoc route hacks.
</success_criteria>
