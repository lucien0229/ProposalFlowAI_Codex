---
phase: 06-discovery-workspace
plan: 04
type: execute
wave: 4
depends_on:
  - "03"
files_modified:
  - tests/e2e/discovery-workspace.spec.ts
  - apps/web/components/opportunities/discovery-workspace.tsx
  - apps/web/components/opportunities/discovery-output-pane.tsx
  - apps/web/components/opportunities/discovery-version-drawer.tsx
  - apps/web/app/opportunities/[opportunityId]/[step]/loading.tsx
  - apps/web/app/opportunities/[opportunityId]/[step]/error.tsx
  - apps/web/app/globals.css
autonomous: true
requirements:
  - DISC-01
  - DISC-02
must_haves:
  truths:
    - "This is the release-hardening wave: browser verification, recovery flows, and final polish are mandatory."
    - "The browser coverage must run on desktop Chromium only; no mobile or tablet emulation."
    - "Every destructive action must be verified with an explicit confirm or recovery path."
    - "The final result must still satisfy the web-only, desktop/laptop-only product constraint."
  artifacts:
    - path: "tests/e2e/discovery-workspace.spec.ts"
      provides: "Browser-level coverage for happy path, weak evidence, restore, conflict, and recovery states"
    - path: "apps/web/components/opportunities/discovery-workspace.tsx"
      provides: "Final UI polish and recovery flow wiring"
  key_links:
    - from: "tests/e2e/discovery-workspace.spec.ts"
      to: ".planning/phases/06-discovery-workspace/06-UI-SPEC.md"
      via: "browser verification of state, drawer, and recovery contracts"
      pattern: "loading|empty|error|blocked|retry|success|restore|Needs more evidence"
    - from: "tests/e2e/discovery-workspace.spec.ts"
      to: "apps/api/app/discovery_routes.py"
      via: "end-to-end workflow and conflict recovery"
      pattern: "expected_revision_no|409|restore|save-version"
---

<objective>
Prove the Discovery workspace is production-ready by covering the full user journey in the browser: load, recover, generate, edit, save, version, restore, conflict, and weak-evidence handling.

This wave is the final productization gate for Phase 6.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-discovery-workspace/06-CONTEXT.md
@.planning/phases/06-discovery-workspace/06-RESEARCH.md
@.planning/phases/06-discovery-workspace/06-UI-SPEC.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author browser E2E coverage for the full Discovery journey</name>
  <files>tests/e2e/discovery-workspace.spec.ts</files>
  <read_first>
    - tests/e2e/discovery-workspace.spec.ts
    - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
    - apps/web/components/opportunities/discovery-workspace.tsx
    - apps/web/components/opportunities/discovery-output-pane.tsx
    - apps/web/components/opportunities/discovery-version-drawer.tsx
    - apps/web/lib/discovery-api.ts
    - apps/api/app/discovery_routes.py
    - .planning/phases/06-discovery-workspace/06-UI-SPEC.md
    - .planning/phases/06-discovery-workspace/06-RESEARCH.md
    - .planning/phases/06-discovery-workspace/06-CONTEXT.md
  </read_first>
  <action>
    Add Chromium browser coverage for the Discovery workspace end-to-end.

    Cover these flows explicitly:
    1. Route loads into the Discovery workspace with the expected split-pane layout.
    2. Loading, empty, error, blocked, retry, and success states all render with visible recovery actions.
    3. Generate -> edit -> save current -> save version works end to end.
    4. Version drawer opens, previews a version, and restores with explicit confirmation.
    5. Weak evidence shows the `Needs more evidence` state and keeps the page usable.
    6. A stale revision triggers the conservative conflict path and the UI points the user to refresh.
    7. Keyboard-only coverage confirms focus visibility, drawer open/close, restore confirmation, and accessible error announcements.
    8. The browser test keeps desktop-only dimensions and does not introduce mobile/tablet emulation.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/discovery-workspace.spec.ts --project chromium</automated>
  </verify>
  <acceptance_criteria>
    - The E2E file exists and covers the full Discovery journey.
    - The browser suite exercises recovery and restore semantics, not just the happy path.
    - The browser command runs the Discovery coverage in Chromium desktop mode only.
  </acceptance_criteria>
  <done>
    Browser-level proof exists for the Discovery workspace and its recovery behaviors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish release hardening, final copy polish, and verification cleanup</name>
  <files>apps/web/components/opportunities/discovery-workspace.tsx, apps/web/components/opportunities/discovery-output-pane.tsx, apps/web/components/opportunities/discovery-version-drawer.tsx, apps/web/app/globals.css</files>
  <read_first>
    - apps/web/components/opportunities/discovery-workspace.tsx
    - apps/web/components/opportunities/discovery-output-pane.tsx
    - apps/web/components/opportunities/discovery-version-drawer.tsx
    - apps/web/app/globals.css
    - tests/e2e/discovery-workspace.spec.ts
    - .planning/phases/06-discovery-workspace/06-UI-SPEC.md
    - .planning/phases/06-discovery-workspace/06-RESEARCH.md
  </read_first>
  <action>
    Resolve any remaining browser or productization gaps after the E2E coverage is in place.

    1. Tighten copy for thin-evidence, conflict, restore, and retry states so the user always knows what happened and what to do next.
    2. Remove any motion, spacing, or visual drift that makes the page feel like a prototype instead of a shipping product.
    3. Confirm route-level error and loading behavior still work after the final UI changes.
    4. Confirm keyboard focus, `role=\"alert\"`, `aria-live`, and busy semantics remain intact after the final UI changes.
    5. Leave the implementation web-only and desktop/laptop-only.
  </action>
  <verify>
    <automated>pnpm build && pnpm exec playwright test tests/e2e/discovery-workspace.spec.ts --project chromium</automated>
  </verify>
  <acceptance_criteria>
    - The final browser suite passes after the last polish fixes.
    - The build and smoke commands both exit successfully.
    - The Discovery page still presents explicit recovery paths for loading, error, blocked, retry, restore, and conflict states.
    - Keyboard-only browser verification confirms visible focus and accessible error announcements.
  </acceptance_criteria>
  <done>
    Phase 6 is hardened to a release-ready standard with browser verification and no remaining UI drift.
  </done>
</task>

</tasks>

<verification>
Wave 4 is complete only when the browser suite passes, the page still behaves like a product under error and conflict conditions, and the final build is clean.
</verification>

<success_criteria>
- Discovery is verified in a real browser, not only in unit or API tests.
- Recovery, conflict, and restore semantics are visible in the browser flow.
- The phase ends at a product-ready standard for web-only desktop/laptop users.
</success_criteria>
