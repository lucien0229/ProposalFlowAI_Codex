---
phase: 01-foundation-repo-infra-ui-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - infra/compose/docker-compose.local.yml
  - infra/deploy/web.yaml
  - infra/deploy/admin.yaml
  - infra/deploy/api.yaml
  - infra/deploy/worker.yaml
  - apps/api/pyproject.toml
  - apps/api/app/__init__.py
  - apps/api/app/main.py
  - apps/api/app/product.py
  - apps/api/app/admin.py
  - apps/api/alembic.ini
  - apps/api/alembic/env.py
  - apps/api/alembic/versions/0001_initial.py
  - apps/worker/pyproject.toml
  - apps/worker/main.py
autonomous: true
requirements:
  - PLAT-03
  - PLAT-02
must_haves:
  truths:
    - "Local development can start the dependency stack with PostgreSQL, Redis, and object storage separated from app processes."
    - "The backend exposes separate product and admin API namespaces plus health and readiness endpoints."
    - "The API and worker each have a runnable bootstrap path and a versioned migration baseline."
  artifacts:
    - path: "infra/compose/docker-compose.local.yml"
      provides: "Local dependency stack for PostgreSQL, Redis, and object storage"
    - path: "infra/deploy/web.yaml"
      provides: "Web deploy object with local, staging, and production sections"
    - path: "apps/api/app/main.py"
      provides: "FastAPI application entrypoint and namespace wiring"
    - path: "apps/api/alembic/versions/0001_initial.py"
      provides: "Versioned migration baseline"
    - path: "apps/worker/main.py"
      provides: "Internal worker bootstrap entrypoint"
  key_links:
    - from: "infra/compose/docker-compose.local.yml"
      to: "infra/deploy/web.yaml"
      via: "shared environment and service naming"
      pattern: "web|admin|api|worker"
    - from: "apps/api/app/main.py"
      to: "apps/api/app/product.py"
      via: "FastAPI router inclusion"
      pattern: "include_router"
    - from: "apps/api/alembic/env.py"
      to: "apps/api/alembic/versions/0001_initial.py"
      via: "Alembic migration loading"
      pattern: "target_metadata|revision"
---

<objective>
Build the runtime and deployment skeleton for the backend and worker.

Purpose: give later phases a real execution model for the API, worker, environment layers, and migrations instead of a placeholder-only repo.
Output: local dependency stack, deploy object skeletons, FastAPI namespace wiring, Alembic baseline, and worker bootstrap.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
@docs/share/ProposalFlow AI｜数据库设计 v1.0.md
@docs/share/ProposalFlow AI｜部署与环境搭建说明 v1.0.md
@docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the local dependency stack and deploy object skeletons</name>
  <files>infra/compose/docker-compose.local.yml, infra/deploy/web.yaml, infra/deploy/admin.yaml, infra/deploy/api.yaml, infra/deploy/worker.yaml</files>
  <read_first>
    - infra/compose/docker-compose.local.yml
    - infra/deploy/web.yaml
    - infra/deploy/admin.yaml
    - infra/deploy/api.yaml
    - infra/deploy/worker.yaml
    - docs/share/ProposalFlow AI｜部署与环境搭建说明 v1.0.md
    - docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
  </read_first>
  <action>
    Create `infra/compose/docker-compose.local.yml` with the phase-1 local dependency set: PostgreSQL, Redis, and object storage.

    Add one deploy object file per runtime unit (`web`, `admin`, `api`, and `worker`) and give each file explicit `local`, `staging`, and `production` sections so the environment split is visible in code rather than implied.

    Use the runtime names from the frozen docs, not generic `frontend` or `backend` labels.
  </action>
  <verify>
    <automated>test -f infra/compose/docker-compose.local.yml && test -f infra/deploy/web.yaml && test -f infra/deploy/admin.yaml && test -f infra/deploy/api.yaml && test -f infra/deploy/worker.yaml && grep -q 'postgres' infra/compose/docker-compose.local.yml && grep -q 'redis' infra/compose/docker-compose.local.yml && grep -q 'minio' infra/compose/docker-compose.local.yml && grep -q 'local' infra/deploy/web.yaml && grep -q 'production' infra/deploy/worker.yaml</automated>
  </verify>
  <acceptance_criteria>
    - Local dependencies are containerized and clearly separated from app processes.
    - The deploy skeleton names `web`, `admin`, `api`, and `worker` explicitly.
    - The environment split is visible in the deploy files, not only in prose.
  </acceptance_criteria>
  <done>
    The repo has a concrete environment and deployment scaffold that later phases can extend without renaming the service model.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the FastAPI runtime and route namespace split</name>
  <files>apps/api/pyproject.toml, apps/api/app/__init__.py, apps/api/app/main.py, apps/api/app/product.py, apps/api/app/admin.py</files>
  <read_first>
    - apps/api/pyproject.toml
    - apps/api/app/__init__.py
    - apps/api/app/main.py
    - apps/api/app/product.py
    - apps/api/app/admin.py
    - docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
    - docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
  </read_first>
  <action>
    Create `apps/api` as a runnable FastAPI project with a minimal `pyproject.toml` and a package entrypoint under `apps/api/app`.

    Wire `apps/api/app/main.py` to expose `/health` and `/ready`, include a product router under `/api/v1`, and include a separate admin router under `/api/v1/admin`.

    Keep the namespace split explicit in code so product and admin routes do not collapse into one generic API surface.
  </action>
  <verify>
    <automated>python3 -m py_compile apps/api/app/__init__.py apps/api/app/main.py apps/api/app/product.py apps/api/app/admin.py && grep -q '/api/v1' apps/api/app/main.py && grep -q '/api/v1/admin' apps/api/app/main.py && grep -q '/health' apps/api/app/main.py && grep -q '/ready' apps/api/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - The API is a runnable Python app, not just a directory placeholder.
    - Product and admin namespaces are separated in the FastAPI wiring.
    - Health and readiness endpoints exist for local and deployment smoke checks.
  </acceptance_criteria>
  <done>
    The backend has a real entrypoint and the route boundary matches the frozen product/admin split.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add the Alembic baseline and worker bootstrap</name>
  <files>apps/api/alembic.ini, apps/api/alembic/env.py, apps/api/alembic/versions/0001_initial.py, apps/worker/pyproject.toml, apps/worker/main.py</files>
  <read_first>
    - apps/api/alembic.ini
    - apps/api/alembic/env.py
    - apps/api/alembic/versions/0001_initial.py
    - apps/worker/pyproject.toml
    - apps/worker/main.py
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
    - docs/share/ProposalFlow AI｜部署与环境搭建说明 v1.0.md
  </read_first>
  <action>
    Add an Alembic configuration that points at the API application metadata and can load versioned migrations from `apps/api/alembic/versions`.

    Create the initial revision as a versioned baseline so later phases can add schema incrementally instead of inventing the migration framework under pressure.

    Add a lightweight worker `pyproject.toml` and `main.py` entrypoint that is clearly internal-only and can be extended for queue consumption and async retries later.
  </action>
  <verify>
    <automated>python3 -m py_compile apps/api/alembic/env.py apps/api/alembic/versions/0001_initial.py apps/worker/main.py && grep -q 'versions' apps/api/alembic.ini && grep -q 'revision' apps/api/alembic/versions/0001_initial.py && grep -q 'worker' apps/worker/main.py</automated>
  </verify>
  <acceptance_criteria>
    - The migration framework exists before any real schema tables are added.
    - The worker has a dedicated bootstrap entrypoint and is not exposed as a public service.
    - The API and worker lifecycle are both explicit and versioned.
  </acceptance_criteria>
  <done>
    The backend persistence baseline and worker skeleton are ready for later domain models and file/AI workloads.
  </done>
</task>

</tasks>

<verification>
The local dependency stack, API app, Alembic baseline, and worker bootstrap should all be present and syntax-check clean.
</verification>

<success_criteria>
- Local infrastructure is containerized and named for the four runtime units.
- The API has separate product and admin namespaces plus health/readiness endpoints.
- The worker and migration framework exist as durable phase-1 scaffolding.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-infra-ui-baseline/01-03-SUMMARY.md`
</output>
