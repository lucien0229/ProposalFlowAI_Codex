---
phase: 01-foundation-repo-infra-ui-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .gitignore
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/admin/package.json
  - apps/admin/tsconfig.json
autonomous: true
requirements:
  - PLAT-01
must_haves:
  truths:
    - "The repository installs as one workspace and recognizes the top-level app/package layout without ambiguity."
    - "The frontend apps have explicit Next.js entry packages instead of being hidden inside a generic frontend folder."
  artifacts:
    - path: "package.json"
      provides: "Root workspace scripts and package manager metadata"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace membership for frontend apps and shared packages"
    - path: "apps/web/package.json"
      provides: "Customer web app package entrypoint"
    - path: "apps/admin/package.json"
      provides: "Admin app package entrypoint"
  key_links:
    - from: "package.json"
      to: "pnpm-workspace.yaml"
      via: "pnpm workspace resolution"
      pattern: "apps/|packages/"
    - from: "apps/web/package.json"
      to: "apps/web/tsconfig.json"
      via: "Next.js build-time TypeScript resolution"
      pattern: "extends.*tsconfig.base"
    - from: "apps/admin/package.json"
      to: "apps/admin/tsconfig.json"
      via: "Next.js build-time TypeScript resolution"
      pattern: "extends.*tsconfig.base"
---

<objective>
Bootstrap the root monorepo workspace and the frontend app package entrypoints.

Purpose: lock the repo shape early so later plans can add backend, shared-contract, and UI work against stable top-level boundaries.
Output: root workspace config plus the `apps/web` and `apps/admin` app package scaffolds.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/share/ProposalFlow AI 开发骨架与目录结构设计说明 v1.0.md
@docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
@docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the root workspace and task pipeline</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .gitignore</files>
  <read_first>
    - package.json
    - pnpm-workspace.yaml
    - turbo.json
    - tsconfig.base.json
    - .gitignore
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - docs/share/ProposalFlow AI 开发骨架与目录结构设计说明 v1.0.md
    - docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
  </read_first>
  <action>
    Create a root `package.json` that declares the pnpm workspace entrypoint and repo-level scripts for `dev`, `build`, `lint`, `test`, and `smoke`.

    Add `pnpm-workspace.yaml` with explicit workspace globs for `apps/*` and `packages/*`.

    Add `turbo.json` with cached pipelines for `build`, `lint`, `test`, and `dev`, and add a strict `tsconfig.base.json` that future TypeScript packages can extend.

    Add `.gitignore` entries for Node, Python, Next.js, Turborepo, and test/build output so phase 1 does not accumulate accidental artifacts.
  </action>
  <verify>
    <automated>test -f package.json && test -f pnpm-workspace.yaml && test -f turbo.json && test -f tsconfig.base.json && test -f .gitignore && grep -q 'apps/\*' pnpm-workspace.yaml && grep -q 'packages/\*' pnpm-workspace.yaml && grep -q '"build"' package.json && grep -q '"smoke"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - Root workspace config exists and names the intended workspace folders explicitly.
    - Root scripts exist for local dev, build, lint, test, and smoke verification.
    - Tooling ignores prevent generated files from polluting the bootstrap commit.
  </acceptance_criteria>
  <done>
    The repo has one root workspace baseline that later plans can extend without renaming or restructuring the top-level toolchain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add frontend app package scaffolds</name>
  <files>apps/web/package.json, apps/web/tsconfig.json, apps/admin/package.json, apps/admin/tsconfig.json</files>
  <read_first>
    - apps/web/package.json
    - apps/web/tsconfig.json
    - apps/admin/package.json
    - apps/admin/tsconfig.json
    - package.json
    - tsconfig.base.json
    - docs/share/ProposalFlow AI 开发骨架与目录结构设计说明 v1.0.md
    - docs/design/DESIGN.md
  </read_first>
  <action>
    Create `apps/web/package.json` and `apps/admin/package.json` as separate Next.js app packages, each with its own `dev`, `build`, `start`, and `lint` scripts.

    Pin each frontend app to the phase-1 stack from research: Next.js 15, React 19, React DOM 19, Tailwind CSS 4, TypeScript, and the minimal app-router build setup.

    Add matching `tsconfig.json` files for web and admin that extend the root `tsconfig.base.json` and set up app-local path resolution without introducing a shared UI package.
  </action>
  <verify>
    <automated>test -f apps/web/package.json && test -f apps/web/tsconfig.json && test -f apps/admin/package.json && test -f apps/admin/tsconfig.json && grep -q '"dev"' apps/web/package.json && grep -q '"build"' apps/admin/package.json && grep -q 'tsconfig.base' apps/web/tsconfig.json && grep -q 'tsconfig.base' apps/admin/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web` and `apps/admin` are both first-class app packages, not folders hidden under a generic frontend tree.
    - The frontend app manifests are independent and ready for later route-shell work.
    - TypeScript inheritance points at the root base config, not ad hoc per-app copies.
  </acceptance_criteria>
  <done>
    The frontend side of the monorepo is a real workspace surface, ready for customer and admin shell code in later plans.
  </done>
</task>

</tasks>

<verification>
Root workspace commands should resolve both frontend app packages, and the app manifests should be discoverable by pnpm and the TypeScript baseline.
</verification>

<success_criteria>
- The repo has a single root workspace configuration.
- The web and admin frontend apps exist as explicit packages.
- The root toolchain is ready for subsequent shared-package, backend, and UI shell work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-infra-ui-baseline/01-01-SUMMARY.md`
</output>
