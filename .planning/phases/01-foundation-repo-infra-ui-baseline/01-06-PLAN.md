---
phase: 01-foundation-repo-infra-ui-baseline
plan: 06
type: execute
wave: 4
depends_on:
  - "04"
files_modified:
  - playwright.config.ts
  - tests/conftest.py
  - tests/smoke/web-shell.spec.ts
  - tests/smoke/admin-shell.spec.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - PLAT-02
must_haves:
  truths:
    - "Browser smoke coverage proves the customer shell boots, renders product states, and keeps the admin shell distinct."
    - "The Playwright harness is a verification-only surface and does not become a second UI framework."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Browser smoke harness for the phase-1 shell checks"
    - path: "tests/smoke/web-shell.spec.ts"
      provides: "Customer shell smoke coverage and product-state assertions"
    - path: "tests/smoke/admin-shell.spec.ts"
      provides: "Admin shell smoke coverage and boundary assertions"
  key_links:
    - from: "playwright.config.ts"
      to: "tests/smoke/web-shell.spec.ts"
      via: "browser smoke runner"
      pattern: "webServer|web-shell"
    - from: "playwright.config.ts"
      to: "tests/smoke/admin-shell.spec.ts"
      via: "browser smoke runner"
      pattern: "webServer|admin-shell"
    - from: "tests/smoke/web-shell.spec.ts"
      to: "apps/web/app/page.tsx"
      via: "customer shell entry route"
      pattern: "loading|empty|error|blocked|retry|success"
---

<objective>
Add browser smoke coverage for the customer and admin shells created in the earlier UI plan.

Purpose: prove the phase-1 shells boot and render the boundary states that future phases depend on.
Output: Playwright config plus customer/admin smoke specs.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-repo-infra-ui-baseline/01-CONTEXT.md
@.planning/phases/01-foundation-repo-infra-ui-baseline/01-RESEARCH.md
@.planning/phases/01-foundation-repo-infra-ui-baseline/01-VALIDATION.md
@docs/design/DESIGN.md
@docs/share/ProposalFlow AI｜开发骨架与目录结构设计说明 v1.0.md
@docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Playwright browser smoke harness</name>
  <files>playwright.config.ts, tests/conftest.py</files>
  <read_first>
    - playwright.config.ts
    - tests/conftest.py
    - package.json
    - apps/web/app/page.tsx
    - apps/admin/app/page.tsx
    - docs/design/DESIGN.md
  </read_first>
  <action>
    Add a Playwright configuration that boots the phase-1 customer and admin apps as browser smoke targets and keeps the config focused on route-level verification.

    Create a shared `tests/conftest.py` fixture module for any common smoke-test setup so the browser specs do not duplicate launch or state helpers.

    Use the repo-level dev commands and local ports already established in Phase 1 so the smoke harness can reuse the same shell entrypoints the developer sees.
  </action>
  <verify>
    <automated>test -f playwright.config.ts && test -f tests/conftest.py && grep -q 'webServer' playwright.config.ts && grep -q 'projects' playwright.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - The Playwright config exists as a dedicated smoke harness.
    - The harness is wired to the same app entrypoints that the shell plans create.
    - The config stays narrow and does not grow into a second test platform.
  </acceptance_criteria>
  <done>
    The browser smoke harness is ready to execute against the phase-1 shell routes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add customer shell smoke coverage</name>
  <files>tests/smoke/web-shell.spec.ts</files>
  <read_first>
    - tests/smoke/web-shell.spec.ts
    - playwright.config.ts
    - apps/web/app/page.tsx
    - apps/web/components/status-panel.tsx
    - packages/shared-types/index.ts
    - docs/design/DESIGN.md
  </read_first>
  <action>
    Add a customer shell smoke spec that visits the web landing route, verifies the shipping UI chrome renders, and exercises the six shared product states: `loading`, `empty`, `error`, `blocked`, `retry`, and `success`.

    Keep the assertions route-level and boundary-focused so the smoke test proves the shell is a product surface rather than a spec page.
  </action>
  <verify>
    <automated>test -f tests/smoke/web-shell.spec.ts && grep -q 'loading' tests/smoke/web-shell.spec.ts && grep -q 'blocked' tests/smoke/web-shell.spec.ts && grep -q 'success' tests/smoke/web-shell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The customer shell has automated browser coverage.
    - All six shared product states are reachable in the smoke spec.
    - The spec verifies shipping UI rather than text-only output.
  </acceptance_criteria>
  <done>
    The customer route has browser coverage that matches the phase-1 shipping UI baseline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add admin shell smoke coverage</name>
  <files>tests/smoke/admin-shell.spec.ts</files>
  <read_first>
    - tests/smoke/admin-shell.spec.ts
    - playwright.config.ts
    - apps/admin/app/page.tsx
    - apps/admin/components/admin-shell.tsx
    - docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
    - docs/design/DESIGN.md
  </read_first>
  <action>
    Add an admin shell smoke spec that visits the admin landing route, verifies the internal placeholder treatment renders, and confirms the page does not expose customer-workflow language.

    Keep the spec intentionally minimal so the admin boundary remains clearly separate from the customer shell.
  </action>
  <verify>
    <automated>test -f tests/smoke/admin-shell.spec.ts && grep -q 'internal' tests/smoke/admin-shell.spec.ts && grep -q 'read-only' tests/smoke/admin-shell.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The admin shell has browser coverage as a separate surface.
    - The smoke test proves the admin page is internal-only.
    - The spec remains placeholder-level and does not drift into real admin workflows.
  </acceptance_criteria>
  <done>
    The admin boundary is verified in-browser without becoming a launch-scope workflow surface.
  </done>
</task>

</tasks>

<verification>
The Playwright harness and both smoke specs should exist and be syntax-check clean.
</verification>

<success_criteria>
- Browser smoke coverage exists for both shell routes.
- The customer shell product-state variants are exercised automatically.
- The admin route is verified as a separate internal placeholder surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-infra-ui-baseline/01-06-SUMMARY.md`
</output>
