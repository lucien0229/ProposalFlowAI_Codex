---
phase: 01-foundation-repo-infra-ui-baseline
plan: 05
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - apps/api/app/security.py
  - apps/api/app/main.py
  - apps/api/app/product.py
  - apps/api/app/admin.py
  - apps/api/app/activity_logs.py
  - apps/api/alembic/versions/0002_activity_logs_security.py
autonomous: true
requirements:
  - PLAT-02
  - PLAT-03
must_haves:
  truths:
    - "Browser-session boundary helpers and CSRF checks are explicit in backend code rather than only in documentation."
    - "The shared activity-log primitive exists as a concrete backend service and a versioned migration target."
  artifacts:
    - path: "apps/api/app/security.py"
      provides: "Session and CSRF guard helpers for browser write requests"
    - path: "apps/api/app/activity_logs.py"
      provides: "Shared activity-log helper and persistence contract bridge"
    - path: "apps/api/alembic/versions/0002_activity_logs_security.py"
      provides: "Versioned migration that adds the activity log table"
  key_links:
    - from: "apps/api/app/main.py"
      to: "apps/api/app/security.py"
      via: "guard helper import and initialization"
      pattern: "require_web_session|require_admin_session|require_browser_csrf"
    - from: "apps/api/app/product.py"
      to: "apps/api/app/security.py"
      via: "browser write dependency wiring"
      pattern: "require_web_session|require_browser_csrf"
    - from: "apps/api/alembic/versions/0002_activity_logs_security.py"
      to: "apps/api/app/activity_logs.py"
      via: "shared activity-log shape reused by the migration"
      pattern: "workspace_id|action_type|metadata"
---

<objective>
Add the browser session guard scaffolding and shared activity-log primitive that Phase 1 locked in but deferred from the runtime bootstrap.

Purpose: make the API surface safe for later authenticated writes and give shared platform logging a single explicit shape.
Output: CSRF and session guard helpers, activity-log service scaffolding, and a versioned migration that introduces the shared log table.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-repo-infra-ui-baseline/01-CONTEXT.md
@.planning/phases/01-foundation-repo-infra-ui-baseline/01-RESEARCH.md
@.planning/phases/01-foundation-repo-infra-ui-baseline/01-VALIDATION.md
@docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
@docs/share/ProposalFlow AI｜数据库设计 v1.0.md
@docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add browser-session guard and CSRF scaffolding</name>
  <files>apps/api/app/security.py, apps/api/app/main.py, apps/api/app/product.py, apps/api/app/admin.py</files>
  <read_first>
    - apps/api/app/security.py
    - apps/api/app/main.py
    - apps/api/app/product.py
    - apps/api/app/admin.py
    - .planning/phases/01-foundation-repo-infra-ui-baseline/01-VALIDATION.md
    - docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
    - docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
  </read_first>
  <action>
    Create explicit backend scaffolding for browser-session and CSRF enforcement instead of leaving the rules implicit in prose.

    Add helper functions or dependencies for `require_web_session`, `require_admin_session`, `require_browser_csrf`, and a small session-context extractor so later mutation routes can opt in to the correct guard path.

    Wire the FastAPI app entrypoint so the security helpers are available alongside the product/admin routers and the API surface can clearly separate browser-session behavior from public health/readiness endpoints.
  </action>
  <verify>
    <automated>test -f apps/api/app/security.py && grep -q 'require_web_session' apps/api/app/security.py && grep -q 'require_admin_session' apps/api/app/security.py && grep -q 'require_browser_csrf' apps/api/app/security.py && grep -q 'csrf' apps/api/app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - Browser-session boundary helpers are explicit in code.
    - CSRF enforcement is part of the API baseline rather than a later retrofit.
    - The backend planning covers the locked session and guard constraints from CONTEXT.md.
  </acceptance_criteria>
  <done>
    The API bootstrap now has explicit session and CSRF guard scaffolding for later authenticated writes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the shared activity-log primitive and migration</name>
  <files>apps/api/app/activity_logs.py, apps/api/alembic/versions/0002_activity_logs_security.py</files>
  <read_first>
    - apps/api/app/activity_logs.py
    - apps/api/alembic/versions/0002_activity_logs_security.py
    - packages/shared-types/index.ts
    - packages/shared-schemas/index.ts
    - packages/shared-config/index.ts
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
    - docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
    - docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
  </read_first>
  <action>
    Create an internal `activity_logs` helper module that uses the shared activity-log contract from Phase 1 and exposes one explicit write helper for later phases.

    Add a second versioned migration that introduces the `activity_logs` table with `workspace_id`, `user_id`, `opportunity_id`, `entity_type`, `entity_id`, `action_type`, `metadata`, and `created_at`, plus the obvious supporting indexes for workspace and created-at lookups.

    Keep the table and helper narrow so the admin surface can read from one shared primitive without turning Phase 1 into a generic event bus.
  </action>
  <verify>
    <automated>test -f apps/api/app/activity_logs.py && test -f apps/api/alembic/versions/0002_activity_logs_security.py && grep -q 'activity_logs' apps/api/app/activity_logs.py && grep -q 'workspace_id' apps/api/alembic/versions/0002_activity_logs_security.py && grep -q 'action_type' apps/api/alembic/versions/0002_activity_logs_security.py</automated>
  </verify>
  <acceptance_criteria>
    - The shared activity log primitive has backend persistence behind it.
    - The migration is versioned instead of hand-written SQL.
    - The log shape remains explicit and intentionally narrow.
  </acceptance_criteria>
  <done>
    The API now has a concrete shared activity-log primitive that future customer and admin features can reuse.
  </done>
</task>

</tasks>

<verification>
The browser-session guard scaffolding and shared activity-log primitive should both exist and be syntax-check clean.
</verification>

<success_criteria>
- Browser write requests have explicit session and CSRF helpers.
- `activity_logs` exists as a shared platform primitive with a versioned migration.
- The API baseline stays narrow and does not become a generic security or events framework.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-infra-ui-baseline/01-05-SUMMARY.md`
</output>
