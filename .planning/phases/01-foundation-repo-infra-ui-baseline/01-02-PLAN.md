---
phase: 01-foundation-repo-infra-ui-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/shared-types/package.json
  - packages/shared-types/index.ts
  - packages/shared-schemas/package.json
  - packages/shared-schemas/index.ts
  - packages/shared-config/package.json
  - packages/shared-config/index.ts
autonomous: true
requirements:
  - PLAT-02
must_haves:
  truths:
    - "Cross-app session, environment, route, and state labels live in explicit shared packages instead of being duplicated in each app."
    - "The shared contract surface stays small and explicit, with separate packages for types, schemas, and config."
  artifacts:
    - path: "packages/shared-types/index.ts"
      provides: "Shared enums and type aliases for session, role, and state boundaries"
    - path: "packages/shared-schemas/index.ts"
      provides: "Shared runtime validation schemas for phase-1 contract values"
    - path: "packages/shared-config/index.ts"
      provides: "Shared environment, route, event, and cookie constants"
  key_links:
    - from: "packages/shared-config/index.ts"
      to: "apps/web/app/page.tsx"
      via: "route/state constants imported by the customer shell"
      pattern: "APP_ENV|ROUTE|COOKIE|EVENT"
    - from: "packages/shared-types/index.ts"
      to: "apps/web/components/status-panel.tsx"
      via: "state enum imported by the customer shell"
      pattern: "ProductState"
    - from: "packages/shared-types/index.ts"
      to: "apps/api/app/main.py"
      via: "phase-1 session and role naming reference"
      pattern: "SessionType|WorkspaceRole|InternalRole"
---

<objective>
Define the shared contract packages that make the monorepo boundaries explicit.

Purpose: give later plans a stable source of truth for env names, route namespaces, session types, roles, and product states without creating a premature UI library.
Output: `shared-types`, `shared-schemas`, and `shared-config` packages with phase-1 exports.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
@docs/share/ProposalFlow AI｜数据库设计 v1.0.md
@docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
@docs/share/ProposalFlow AI 开发骨架与目录结构设计说明 v1.0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared types for sessions, roles, and product states</name>
  <files>packages/shared-types/package.json, packages/shared-types/index.ts</files>
  <read_first>
    - packages/shared-types/package.json
    - packages/shared-types/index.ts
    - .planning/ROADMAP.md
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
    - docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
  </read_first>
  <action>
    Create `packages/shared-types` as the type-only contract package for phase 1.

    Export the exact phase-1 identity and boundary unions needed across the repo: `SessionType` (`web | admin`), `WorkspaceRole` (`owner | member`), `InternalRole` (`internal_admin | internal_analyst`), `AppEnvironment` (`local | staging | production`), `RouteNamespace` (`product | admin | shared`), and `ProductState` (`loading | empty | error | blocked | retry | success`).
  </action>
  <verify>
    <automated>test -f packages/shared-types/package.json && test -f packages/shared-types/index.ts && grep -q 'SessionType' packages/shared-types/index.ts && grep -q 'WorkspaceRole' packages/shared-types/index.ts && grep -q 'ProductState' packages/shared-types/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Session and role boundaries are encoded in a single explicit type contract.
    - The customer-facing product state union is available for later UI and smoke-test work.
    - The package stays small enough to be copied mentally without hunting through a platform abstraction layer.
  </acceptance_criteria>
  <done>
    Shared identity and state types are available as the first cross-app contract surface.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define shared runtime schemas for the phase-1 contract values</name>
  <files>packages/shared-schemas/package.json, packages/shared-schemas/index.ts</files>
  <read_first>
    - packages/shared-schemas/package.json
    - packages/shared-schemas/index.ts
    - packages/shared-types/index.ts
    - docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
  </read_first>
  <action>
    Create `packages/shared-schemas` as the runtime validation package for phase-1 contract values.

    Export schema definitions for the same boundary values captured in shared types: environment names, session types, route namespaces, and product states.

    Use explicit schema names so later plans can import them directly without inventing a generic validator layer.
  </action>
  <verify>
    <automated>test -f packages/shared-schemas/package.json && test -f packages/shared-schemas/index.ts && grep -q 'Schema' packages/shared-schemas/index.ts && grep -q 'AppEnvironment' packages/shared-schemas/index.ts && grep -q 'ProductState' packages/shared-schemas/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Runtime validation lives beside the type contract instead of being hidden in app-local code.
    - The schema package mirrors the same phase-1 values as `shared-types`.
    - The contract surface remains explicit and narrow.
  </acceptance_criteria>
  <done>
    Shared runtime schemas exist for the values that need validation across app boundaries.
  </done>
</task>

<task type="auto">
  <name>Task 3: Define shared config constants for route, cookie, and event names</name>
  <files>packages/shared-config/package.json, packages/shared-config/index.ts</files>
  <read_first>
    - packages/shared-config/package.json
    - packages/shared-config/index.ts
    - packages/shared-types/index.ts
    - docs/share/ProposalFlow AI｜部署与环境搭建说明 v1.0.md
    - docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
  </read_first>
  <action>
    Create `packages/shared-config` as the canonical home for phase-1 repo constants.

    Export the environment labels, product/admin route prefixes, browser cookie names, shared event names, and the default page-size constant needed by later app shells and backend scaffolds.

    Keep the names concrete and literal so plan 3 and plan 4 can wire against them without guessing.
  </action>
  <verify>
    <automated>test -f packages/shared-config/package.json && test -f packages/shared-config/index.ts && grep -q 'WEB_ROUTE_PREFIX' packages/shared-config/index.ts && grep -q 'ADMIN_ROUTE_PREFIX' packages/shared-config/index.ts && grep -q 'SESSION_COOKIE' packages/shared-config/index.ts && grep -q 'EVENT' packages/shared-config/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - Route, cookie, and event naming is centralized in one explicit config package.
    - Later plans can import the constants instead of retyping names in each app.
    - The shared config package stays narrow enough to avoid becoming a generic platform layer.
  </acceptance_criteria>
  <done>
    The repository has a clear cross-app constant layer for the non-UI values that Phase 1 needs.
  </done>
</task>

</tasks>

<verification>
The shared packages must export the phase-1 boundary values without leaking unrelated abstractions or UI code.
</verification>

<success_criteria>
- Types, schemas, and config are split into separate packages.
- The phase-1 session, role, route, environment, and product-state values have a single explicit home.
- The contract layer is ready for the backend and UI shell plans to consume.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-infra-ui-baseline/01-02-SUMMARY.md`
</output>
