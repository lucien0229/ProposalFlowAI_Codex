---
phase: 01-foundation-repo-infra-ui-baseline
plan: 04
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - apps/web/app/layout.tsx
  - apps/web/app/page.tsx
  - apps/web/app/globals.css
  - apps/web/components/app-shell.tsx
  - apps/web/components/status-panel.tsx
  - apps/admin/app/layout.tsx
  - apps/admin/app/page.tsx
  - apps/admin/app/globals.css
  - apps/admin/components/admin-shell.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - PLAT-02
must_haves:
  truths:
    - "The customer web route renders shipping product UI, not a specification page."
    - "The customer shell supports the shared product states loading, empty, error, blocked, retry, and success."
    - "The admin surface is a separate placeholder shell and does not blur into the customer app."
  artifacts:
    - path: "apps/web/components/status-panel.tsx"
      provides: "Customer product-state renderer"
    - path: "apps/web/app/page.tsx"
      provides: "Customer shell landing route"
    - path: "apps/admin/app/page.tsx"
      provides: "Admin placeholder landing route"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/components/status-panel.tsx"
      via: "state prop and query-driven rendering"
      pattern: "loading|empty|error|blocked|retry|success"
    - from: "apps/admin/app/page.tsx"
      to: "apps/admin/components/admin-shell.tsx"
      via: "app shell composition"
      pattern: "admin shell"
---

<objective>
Ship the first-pass customer and admin shells using the phase-1 visual baseline.

Purpose: make the customer app feel like a real product from day one while keeping the admin surface clearly separated and minimal.
Output: customer shipping shell and admin placeholder shell using the phase-1 visual baseline.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/design/DESIGN.md
@docs/share/ProposalFlow AI｜开发骨架与目录结构设计说明 v1.0.md
@docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the customer shipping shell and product-state renderer</name>
  <files>apps/web/app/layout.tsx, apps/web/app/page.tsx, apps/web/app/globals.css, apps/web/components/app-shell.tsx, apps/web/components/status-panel.tsx</files>
  <read_first>
    - apps/web/app/layout.tsx
    - apps/web/app/page.tsx
    - apps/web/app/globals.css
    - apps/web/components/app-shell.tsx
    - apps/web/components/status-panel.tsx
    - packages/shared-types/index.ts
    - packages/shared-config/index.ts
    - docs/design/DESIGN.md
  </read_first>
  <action>
    Create the customer-facing Next.js shell with the Linear-inspired dark baseline from `DESIGN.md`: near-black background, translucent panels, Inter-driven typography, restrained borders, and a visible action-oriented layout.

    Build an `AppShell` component that gives the customer route a stable product chrome with navigation, header actions, and a compact content column.

    Build a `StatusPanel` component that renders the six required product states (`loading`, `empty`, `error`, `blocked`, `retry`, `success`) from a typed prop or query-param driven state, so the route can demonstrate the shared state model without becoming a spec page.
  </action>
  <verify>
    <automated>test -f apps/web/app/layout.tsx && test -f apps/web/app/page.tsx && test -f apps/web/app/globals.css && test -f apps/web/components/app-shell.tsx && test -f apps/web/components/status-panel.tsx && grep -q 'loading' apps/web/components/status-panel.tsx && grep -q 'blocked' apps/web/components/status-panel.tsx && grep -q 'Inter' apps/web/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - The customer route looks like a shipping product surface instead of a documentation page.
    - All six shared product states are represented by the customer shell component.
    - The visual treatment follows the phase-1 dark product baseline.
  </acceptance_criteria>
  <done>
    The customer-facing shell is ready for later workflow pages to mount inside it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the admin placeholder shell as a separate surface</name>
  <files>apps/admin/app/layout.tsx, apps/admin/app/page.tsx, apps/admin/app/globals.css, apps/admin/components/admin-shell.tsx</files>
  <read_first>
    - apps/admin/app/layout.tsx
    - apps/admin/app/page.tsx
    - apps/admin/app/globals.css
    - apps/admin/components/admin-shell.tsx
    - packages/shared-config/index.ts
    - docs/design/DESIGN.md
    - docs/share/ProposalFlow AI｜共享平台边界说明 v1.0.md
  </read_first>
  <action>
    Create the admin app shell as a clearly separate internal surface with its own layout, chrome, and landing route.

    Keep the admin placeholder deliberately minimal: an internal badge, a short overview card, and boundary-first navigation styling that does not imitate the customer workflow shell.

    Preserve the same dark visual baseline while making the admin surface obviously read-only and non-customer-facing.
  </action>
  <verify>
    <automated>test -f apps/admin/app/layout.tsx && test -f apps/admin/app/page.tsx && test -f apps/admin/app/globals.css && test -f apps/admin/components/admin-shell.tsx && grep -q 'internal' apps/admin/app/page.tsx && grep -q 'read-only' apps/admin/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The admin app is visibly separate from the customer app.
    - The admin shell stays placeholder-level and does not drift into real workflow content.
    - The surface reads as internal-only at a glance.
  </acceptance_criteria>
  <done>
    The admin boundary is represented in UI without becoming a launch-focus product surface.
  </done>
</task>

</tasks>

<verification>
The browser smoke suite should boot both apps and confirm the customer and admin shells render their intended boundary states.
</verification>

<success_criteria>
- Customer routes render shipping UI instead of spec pages.
- The six shared product states are supported in the customer shell.
- Admin remains a separate, minimal, internal placeholder surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repo-infra-ui-baseline/01-04-SUMMARY.md`
</output>
