---
phase: 05-lead-brief-workspace
plan: 04
type: execute
wave: 4
depends_on:
  - "05-03"
files_modified:
  - apps/web/components/opportunities/lead-brief-version-drawer.tsx
  - apps/web/components/opportunities/lead-brief-workspace.tsx
  - apps/web/components/opportunities/lead-brief-output-pane.tsx
  - apps/web/lib/lead-brief-api.ts
  - apps/web/app/globals.css
  - tests/e2e/lead-brief-workspace.spec.ts
  - tests/api/test_lead_brief_api.py
requirements:
  - STATE-01
  - BRIEF-01
  - BRIEF-02
must_haves:
  truths:
    - "Version history must be discoverable in a right-side drawer and must follow list -> preview -> restore confirmation."
    - "Restore must be deliberate, explicit, and risk-aware."
    - "Conflicts must fail conservatively with reload/retry guidance and must not silently merge user edits."
    - "UI refinements must continue to honor the `ui-ux-pro-max` and `frontend-skill` workflow requirements."
  artifacts:
    - path: "apps/web/components/opportunities/lead-brief-version-drawer.tsx"
      provides: "Version list, preview, and restore confirmation UX"
    - path: "tests/e2e/lead-brief-workspace.spec.ts"
      provides: "Restore, overwrite warning, and conflict-recovery browser coverage"
  key_links:
    - from: "apps/web/components/opportunities/lead-brief-version-drawer.tsx"
      to: "apps/web/lib/lead-brief-api.ts"
      via: "versions, version detail, and restore API calls"
      pattern: "Versions|Preview|Restore|Current|expected_revision_no"
    - from: "tests/e2e/lead-brief-workspace.spec.ts"
      to: "apps/web/components/opportunities/lead-brief-version-drawer.tsx"
      via: "drawer focus, preview, restore confirmation, and retry recovery assertions"
      pattern: "restore|preview|conflict|reload latest|Versions"
---

<objective>
Finish the versioned-workspace experience by adding the drawer, restore confirmation, overwrite protection, conflict recovery, and the final regression sweep.

This is the last phase-5 wave. It should leave the browser and API fully green at desktop/laptop widths with no silent overwrite paths.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-lead-brief-workspace/05-CONTEXT.md
@.planning/phases/05-lead-brief-workspace/05-RESEARCH.md
@.planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the version drawer list, preview, and explicit restore confirmation flow</name>
  <files>apps/web/components/opportunities/lead-brief-version-drawer.tsx, apps/web/components/opportunities/lead-brief-workspace.tsx, apps/web/lib/lead-brief-api.ts, apps/web/app/globals.css, tests/e2e/lead-brief-workspace.spec.ts</files>
  <read_first>
    - apps/web/components/opportunities/lead-brief-workspace.tsx
    - apps/web/lib/lead-brief-api.ts
    - .planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
    - docs/design/DESIGN.md
    - .planning/phases/05-lead-brief-workspace/05-CONTEXT.md
    - .planning/phases/05-lead-brief-workspace/05-RESEARCH.md
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: opening `Versions` shows a right-side drawer with newest-first immutable snapshots.
    - Test 2: selecting a version opens a read-only preview and does not restore by itself.
    - Test 3: restore requires a separate confirmation step that names overwrite risk.
    - Test 4: the drawer keeps focus while open and closes cleanly with Escape.
    - Test 5: loading, empty, error, and retry states remain visible inside the drawer surface.
  </behavior>
  <action>
    Run the browser drawer flow from RED first, then implement the drawer and restore UX.

    1. Create `apps/web/components/opportunities/lead-brief-version-drawer.tsx`.
    2. Wire list -> preview -> explicit restore confirmation inside the drawer without adding inline restore actions to the list rows.
    3. Keep the current working copy distinct from the selected history snapshot.
    4. Preserve the workspace shell and do not let the drawer collapse the page into a modal-only experience.
    5. Before visual polish, re-run the `ui-ux-pro-max` design-system search workflow if the drawer needs additional hierarchy or motion decisions.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/lead-brief-workspace.spec.ts -g 'Versions|preview|restore|drawer'</automated>
  </verify>
  <acceptance_criteria>
    - The drawer exists and supports list, preview, and confirmation separately.
    - Restore remains deliberate and risk-aware.
    - The targeted browser drawer command exits `0`.
  </acceptance_criteria>
  <done>
    Version history is now a production drawer instead of a hidden implementation detail.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish overwrite protection, conflict recovery, copy summary, and the final regression sweep</name>
  <files>apps/web/components/opportunities/lead-brief-workspace.tsx, apps/web/components/opportunities/lead-brief-output-pane.tsx, apps/web/lib/lead-brief-api.ts, apps/web/app/globals.css, tests/e2e/lead-brief-workspace.spec.ts, tests/api/test_lead_brief_api.py</files>
  <read_first>
    - tests/api/test_lead_brief_api.py
    - tests/e2e/lead-brief-workspace.spec.ts
    - apps/web/lib/lead-brief-api.ts
    - apps/web/components/opportunities/lead-brief-output-pane.tsx
    - apps/web/components/opportunities/lead-brief-workspace.tsx
    - .planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: `Save current`, `Save version`, and `Regenerate brief` all send `expected_revision_no`.
    - Test 2: stale writes fail conservatively and show reload/retry guidance while keeping the draft visible.
    - Test 3: regenerate warns about overwrite risk when there are unsaved edits.
    - Test 4: `Copy summary` copies the current structured brief summary, not source text.
    - Test 5: the final browser sweep covers loading, empty, error, blocked, retry, success, restore, and conflict recovery at 1024/1280/1440 widths.
  </behavior>
  <action>
    Use the last wave to close every recovery gap.

    1. Finish `apps/web/components/opportunities/lead-brief-output-pane.tsx` and `apps/web/components/opportunities/lead-brief-workspace.tsx` so save, save-version, regenerate, and copy-summary actions share a single concurrency-aware flow.
    2. Keep conflict handling conservative: surface the latest revision, show reload/retry guidance, and preserve the visible draft.
    3. Run the desktop-width Playwright sweep and the API regression together once the UI is wired.
    4. Do not introduce fine-grained regenerate, diff previews, or mobile/tablet breakpoints in this phase.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_lead_brief_api.py -q && pnpm exec playwright test tests/e2e/lead-brief-workspace.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Concurrency failures are conservative and visible.
    - Copy summary and overwrite warnings behave as specified.
    - The full Lead Brief API + browser regression suite exits `0`.
  </acceptance_criteria>
  <done>
    Phase 05 is now fully productionized and regression-verified.
  </done>
</task>

</tasks>

<verification>
Wave 4 is complete only when the version drawer, restore confirmation, overwrite protection, conflict recovery, and final regression commands are all green.
</verification>

<success_criteria>
- Version history is inspectable without leaving the workspace.
- Restore is explicit and overwrite-aware.
- Save current, save version, regenerate, and copy summary all honor concurrency.
- The browser passes at 1024px, 1280px, and 1440px with no mobile/tablet scope added.
</success_criteria>
