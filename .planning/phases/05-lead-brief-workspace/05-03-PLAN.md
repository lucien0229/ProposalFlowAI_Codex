---
phase: 05-lead-brief-workspace
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - tests/e2e/lead-brief-workspace.spec.ts
  - tests/e2e/helpers/lead-brief.ts
  - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
  - apps/web/components/opportunities/lead-brief-workspace.tsx
  - apps/web/components/opportunities/lead-brief-source-pane.tsx
  - apps/web/components/opportunities/lead-brief-output-pane.tsx
  - apps/web/components/opportunities/lead-brief-field-card.tsx
  - apps/web/lib/lead-brief-api.ts
  - apps/web/lib/lead-brief-copy.ts
  - apps/web/app/globals.css
requirements:
  - STATE-01
  - BRIEF-01
  - BRIEF-02
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Browser verification for the workspace must be written before UI implementation starts."
    - "UI work must explicitly follow `05-UI-SPEC.md`, `docs/design/DESIGN.md`, the `frontend-skill` workflow, and the `ui-ux-pro-max` design-system search workflow."
    - "The Lead Brief page must feel like a two-pane production workspace, not a report sheet, dashboard, or landing page."
  artifacts:
    - path: "tests/e2e/lead-brief-workspace.spec.ts"
      provides: "Failing browser coverage for empty/populated states, field states, save actions, and desktop widths"
    - path: "apps/web/components/opportunities/lead-brief-workspace.tsx"
      provides: "Main Lead Brief workspace shell"
    - path: "apps/web/components/opportunities/lead-brief-output-pane.tsx"
      provides: "Structured field editor and action surface"
  key_links:
    - from: "apps/web/components/opportunities/lead-brief-workspace.tsx"
      to: "apps/web/lib/lead-brief-api.ts"
      via: "load, save current, save version, regenerate, and restore calls"
      pattern: "Save current|Save version|Regenerate|Versions|Copy summary"
    - from: "tests/e2e/lead-brief-workspace.spec.ts"
      to: "apps/web/components/opportunities/lead-brief-version-drawer.tsx"
      via: "list -> preview -> restore browser assertions"
      pattern: "Versions|Preview|Restore|lead-brief"
---

<objective>
Write the browser contract first and then build the visible Lead Brief workspace around it.

This wave establishes the desktop-only UI shape, state vocabulary, and page copy that later waves will refine with version history and conflict recovery.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-lead-brief-workspace/05-CONTEXT.md
@.planning/phases/05-lead-brief-workspace/05-RESEARCH.md
@.planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing Playwright coverage for the Lead Brief workspace and its desktop widths</name>
  <files>tests/e2e/lead-brief-workspace.spec.ts, tests/e2e/helpers/lead-brief.ts, tests/e2e/opportunity-intake.spec.ts</files>
  <read_first>
    - tests/e2e/opportunity-intake.spec.ts
    - tests/e2e/opportunity-overview-shell.spec.ts
    - .planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
    - .planning/phases/05-lead-brief-workspace/05-CONTEXT.md
    - .planning/phases/05-lead-brief-workspace/05-RESEARCH.md
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: the page renders the `Lead Brief` workspace title and the two-pane layout.
    - Test 2: empty state shows `No current lead brief.` with the primary `Generate lead brief` action.
    - Test 3: populated state shows `Save current`, `Save version`, `Regenerate brief`, `Versions`, `Copy summary`, and `Continue to Discovery`.
    - Test 4: field states `Confirmed`, `Inferred`, `Missing`, and `Needs Review` are visible in the browser.
    - Test 5: the shell remains readable and non-overflowing at `1024px`, `1280px`, and `1440px`.
  </behavior>
  <action>
    Run the browser spec from RED first, then use the spec to steer the UI implementation.

    1. Create `tests/e2e/lead-brief-workspace.spec.ts` and `tests/e2e/helpers/lead-brief.ts`.
    2. Put the helper around browser setup, route navigation, width switching, and workspace assertions.
    3. Include assertions for the phase UI copy from `05-UI-SPEC.md` and for the desktop-only layout contract.
    4. Do not add mobile/tablet coverage; this phase is web desktop/laptop only.
    5. Run the `ui-ux-pro-max` design-system search workflow before styling decisions so the workspace stays a deliberate B2B operations surface instead of drifting into generic app chrome.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/lead-brief-workspace.spec.ts -g 'Lead Brief|empty|populated|widths'</automated>
  </verify>
  <acceptance_criteria>
    - The new Playwright file exists and fails before UI implementation.
    - Desktop width coverage is explicit at 1024, 1280, and 1440 pixels.
    - The browser assertions encode the frozen page copy and action hierarchy.
  </acceptance_criteria>
  <done>
    The browser contract is now a red test harness for the workspace UI.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Lead Brief workspace shell, source pane, output pane, and field cards</name>
  <files>apps/web/app/opportunities/[opportunityId]/[step]/page.tsx, apps/web/components/opportunities/lead-brief-workspace.tsx, apps/web/components/opportunities/lead-brief-source-pane.tsx, apps/web/components/opportunities/lead-brief-output-pane.tsx, apps/web/components/opportunities/lead-brief-field-card.tsx, apps/web/lib/lead-brief-api.ts, apps/web/lib/lead-brief-copy.ts, apps/web/app/globals.css, tests/e2e/lead-brief-workspace.spec.ts</files>
  <read_first>
    - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
    - apps/web/components/product-shell.tsx
    - apps/web/components/product-state-block.tsx
    - apps/web/lib/opportunities-api.ts
    - .planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
    - docs/design/DESIGN.md
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: the page still uses the opportunity shell and current-step stepper from earlier phases.
    - Test 2: the workspace stays two-pane and source-aware instead of becoming a single long editor.
    - Test 3: the right pane exposes edit and confirm affordances for the locked field-state vocabulary.
    - Test 4: loading, empty, error, blocked, retry, and success states render without losing the final layout shell.
    - Test 5: the UI copy matches the frozen page contract and remains desktop/laptop only.
  </behavior>
  <action>
    Follow the phase UI contract before touching implementation details.

    1. Read and apply `/Users/wxm/.codex/skills/frontend-skill/SKILL.md` and `.planning/phases/05-lead-brief-workspace/05-UI-SPEC.md` before editing web UI files.
    2. Create `apps/web/components/opportunities/lead-brief-workspace.tsx`, `apps/web/components/opportunities/lead-brief-source-pane.tsx`, `apps/web/components/opportunities/lead-brief-output-pane.tsx`, and `apps/web/components/opportunities/lead-brief-field-card.tsx`.
    3. Add `apps/web/lib/lead-brief-api.ts` and `apps/web/lib/lead-brief-copy.ts` for typed client calls and page copy.
    4. Update `apps/web/app/opportunities/[opportunityId]/[step]/page.tsx` so the canonical `lead-brief` route renders the workspace seam instead of a placeholder.
    5. Use the `Confirmed` / `Inferred` / `Missing` / `Needs Review` vocabulary exactly as locked in the UI spec.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/lead-brief-workspace.spec.ts -g 'Lead Brief|empty|populated|states'</automated>
  </verify>
  <acceptance_criteria>
    - The workspace shell and panes exist and render the frozen page copy.
    - Field-state chips are visible and semantically correct.
    - The desktop browser spec starts passing for the empty and populated workspace shell.
  </acceptance_criteria>
  <done>
    The browser can now see a production-shaped Lead Brief workspace.
  </done>
</task>

</tasks>

<verification>
Wave 3 is complete only when the browser can prove the Lead Brief shell, the two-pane layout, the field vocabulary, and the desktop-only width contract.
</verification>

<success_criteria>
- The Lead Brief workspace is browser-specified before it is polished.
- The UI follows the frozen design contract and uses the frontend skill workflow.
- The page remains a two-pane production workspace at desktop widths.
</success_criteria>
