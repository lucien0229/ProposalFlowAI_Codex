---
phase: 05-lead-brief-workspace
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - apps/api/alembic/versions/0007_phase5_lead_brief_workspace.py
  - apps/api/app/lead_brief_models.py
  - apps/api/app/lead_brief_repository.py
  - apps/api/app/lead_brief_service.py
  - apps/api/app/lead_brief_routes.py
  - apps/api/app/opportunity_routes.py
  - apps/api/app/main.py
  - packages/shared-types/index.ts
  - packages/shared-schemas/index.ts
  - packages/shared-config/index.ts
  - tests/api/test_lead_brief_api.py
  - tests/api/test_lead_brief_routing_api.py
requirements:
  - STATE-01
  - BRIEF-01
  - BRIEF-02
must_haves:
  truths:
    - "The backend owns the canonical current Lead Brief and immutable version snapshots."
    - "Restore copies a snapshot back into the current resource and must not auto-create a new version row."
    - "Every mutating request must carry `expected_revision_no` and fail conservatively on stale state."
  artifacts:
    - path: "apps/api/app/lead_brief_models.py"
      provides: "Current-resource and version-snapshot tables"
    - path: "apps/api/app/lead_brief_service.py"
      provides: "Business rules for save, version, regenerate, and restore"
    - path: "apps/api/app/lead_brief_routes.py"
      provides: "REST surface for read/update/version/restore operations"
  key_links:
    - from: "apps/api/app/lead_brief_service.py"
      to: "apps/api/app/opportunity_service.py"
      via: "generate handoff and workflow guard integration"
      pattern: "current_revision_no|expected_revision_no|restore|save version"
    - from: "apps/api/app/lead_brief_routes.py"
      to: "tests/api/test_lead_brief_api.py"
      via: "contract-driven endpoint behavior"
      pattern: "GET|PATCH|versions|restore|409"
---

<objective>
Implement the backend contract so the Lead Brief workspace has real persistence, snapshot history, and optimistic concurrency instead of placeholder responses.

This wave should leave the API tests green before any UI work starts.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-lead-brief-workspace/05-CONTEXT.md
@.planning/phases/05-lead-brief-workspace/05-RESEARCH.md
@.planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Lead Brief tables, repository, and service logic for current state plus version snapshots</name>
  <files>apps/api/alembic/versions/0007_phase5_lead_brief_workspace.py, apps/api/app/lead_brief_models.py, apps/api/app/lead_brief_repository.py, apps/api/app/lead_brief_service.py, tests/api/test_lead_brief_api.py</files>
  <read_first>
    - tests/api/test_lead_brief_api.py
    - apps/api/app/opportunity_repository.py
    - apps/api/app/opportunity_service.py
    - apps/api/alembic/versions/0006_phase4_file_processing.py
    - .planning/phases/05-lead-brief-workspace/05-CONTEXT.md
    - .planning/phases/05-lead-brief-workspace/05-RESEARCH.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: the current Lead Brief row is one-per-opportunity and stores the current structured payload plus revision metadata.
    - Test 2: version snapshots are append-only and full-resource, not diffs.
    - Test 3: `Save current` mutates the current row only.
    - Test 4: `Save version` appends history without mutating earlier snapshots.
    - Test 5: restore copies snapshot payload back to current state without auto-saving a new version.
  </behavior>
  <action>
    Run the Lead Brief API tests from RED first, then implement the data model and business logic.

    1. Create the `lead_briefs` and `lead_brief_versions` persistence layer in `apps/api/app/lead_brief_models.py` and `apps/api/alembic/versions/0007_phase5_lead_brief_workspace.py`.
    2. Implement repository helpers for current-resource fetch/update, version append/list/detail, and restore copy semantics in `apps/api/app/lead_brief_repository.py`.
    3. Implement `apps/api/app/lead_brief_service.py` so the service enforces `expected_revision_no`, whole-resource regenerate, and conservative conflict behavior.
    4. Keep the payload field-oriented and explicitly stateful; do not flatten the brief into prose.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_lead_brief_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Current resource and version-snapshot tables exist.
    - The repository/service layer satisfies the current-resource and immutable history contract.
    - The API tests pass for current read, save current, save version, version list/detail, restore, and stale revision failures.
  </acceptance_criteria>
  <done>
    The backend now stores Lead Brief state as a real product resource.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the Lead Brief API routes, generate handoff, and shared contract responses</name>
  <files>apps/api/app/lead_brief_routes.py, apps/api/app/opportunity_routes.py, apps/api/app/main.py, packages/shared-config/index.ts, packages/shared-types/index.ts, packages/shared-schemas/index.ts, tests/api/test_lead_brief_gate_api.py, tests/api/test_lead_brief_routing_api.py</files>
  <read_first>
    - apps/api/app/opportunity_routes.py
    - apps/api/app/main.py
    - packages/shared-config/index.ts
    - packages/shared-types/index.ts
    - packages/shared-schemas/index.ts
    - tests/api/test_lead_brief_gate_api.py
    - tests/api/test_lead_brief_routing_api.py
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: `GET`, `PATCH`, `save-version`, `versions`, `version detail`, and `restore` all speak the shared DTO contract.
    - Test 2: the generate handoff still returns `/opportunities/{id}/lead-brief` and now carries the Lead Brief current-resource metadata.
    - Test 3: the route dispatcher recognizes the canonical `lead-brief` slug and branches into the Lead Brief workspace seam.
    - Test 4: API responses remain explicit about blocked writes versus read-only viewing.
  </behavior>
  <action>
    Keep the backend route wiring small and centralized.

    1. Add `apps/api/app/lead_brief_routes.py` and register it in `apps/api/app/main.py`.
    2. Update `apps/api/app/opportunity_routes.py` so the generate flow uses the new service and emits the canonical Lead Brief handoff response.
    3. Update shared config and shared schema/typing files so the public route helpers and Lead Brief DTOs stay aligned with the backend.
    4. Keep all public route strings centralized; do not duplicate `lead-brief` string literals across the app.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_lead_brief_api.py -q && PYTHONPATH=apps/api pytest tests/api/test_lead_brief_gate_api.py -q && PYTHONPATH=apps/api pytest tests/api/test_lead_brief_routing_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The API routes are registered and return the shared Lead Brief DTOs.
    - Generate handoff, current-resource read/write, version list/detail, and restore are all wired.
    - The API regression command exits `0`.
  </acceptance_criteria>
  <done>
    The backend is now a complete, typed Lead Brief surface for the browser to consume.
  </done>
</task>

</tasks>

<verification>
Wave 2 is complete only when the backend persists current resource plus immutable versions, enforces `expected_revision_no`, and exposes typed routes that satisfy the API tests.
</verification>

<success_criteria>
- Lead Brief data is persisted as a dedicated current resource plus immutable history.
- The generate handoff, read/write endpoints, and restore semantics are all real.
- Shared types, schemas, and route helpers match the backend contract.
</success_criteria>
