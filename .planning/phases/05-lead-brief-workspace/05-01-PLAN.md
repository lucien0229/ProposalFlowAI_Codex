---
phase: 05-lead-brief-workspace
plan: 01
type: execute
wave: 1
depends_on:
  - "04"
files_modified:
  - packages/shared-types/index.ts
  - packages/shared-schemas/index.ts
  - packages/shared-config/index.ts
  - apps/api/app/opportunity_service.py
  - apps/api/app/opportunity_routes.py
  - tests/api/test_lead_brief_routing_api.py
  - tests/api/test_lead_brief_api.py
requirements:
  - STATE-01
  - BRIEF-01
  - BRIEF-02
  - UI-01
must_haves:
  truths:
    - "The first wave must fail fast on route drift, DTO drift, and missing Lead Brief contracts."
    - "Public URL segments must normalize to `lead-brief` while internal enums can stay snake_case."
    - "Lead Brief API tests must cover current resource reads, save current, save version, version list/detail, restore, and stale revision rejection before implementation starts."
  artifacts:
    - path: "tests/api/test_lead_brief_routing_api.py"
      provides: "Failing route-slug and current_step_url coverage"
    - path: "tests/api/test_lead_brief_api.py"
      provides: "Failing current-resource/versioning/concurrency contract coverage"
    - path: "packages/shared-config/index.ts"
      provides: "Canonical route builders and lead-brief path helpers"
  key_links:
    - from: "packages/shared-config/index.ts"
      to: "apps/api/app/opportunity_service.py"
      via: "current_step_url and lead-brief redirect normalization"
      pattern: "lead-brief|current_step_url|OpportunityCurrentStep"
    - from: "tests/api/test_lead_brief_api.py"
      to: "apps/api/app/lead_brief_routes.py"
      via: "current resource and version snapshot contract"
      pattern: "Save current|Save version|Restore|409|current_revision_no"
---

<objective>
Freeze the Lead Brief contract before implementation by making route drift and resource-shape drift visible in red tests.

This wave does not build the workspace UI yet. It makes the API and shared contract expectations precise enough that the backend wave can implement without re-opening scope.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-lead-brief-workspace/05-CONTEXT.md
@.planning/phases/05-lead-brief-workspace/05-RESEARCH.md
@.planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize the public Lead Brief route contract and make route drift fail in tests</name>
  <files>packages/shared-config/index.ts, packages/shared-types/index.ts, apps/api/app/opportunity_service.py, tests/api/test_lead_brief_routing_api.py</files>
  <read_first>
    - packages/shared-config/index.ts
    - packages/shared-types/index.ts
    - apps/api/app/opportunity_service.py
    - .planning/phases/05-lead-brief-workspace/05-CONTEXT.md
    - .planning/phases/05-lead-brief-workspace/05-RESEARCH.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: `current_step_url` for Lead Brief resolves to `/opportunities/{id}/lead-brief`, not `/lead_brief`.
    - Test 2: `POST /api/v1/opportunities/{id}/lead-brief/generate` and dashboard/list links all point at the same canonical slug.
    - Test 3: internal enum names may remain snake_case, but the public route helper must normalize to the hyphenated slug.
  </behavior>
  <action>
    Run the new routing test from RED first, then update the shared route helper and opportunity serialization.

    1. Create `tests/api/test_lead_brief_routing_api.py` with route-drift assertions.
    2. Update `packages/shared-config/index.ts` so the Lead Brief public route is generated from a canonical mapping instead of interpolating raw enum values.
    3. Update `apps/api/app/opportunity_service.py` so `build_current_step_url()` emits the hyphenated Lead Brief segment.
    4. Keep the route drift fix centralized; do not duplicate string handling in web-only code.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_lead_brief_routing_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The route test file exists and fails before implementation.
    - Lead Brief URLs are canonicalized to `lead-brief`.
    - The routing-focused pytest command exits `0`.
  </acceptance_criteria>
  <done>
    Public route drift is locked down before the resource model is built.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing current-resource and version-history API contract tests</name>
  <files>tests/api/test_lead_brief_api.py, tests/api/test_lead_brief_gate_api.py, packages/shared-types/index.ts, packages/shared-schemas/index.ts</files>
  <read_first>
    - tests/api/test_lead_brief_gate_api.py
    - packages/shared-types/index.ts
    - packages/shared-schemas/index.ts
    - .planning/phases/05-lead-brief-workspace/05-CONTEXT.md
    - .planning/phases/05-lead-brief-workspace/05-RESEARCH.md
    - .planning/phases/05-lead-brief-workspace/05-UI-SPEC.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test 1: empty current Lead Brief can be read without a version row.
    - Test 2: `Save current` updates only the current working copy.
    - Test 3: `Save version` appends an immutable full snapshot and increments version history.
    - Test 4: version list/detail returns read-only snapshots and restore replaces the current working copy.
    - Test 5: stale `expected_revision_no` returns `409 Conflict` with a conservative reload hint.
    - Test 6: billing-restricted writes stay blocked while read-only viewing still works.
  </behavior>
  <action>
    Run the Lead Brief API test file from RED first, then lock the contract in the test layer.

    1. Create `tests/api/test_lead_brief_api.py` with the current-resource, save, version, restore, and conflict scenarios.
    2. Extend `packages/shared-types/index.ts` and `packages/shared-schemas/index.ts` with the Lead Brief DTOs, version response shapes, field-state union, and `expected_revision_no`.
    3. Keep the DTOs field-oriented and structured; do not test against free-form prose.
    4. Keep the blocked-write semantics explicit in the tests so later implementation cannot collapse them into generic auth errors.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_lead_brief_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The Lead Brief API test file exists and is red before implementation.
    - The test suite encodes current resource, immutable snapshot, restore, and conflict rules.
    - The targeted pytest command exits `0` only after the backend wave implements the contract.
  </acceptance_criteria>
  <done>
    The API contract is fully specified in tests and ready for backend implementation.
  </done>
</task>

</tasks>

<verification>
Wave 1 is complete only when the route slug is canonicalized, the API contract is written in red tests, and the shared DTO layer has explicit Lead Brief shapes.
</verification>

<success_criteria>
- Lead Brief route drift is impossible to miss in tests.
- Current-resource and version-history behavior is defined before implementation.
- The contract is explicit about `expected_revision_no`, restore, and billing-restricted write blocking.
</success_criteria>
