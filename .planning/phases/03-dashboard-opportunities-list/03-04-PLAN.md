---
phase: 03-dashboard-opportunities-list
plan: 04
type: execute
wave: 4
depends_on:
  - "02"
  - "03"
files_modified:
  - apps/web/app/dashboard/page.tsx
  - apps/web/components/dashboard/dashboard-page.tsx
  - apps/web/components/dashboard/new-opportunity-dialog.tsx
  - apps/web/components/dashboard/recent-opportunities.tsx
  - apps/web/components/dashboard/needs-attention-list.tsx
  - apps/web/components/dashboard/billing-card.tsx
  - apps/web/lib/dashboard-api.ts
  - tests/e2e/dashboard-command-center.spec.ts
  - tests/e2e/dashboard-command-center-accessibility.spec.ts
autonomous: true
requirements:
  - DASH-01
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Dashboard must function as a command center that starts new work and resumes current work without becoming a metrics board."
    - "Dashboard success is defined by visible CTA hierarchy, clear blocker explanations, and browser-verified empty/error/restricted behavior."
  artifacts:
    - path: "apps/web/app/dashboard/page.tsx"
      provides: "Phase 3 dashboard route"
    - path: "apps/web/components/dashboard/dashboard-page.tsx"
      provides: "Dashboard layout/composition"
    - path: "apps/web/components/dashboard/new-opportunity-dialog.tsx"
      provides: "Explicit create-opportunity entrypoint"
    - path: "tests/e2e/dashboard-command-center.spec.ts"
      provides: "Dashboard behavior coverage"
  key_links:
    - from: "apps/web/app/dashboard/page.tsx"
      to: "apps/web/lib/dashboard-api.ts"
      via: "summary data bootstrap"
      pattern: "summary_counts|recent_opportunities|needs_attention|billing_snapshot"
    - from: "apps/web/components/dashboard/new-opportunity-dialog.tsx"
      to: "apps/api/app/product.py"
      via: "POST /api/v1/opportunities"
      pattern: "title|company_name|requested_service"
---

<objective>
Implement the real dashboard command center with create/resume behavior, attention surfaces, and billing visibility.

Purpose: turn `/dashboard` from a phase-2 handoff page into the real working entrypoint for ProposalFlow AI.
Output: a browser-verified dashboard that supports create, resume, attention triage, and restriction-aware state handling.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
@.planning/phases/03-dashboard-opportunities-list/03-RESEARCH.md
@.planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
@apps/web/app/dashboard/page.tsx
@apps/web/components/product-shell.tsx
@apps/web/components/product-state-block.tsx
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the dashboard surface and state hierarchy with Playwright-first flow</name>
  <files>apps/web/app/dashboard/page.tsx, apps/web/components/dashboard/dashboard-page.tsx, apps/web/components/dashboard/recent-opportunities.tsx, apps/web/components/dashboard/needs-attention-list.tsx, apps/web/components/dashboard/billing-card.tsx, apps/web/lib/dashboard-api.ts, tests/e2e/dashboard-command-center.spec.ts</files>
  <read_first>
    - apps/web/app/dashboard/page.tsx
    - apps/web/components/product-shell.tsx
    - apps/web/components/product-state-block.tsx
    - apps/web/lib/auth-bootstrap.ts
    - .planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
    - .planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
    - tests/e2e/auth-setup.spec.ts
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Follow TDD inside this plan:
    1. Expand `tests/e2e/dashboard-command-center.spec.ts` with failing scenarios for:
       - empty dashboard shows `New opportunity` and empty-state guidance
       - normal dashboard shows `Recent opportunities`, `Needs attention`, and `Trial / Billing`
       - restricted dashboard keeps content visible but explains blocked actions
       - clicking a recent/attention item opens the returned `current_step_url`

    2. Implement `apps/web/lib/dashboard-api.ts` and the dashboard component files so `/dashboard`:
       - fetches `GET /api/v1/dashboard/summary`
       - uses `product-shell`
       - renders loading/empty/error/blocked/success through `product-state-block`
       - places `Needs attention` above compact summary counts
       - keeps the billing card compact unless the workspace is restricted

    3. Replace the phase-2 placeholder in `apps/web/app/dashboard/page.tsx` with the real phase-3 page entry.

    Keep composition utility-first and restrained. Do not add KPI charts, hero copy, or dashboard mosaics.
  </action>
  <verify>
    <automated>test -f apps/web/components/dashboard/dashboard-page.tsx && test -f apps/web/components/dashboard/needs-attention-list.tsx && grep -q 'Needs attention' apps/web/components/dashboard/dashboard-page.tsx && pnpm exec playwright test tests/e2e/dashboard-command-center.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `/dashboard` is implemented by `apps/web/app/dashboard/page.tsx` and consumes `apps/web/lib/dashboard-api.ts`.
    - `tests/e2e/dashboard-command-center.spec.ts` exits `0`.
    - The page renders `Recent opportunities`, `Needs attention`, and `Trial / Billing`, with attention visually prioritized over summary counts.
  </acceptance_criteria>
  <done>
    The dashboard behaves like the product's command center rather than a transition or BI page.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit new-opportunity creation and browser-side validation</name>
  <files>apps/web/components/dashboard/new-opportunity-dialog.tsx, apps/web/app/dashboard/page.tsx, apps/web/lib/dashboard-api.ts, tests/e2e/dashboard-command-center.spec.ts, tests/e2e/dashboard-command-center-accessibility.spec.ts</files>
  <read_first>
    - apps/web/components/workspace-setup-form.tsx
    - tests/e2e/helpers/opportunities.ts
    - apps/web/lib/product-api.ts
    - .planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
    - .planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
    - docs/web/ProposalFlow AI｜页面流程与信息架构说明 v1.0.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Follow TDD inside this plan:
    1. Add failing Playwright coverage for:
       - opening the new-opportunity dialog/drawer
       - required-field validation for `title` and `company_name`
       - successful create redirects to `/opportunities/{id}/overview`
       - resume CTA uses the API-provided `current_step_url`
       - keyboard navigation can open, complete, and close the create flow cleanly

    2. Implement `apps/web/components/dashboard/new-opportunity-dialog.tsx` using a lightweight modal or drawer, with fields:
       - `title`
       - `company_name`
       - `requested_service`
       - `source_type` defaulting to `manual`

    3. Wire the dialog into the dashboard page, submit through `POST /api/v1/opportunities`, and refresh dashboard data after success.

    4. Update the accessibility spec so the new-opportunity entry flow has visible focus states, no overflow, and clear live-region feedback for validation errors.
  </action>
  <verify>
    <automated>grep -q 'title' apps/web/components/dashboard/new-opportunity-dialog.tsx && grep -q 'company_name' apps/web/components/dashboard/new-opportunity-dialog.tsx && grep -q 'requested_service' apps/web/components/dashboard/new-opportunity-dialog.tsx && pnpm exec playwright test tests/e2e/dashboard-command-center.spec.ts -g 'new opportunity|resume' && pnpm exec playwright test tests/e2e/dashboard-command-center-accessibility.spec.ts -g 'new opportunity'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/dashboard/new-opportunity-dialog.tsx` contains fields for `title`, `company_name`, and `requested_service`.
    - Successful creation redirects to `/opportunities/{id}/overview` instead of staying on the dashboard.
    - The targeted Playwright dashboard create/resume specs exit `0`.
  </acceptance_criteria>
  <done>
    Users can explicitly create a new opportunity from the dashboard and immediately enter the main workflow.
  </done>
</task>

</tasks>

<verification>
Dashboard behavior is green in the browser, with explicit create/resume flows and state handling rather than mocked placeholders.
</verification>

<success_criteria>
- Dashboard is the real customer command center.
- Create/resume flows are browser-tested and state-aware.
- Empty/error/restricted behavior is productized, not placeholder text.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-opportunities-list/03-04-SUMMARY.md`
</output>
