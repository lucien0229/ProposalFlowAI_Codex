---
phase: 03-dashboard-opportunities-list
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - apps/web/app/layout.tsx
  - apps/web/app/globals.css
  - apps/web/components/product-shell.tsx
  - apps/web/components/product-nav.tsx
  - apps/web/components/product-state-block.tsx
  - apps/web/lib/product-api.ts
  - tests/smoke/product-shell-phase3.spec.ts
  - tests/e2e/dashboard-command-center-accessibility.spec.ts
autonomous: true
requirements:
  - DASH-01
  - OPP-01
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Phase 3 needs a stable logged-in shell before dashboard and list pages can feel like one product surface."
    - "Shared loading, empty, error, blocked, retry, and success patterns must exist as reusable product components rather than repeated page-specific copy."
  artifacts:
    - path: "apps/web/components/product-shell.tsx"
      provides: "Reusable logged-in customer shell"
    - path: "apps/web/components/product-nav.tsx"
      provides: "Stable primary navigation with active state"
    - path: "apps/web/components/product-state-block.tsx"
      provides: "Reusable shipping-state primitives"
    - path: "tests/smoke/product-shell-phase3.spec.ts"
      provides: "Browser smoke coverage for shell/navigation"
  key_links:
    - from: "apps/web/components/product-nav.tsx"
      to: "packages/shared-config/index.ts"
      via: "route constants and active nav highlighting"
      pattern: "/dashboard|/opportunities|/billing|/settings"
    - from: "apps/web/components/product-state-block.tsx"
      to: "apps/web/app/globals.css"
      via: "shared state styling"
      pattern: "loading|empty|error|blocked|retry|success"
---

<objective>
Build the logged-in customer shell and shared state primitives that every Phase 3 surface will use.

Purpose: replace the transitional post-auth chrome with a stable product frame that dashboard and opportunities pages can mount into without duplicating navigation, layout, or state logic.
Output: reusable shell components, shared API helper, and browser smoke coverage.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
@.planning/phases/03-dashboard-opportunities-list/03-RESEARCH.md
@.planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
@apps/web/app/layout.tsx
@apps/web/app/globals.css
@apps/web/app/dashboard/page.tsx
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the reusable logged-in customer shell with browser-first smoke coverage</name>
  <files>apps/web/app/layout.tsx, apps/web/app/globals.css, apps/web/components/product-shell.tsx, apps/web/components/product-nav.tsx, tests/smoke/product-shell-phase3.spec.ts</files>
  <read_first>
    - apps/web/app/layout.tsx
    - apps/web/app/globals.css
    - apps/web/components/auth-shell.tsx
    - apps/web/components/workspace-setup-shell.tsx
    - apps/web/app/dashboard/page.tsx
    - .planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Follow TDD inside this plan:
    1. Add a failing Playwright smoke spec in `tests/smoke/product-shell-phase3.spec.ts` that checks:
       - primary nav shows `Dashboard`, `Opportunities`, `Templates & Rules`, `Billing`, `Settings`
       - active nav state is visually represented
       - shell renders workspace context without a hero-style marketing section

    2. Implement `apps/web/components/product-shell.tsx` and `apps/web/components/product-nav.tsx` as the stable logged-in frame for Phase 3.

    3. Update `apps/web/app/layout.tsx` and `apps/web/app/globals.css` so the product shell follows `03-UI-SPEC.md`: dark restrained console, one accent color, minimal chrome, desktop/laptop browser navigation behavior, and no dashboard-card mosaic baseline.

    Keep auth/setup shells untouched except for any root-level support needed to host the new product shell cleanly.
  </action>
  <verify>
    <automated>test -f apps/web/components/product-shell.tsx && test -f apps/web/components/product-nav.tsx && grep -q 'Dashboard' apps/web/components/product-nav.tsx && grep -q 'Opportunities' apps/web/components/product-nav.tsx && pnpm exec playwright test tests/smoke/product-shell-phase3.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/product-nav.tsx` renders `Dashboard`, `Opportunities`, `Templates & Rules`, `Billing`, and `Settings`.
    - `tests/smoke/product-shell-phase3.spec.ts` exits `0`.
    - The shell uses the restrained dark console treatment from `03-UI-SPEC.md`, not the phase-2 handoff hero layout.
  </acceptance_criteria>
  <done>
    Dashboard and opportunities pages now have one stable logged-in frame to mount into.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add reusable shipping-state primitives and shared product API helper</name>
  <files>apps/web/components/product-state-block.tsx, apps/web/lib/product-api.ts, apps/web/app/globals.css, tests/e2e/dashboard-command-center-accessibility.spec.ts</files>
  <read_first>
    - apps/web/components/status-panel.tsx
    - apps/web/lib/auth-bootstrap.ts
    - apps/web/app/globals.css
    - tests/e2e/auth-setup-accessibility.spec.ts
    - .planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
    - .planning/phases/03-dashboard-opportunities-list/03-VALIDATION.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Follow TDD inside this plan:
    1. Expand `tests/e2e/dashboard-command-center-accessibility.spec.ts` with failing coverage for shell keyboard flow, visible focus, no horizontal overflow, and one correctly exposed shared state region.

    2. Implement `apps/web/components/product-state-block.tsx` that can render `loading`, `empty`, `error`, `blocked`, `retry`, and `success` with explicit heading, body, CTA, and state semantics.

    3. Implement `apps/web/lib/product-api.ts` with shared fetch helpers for:
       - authenticated GET requests
       - normalized non-200 handling
       - retry-safe JSON parsing

    4. Update `apps/web/app/globals.css` to style the shared state component and focus behavior without adding decorative gradients or hover scale effects.
  </action>
  <verify>
    <automated>test -f apps/web/components/product-state-block.tsx && test -f apps/web/lib/product-api.ts && grep -q 'loading' apps/web/components/product-state-block.tsx && grep -q 'blocked' apps/web/components/product-state-block.tsx && pnpm exec playwright test tests/e2e/dashboard-command-center-accessibility.spec.ts -g 'navigation|shared state'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/product-state-block.tsx` explicitly supports `loading`, `empty`, `error`, `blocked`, `retry`, and `success`.
    - `apps/web/lib/product-api.ts` centralizes authenticated JSON fetch/error parsing for Phase 3 pages.
    - `pnpm exec playwright test tests/e2e/dashboard-command-center-accessibility.spec.ts -g 'navigation|shared state'` exits `0`.
  </acceptance_criteria>
  <done>
    Phase 3 pages can reuse one consistent state vocabulary and one product fetch helper instead of hand-rolling state/error logic per page.
  </done>
</task>

</tasks>

<verification>
The repo has one real logged-in product shell and one shared state primitive layer before page-specific UI work begins.
</verification>

<success_criteria>
- Logged-in customer shell exists and is browser-tested.
- Shared state blocks cover all required product states.
- Dashboard/list implementation can reuse shell, fetch helpers, and state primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-opportunities-list/03-03-SUMMARY.md`
</output>
