---
phase: 03-dashboard-opportunities-list
plan: 05
type: execute
wave: 5
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - apps/web/app/opportunities/page.tsx
  - apps/web/components/opportunities/opportunities-page.tsx
  - apps/web/components/opportunities/opportunities-toolbar.tsx
  - apps/web/components/opportunities/opportunity-row.tsx
  - apps/web/components/opportunities/archive-toggle.tsx
  - apps/web/lib/opportunities-api.ts
  - tests/e2e/opportunities-list.spec.ts
  - tests/e2e/opportunities-list-accessibility.spec.ts
autonomous: true
requirements:
  - OPP-01
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Opportunities must feel like a workflow queue for small service teams, not a CRM spreadsheet or admin table."
    - "Search, filter, minimal sort, archive, and open behavior must be URL-backed and browser-verified."
  artifacts:
    - path: "apps/web/app/opportunities/page.tsx"
      provides: "Phase 3 opportunities route"
    - path: "apps/web/components/opportunities/opportunities-toolbar.tsx"
      provides: "Search/filter/minimal-sort toolbar"
    - path: "apps/web/components/opportunities/opportunity-row.tsx"
      provides: "Scan-friendly row-card presentation"
    - path: "tests/e2e/opportunities-list.spec.ts"
      provides: "Primary opportunities browser flow coverage"
  key_links:
    - from: "apps/web/components/opportunities/opportunities-toolbar.tsx"
      to: "packages/shared-config/index.ts"
      via: "frozen query param names and sort presets"
      pattern: "q|status|archived|order_by|order_direction"
    - from: "apps/web/components/opportunities/opportunity-row.tsx"
      to: "apps/api/app/product.py"
      via: "open/archive/unarchive actions"
      pattern: "current_step_url|archive|unarchive"
---

<objective>
Implement the opportunities management surface with URL-backed search/filter/minimal sort, archive/unarchive, and open behavior.

Purpose: give users one reliable place to locate and manage all active or archived opportunities without drifting into CRM/admin UX.
Output: the real `/opportunities` route with browser-tested queue behavior and desktop/laptop accessibility.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
@.planning/phases/03-dashboard-opportunities-list/03-RESEARCH.md
@.planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
@apps/web/components/product-shell.tsx
@apps/web/components/product-state-block.tsx
@docs/web/ProposalFlow AI｜页面清单与页面级需求 v1.0.md
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the URL-backed list surface with Playwright-first flow</name>
  <files>apps/web/app/opportunities/page.tsx, apps/web/components/opportunities/opportunities-page.tsx, apps/web/components/opportunities/opportunities-toolbar.tsx, apps/web/components/opportunities/opportunity-row.tsx, apps/web/lib/opportunities-api.ts, tests/e2e/opportunities-list.spec.ts</files>
  <read_first>
    - apps/web/app/dashboard/page.tsx
    - apps/web/components/dashboard/new-opportunity-dialog.tsx
    - apps/web/components/product-shell.tsx
    - tests/e2e/helpers/opportunities.ts
    - .planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
    - .planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Follow TDD inside this plan:
    1. Expand `tests/e2e/opportunities-list.spec.ts` with failing scenarios for:
       - initial list render
       - `q` search by `title` and `company_name`
       - status filter
       - archived toggle
       - minimal sort presets `Recently updated`, `Oldest updated`, `Recently created`
       - query params reflected in the URL
       - clicking a row opens the API-provided `current_step_url`

    2. Implement `apps/web/lib/opportunities-api.ts` plus the page/components so `/opportunities`:
       - fetches server data from `GET /api/v1/opportunities`
       - uses the shared product shell and product-state block
       - renders a toolbar with `New opportunity`, `Search`, `Status`, `Archived`, and minimal sort
       - renders scan-friendly row cards with `title`, `company_name`, `current_step`, `status/attention`, `updated_at`, and `owner`

    Keep the layout compact and desktop/laptop-friendly. Do not implement a spreadsheet grid, saved views, assignment tabs, or mobile/tablet-specific layouts.
  </action>
  <verify>
    <automated>test -f apps/web/components/opportunities/opportunities-toolbar.tsx && test -f apps/web/components/opportunities/opportunity-row.tsx && grep -q 'Recently updated' apps/web/components/opportunities/opportunities-toolbar.tsx && grep -q 'current_step' apps/web/components/opportunities/opportunity-row.tsx && pnpm exec playwright test tests/e2e/opportunities-list.spec.ts -g 'search|filter|sort|open'</automated>
  </verify>
  <acceptance_criteria>
    - `/opportunities` is implemented and fetches real server data.
    - The toolbar exposes `Search`, `Status`, `Archived`, and the three frozen sort presets.
    - `pnpm exec playwright test tests/e2e/opportunities-list.spec.ts -g 'search|filter|sort|open'` exits `0`.
  </acceptance_criteria>
  <done>
    Users can reliably locate and open the right opportunity from a real workflow queue.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add archive/unarchive, filtered-empty, retry, and accessibility hardening</name>
  <files>apps/web/components/opportunities/archive-toggle.tsx, apps/web/components/opportunities/opportunities-page.tsx, apps/web/lib/opportunities-api.ts, tests/e2e/opportunities-list.spec.ts, tests/e2e/opportunities-list-accessibility.spec.ts</files>
  <read_first>
    - apps/web/components/product-state-block.tsx
    - tests/e2e/opportunities-list.spec.ts
    - tests/e2e/auth-setup-accessibility.spec.ts
    - .planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
    - .planning/phases/03-dashboard-opportunities-list/03-VALIDATION.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Follow TDD inside this plan:
    1. Add failing Playwright coverage for:
       - archive/unarchive updates current view immediately
       - filtered-empty state keeps the query/filter visible and offers reset
       - API error state keeps toolbar orientation and exposes retry CTA
       - no horizontal overflow or focus-trap regressions at `1024px`

    2. Implement `apps/web/components/opportunities/archive-toggle.tsx` and any required list/page logic so archive actions stay explicit and refresh the visible list without a full reload.

    3. Update `tests/e2e/opportunities-list-accessibility.spec.ts` and the page/components so:
       - keyboard users can move through toolbar → row → archive action
       - status/alert regions have clear semantics
       - desktop/laptop row-card density remains scannable and overflow-free
  </action>
  <verify>
    <automated>test -f apps/web/components/opportunities/archive-toggle.tsx && grep -q 'Retry' apps/web/components/opportunities/opportunities-page.tsx && pnpm exec playwright test tests/e2e/opportunities-list.spec.ts -g 'archive|retry|empty' && pnpm exec playwright test tests/e2e/opportunities-list-accessibility.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Archive/unarchive is exposed as an explicit action and refreshes the current list state immediately.
    - Filtered-empty and retry states keep toolbar context visible.
    - `pnpm exec playwright test tests/e2e/opportunities-list-accessibility.spec.ts` exits `0`.
  </acceptance_criteria>
  <done>
    The opportunities queue is usable under real search/filter/archive/error conditions, not just the happy path.
  </done>
</task>

</tasks>

<verification>
The opportunities surface is green in the browser for search/filter/minimal sort/archive/open behavior and accessible on smaller widths.
</verification>

<success_criteria>
- `/opportunities` works as a real workflow queue.
- URL-backed search/filter/minimal sort and archive/open behavior are browser-tested.
- Retry, filtered-empty, and accessibility behavior are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-opportunities-list/03-05-SUMMARY.md`
</output>
