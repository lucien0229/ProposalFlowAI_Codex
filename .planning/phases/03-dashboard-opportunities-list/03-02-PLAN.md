---
phase: 03-dashboard-opportunities-list
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/api/app/main.py
  - apps/api/app/product.py
  - apps/api/app/db.py
  - apps/api/app/opportunity_models.py
  - apps/api/app/opportunity_repository.py
  - apps/api/app/opportunity_service.py
  - apps/api/app/dashboard_service.py
  - apps/api/alembic/versions/0003_phase3_opportunities.py
  - tests/api/test_dashboard_opportunities_api.py
autonomous: true
requirements:
  - DASH-01
  - OPP-01
  - UI-02
must_haves:
  truths:
    - "Phase 3 cannot be launch-ready unless opportunity records, dashboard summary data, and archive behavior are backed by real API and persistence logic."
    - "Dashboard and opportunities UI must consume backend-provided current-step, readiness, and restriction semantics rather than frontend-only inference."
  artifacts:
    - path: "apps/api/app/opportunity_models.py"
      provides: "Persistent opportunity model aligned to the frozen DB contract"
    - path: "apps/api/app/opportunity_repository.py"
      provides: "Workspace-scoped list/detail/archive query logic"
    - path: "apps/api/app/dashboard_service.py"
      provides: "Action-first dashboard summary read model"
    - path: "apps/api/app/product.py"
      provides: "Registered product endpoints for dashboard/opportunities"
    - path: "tests/api/test_dashboard_opportunities_api.py"
      provides: "Green backend contract coverage for Phase 3 endpoints"
  key_links:
    - from: "apps/api/app/opportunity_models.py"
      to: "apps/api/alembic/versions/0003_phase3_opportunities.py"
      via: "schema and migration alignment"
      pattern: "workspace_id|owner_user_id|status|archived_at"
    - from: "apps/api/app/dashboard_service.py"
      to: "tests/api/test_dashboard_opportunities_api.py"
      via: "summary payload contract"
      pattern: "recent_opportunities|needs_attention|summary_counts"
---

<objective>
Implement the real backend for Phase 3: persistence, product endpoints, and dashboard summary logic.

Purpose: give the dashboard and opportunities UI durable, workspace-scoped APIs for create/list/detail/archive/unarchive and action-first dashboard summary data.
Output: green backend tests plus real product endpoints registered in the FastAPI app.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
@.planning/phases/03-dashboard-opportunities-list/03-RESEARCH.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@docs/share/ProposalFlow AI｜数据库设计 v1.0.md
@apps/api/app/main.py
@apps/api/app/product.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build opportunity persistence and CRUD/archive endpoints with pytest-first flow</name>
  <files>apps/api/app/db.py, apps/api/app/opportunity_models.py, apps/api/app/opportunity_repository.py, apps/api/app/opportunity_service.py, apps/api/app/product.py, apps/api/alembic/versions/0003_phase3_opportunities.py, tests/api/test_dashboard_opportunities_api.py</files>
  <read_first>
    - apps/api/app/main.py
    - apps/api/app/product.py
    - apps/api/alembic/env.py
    - tests/api/test_dashboard_opportunities_api.py
    - .planning/phases/03-dashboard-opportunities-list/03-RESEARCH.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Follow TDD inside this plan:
    1. Expand `tests/api/test_dashboard_opportunities_api.py` with failing tests for:
       - `POST /api/v1/opportunities` accepts `title`, `company_name`, `requested_service`, optional `source_type`, and returns the created record with an Overview redirect target
       - `GET /api/v1/opportunities` honors `q`, `status`, `cursor`, `limit`, `order_by`, and `order_direction`
       - `GET /api/v1/opportunities/{opportunity_id}` returns `current_step`, `step_readiness`, and restriction fields
       - `POST /api/v1/opportunities/{opportunity_id}/archive` and `/unarchive` update `status` and `archived_at`

    2. Implement real persistence files:
       - `apps/api/app/db.py` for SQLAlchemy engine/session helpers
       - `apps/api/app/opportunity_models.py` with concrete columns `workspace_id`, `owner_user_id`, `title`, `source_type`, `company_name`, `contact_name`, `contact_email`, `requested_service`, `status`, `archived_at`, `created_at`, `updated_at`
       - `apps/api/alembic/versions/0003_phase3_opportunities.py` with the opportunity table and indexes for `workspace_id + updated_at`, `workspace_id + status`, and `workspace_id + owner_user_id`
       - `apps/api/app/opportunity_repository.py` and `apps/api/app/opportunity_service.py` for create/list/detail/archive logic

    3. Update `apps/api/app/product.py` to register the real `/opportunities` endpoints rather than placeholder-only routes.

    Keep the implementation workspace-scoped. Do not introduce admin logic or generic data-grid helpers.
  </action>
  <verify>
    <automated>python3 -m py_compile apps/api/app/main.py apps/api/app/product.py apps/api/app/db.py apps/api/app/opportunity_models.py apps/api/app/opportunity_repository.py apps/api/app/opportunity_service.py && grep -q 'POST /api/v1/opportunities' tests/api/test_dashboard_opportunities_api.py && pytest -q tests/api/test_dashboard_opportunities_api.py -k 'create or list or detail or archive'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/app/opportunity_models.py` defines fields `workspace_id`, `owner_user_id`, `title`, `company_name`, `requested_service`, `status`, and `archived_at`.
    - `apps/api/app/product.py` exposes `GET /api/v1/opportunities`, `POST /api/v1/opportunities`, `GET /api/v1/opportunities/{opportunity_id}`, `POST /api/v1/opportunities/{opportunity_id}/archive`, and `POST /api/v1/opportunities/{opportunity_id}/unarchive`.
    - `pytest -q tests/api/test_dashboard_opportunities_api.py -k 'create or list or detail or archive'` exits `0`.
  </acceptance_criteria>
  <done>
    The opportunity queue is now a real product API surface with durable records and archive behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard summary and resume-target semantics with pytest-first flow</name>
  <files>apps/api/app/dashboard_service.py, apps/api/app/opportunity_service.py, apps/api/app/product.py, tests/api/test_dashboard_opportunities_api.py</files>
  <read_first>
    - apps/api/app/product.py
    - apps/api/app/opportunity_service.py
    - tests/api/test_dashboard_opportunities_api.py
    - .planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
    - .planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
    - docs/web/ProposalFlow AI｜页面清单与页面级需求 v1.0.md
    - docs/web/ProposalFlow AI｜页面流程与信息架构说明 v1.0.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Follow TDD inside this plan:
    1. Add failing pytest cases for `GET /api/v1/dashboard/summary` asserting the payload contains:
       - `summary_counts`
       - `recent_opportunities`
       - `needs_attention`
       - `billing_snapshot`
       - per-opportunity `current_step`, `current_step_url`, and `needs_attention_reason` when applicable

    2. Implement `apps/api/app/dashboard_service.py` to compute an action-first summary model from persisted opportunities and workspace billing snapshot data.

    3. Extend `apps/api/app/opportunity_service.py` to normalize resume-target logic:
       - `new` opportunities return `/opportunities/{id}/overview`
       - opportunities with downstream progress return the current actionable step URL
       - blocked or restricted states still include the step URL plus an explanatory restriction field

    4. Register `GET /api/v1/dashboard/summary` in `apps/api/app/product.py`.

    Keep the response intentionally compact. Do not add analytics charts, trend series, or admin-style aggregates.
  </action>
  <verify>
    <automated>grep -q '/dashboard/summary' apps/api/app/product.py && grep -q 'current_step_url' tests/api/test_dashboard_opportunities_api.py && grep -q 'needs_attention' tests/api/test_dashboard_opportunities_api.py && pytest -q tests/api/test_dashboard_opportunities_api.py -k 'dashboard_summary or resume_target'</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/app/product.py` contains `GET /api/v1/dashboard/summary`.
    - `tests/api/test_dashboard_opportunities_api.py` contains named dashboard summary and resume-target tests.
    - `pytest -q tests/api/test_dashboard_opportunities_api.py -k 'dashboard_summary or resume_target'` exits `0`.
  </acceptance_criteria>
  <done>
    The frontend can render Dashboard and resume flows from one authoritative summary API without inventing its own progress logic.
  </done>
</task>

</tasks>

<verification>
API tests prove create/list/detail/archive and dashboard summary semantics are real before any page consumes them.
</verification>

<success_criteria>
- Opportunity persistence exists in the API layer and database migration set.
- Product endpoints cover create/list/detail/archive/unarchive and dashboard summary.
- Resume-target and blocker semantics are backend-authored and pytest-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-opportunities-list/03-02-SUMMARY.md`
</output>
