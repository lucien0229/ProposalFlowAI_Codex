---
phase: 03-dashboard-opportunities-list
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-types/index.ts
  - packages/shared-schemas/index.ts
  - packages/shared-config/index.ts
  - tests/conftest.py
  - tests/api/test_dashboard_opportunities_api.py
  - tests/e2e/helpers/opportunities.ts
autonomous: true
requirements:
  - DASH-01
  - OPP-01
  - UI-02
must_haves:
  truths:
    - "Phase 3 must have one shared vocabulary for opportunity status, step, list filters, sort presets, and dashboard summary data before backend and frontend work begin."
    - "Phase 3 execution must have a TDD-friendly test foundation for API and browser flows instead of adding verification at the end."
  artifacts:
    - path: "packages/shared-types/index.ts"
      provides: "Opportunity and dashboard DTO/type contracts"
    - path: "packages/shared-schemas/index.ts"
      provides: "Frozen list query and response validation shapes"
    - path: "packages/shared-config/index.ts"
      provides: "API/list/query constants and sort presets"
    - path: "tests/api/test_dashboard_opportunities_api.py"
      provides: "API contract test entrypoint for create/list/detail/archive/dashboard summary"
    - path: "tests/conftest.py"
      provides: "Phase-3 test fixtures and client helpers"
  key_links:
    - from: "packages/shared-config/index.ts"
      to: "packages/shared-schemas/index.ts"
      via: "query param naming and sort constants"
      pattern: "q/status/limit/cursor/order_by/order_direction"
    - from: "packages/shared-types/index.ts"
      to: "tests/api/test_dashboard_opportunities_api.py"
      via: "shared response contract"
      pattern: "OpportunityStatus|OpportunityCurrentStep|DashboardSummary"
---

<objective>
Freeze the shared opportunity/dashboard contract surface and establish the Phase 3 TDD test foundation.

Purpose: every later plan must build against the same status, step, query, and summary model, and every implementation slice must have API/browser verification entrypoints ready before production code changes begin.
Output: shared contract updates plus reusable pytest/Playwright helpers for Phase 3.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
@.planning/phases/03-dashboard-opportunities-list/03-RESEARCH.md
@.planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@docs/share/ProposalFlow AI｜数据库设计 v1.0.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared opportunity and dashboard contracts</name>
  <files>packages/shared-types/index.ts, packages/shared-schemas/index.ts, packages/shared-config/index.ts</files>
  <read_first>
    - packages/shared-types/index.ts
    - packages/shared-schemas/index.ts
    - packages/shared-config/index.ts
    - .planning/phases/03-dashboard-opportunities-list/03-CONTEXT.md
    - .planning/phases/03-dashboard-opportunities-list/03-RESEARCH.md
    - .planning/phases/03-dashboard-opportunities-list/03-UI-SPEC.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
  </read_first>
  <action>
    Extend `packages/shared-types/index.ts` with the concrete Phase 3 contracts:
    - `OpportunityStatus` = `new | lead_brief_generated | discovery_added | discovery_reviewed | proposal_draft_generated | proposal_in_review | proposal_ready | follow_up_drafted | archived`
    - `OpportunityCurrentStep` = `overview | lead_brief | discovery | proposal_draft | follow_up`
    - `NeedsAttentionReason` = `missing_input | file_failed | generation_failed | billing_restricted | review_required`
    - DTOs for `OpportunityListItem`, `DashboardSummary`, `DashboardSummaryCounts`, `NeedsAttentionItem`, and create/archive request payloads

    Extend `packages/shared-schemas/index.ts` with explicit schemas for:
    - list query params `q`, `status`, `archived`, `limit`, `cursor`, `order_by`, `order_direction`
    - `OpportunityListItem`
    - `DashboardSummary`

    Extend `packages/shared-config/index.ts` with:
    - `API_ROUTE_PATHS.dashboardSummary = /api/v1/dashboard/summary`
    - `API_ROUTE_PATHS.opportunities = /api/v1/opportunities`
    - `OPPORTUNITY_LIST_DEFAULT_LIMIT = 20`
    - `OPPORTUNITY_SORT_PRESETS` for `updated_at_desc`, `updated_at_asc`, and `created_at_desc`

    Keep the contract surface small and explicit. Do not introduce generic platform-wide list abstractions.
  </action>
  <verify>
    <automated>grep -q 'export type OpportunityStatus' packages/shared-types/index.ts && grep -q 'export type OpportunityCurrentStep' packages/shared-types/index.ts && grep -q 'NeedsAttentionReason' packages/shared-types/index.ts && grep -q 'dashboardSummarySchema' packages/shared-schemas/index.ts && grep -q 'opportunityListQuerySchema' packages/shared-schemas/index.ts && grep -q 'OPPORTUNITY_SORT_PRESETS' packages/shared-config/index.ts && grep -q 'dashboardSummary' packages/shared-config/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/shared-types/index.ts` exports `OpportunityStatus`, `OpportunityCurrentStep`, `NeedsAttentionReason`, `OpportunityListItem`, and `DashboardSummary`.
    - `packages/shared-schemas/index.ts` contains `opportunityListQuerySchema` and `dashboardSummarySchema`.
    - `packages/shared-config/index.ts` contains `API_ROUTE_PATHS.dashboardSummary`, `API_ROUTE_PATHS.opportunities`, `OPPORTUNITY_LIST_DEFAULT_LIMIT`, and `OPPORTUNITY_SORT_PRESETS`.
  </acceptance_criteria>
  <done>
    Backend and frontend work now share the same frozen Phase 3 vocabulary and query semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Establish Phase 3 API/browser test foundation</name>
  <files>tests/conftest.py, tests/api/test_dashboard_opportunities_api.py, tests/e2e/helpers/opportunities.ts</files>
  <read_first>
    - tests/conftest.py
    - package.json
    - tests/e2e/auth-setup.spec.ts
    - tests/smoke/workspace-guard.spec.ts
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
    - .planning/phases/03-dashboard-opportunities-list/03-VALIDATION.md
    - .planning/phases/03-dashboard-opportunities-list/03-RESEARCH.md
  </read_first>
  <action>
    Extend `tests/conftest.py` with reusable Phase 3 fixtures for:
    - authenticated web session cookie values
    - sample workspace and opportunity payload builders
    - FastAPI app/client bootstrap helpers for targeted API tests

    Add `tests/api/test_dashboard_opportunities_api.py` as the canonical Phase 3 backend contract test file with named test cases for:
    - create opportunity
    - list opportunities with filters
    - detail payload includes current step and restriction fields
    - archive/unarchive behavior
    - dashboard summary payload

    Add `tests/e2e/helpers/opportunities.ts` with deterministic browser helpers for:
    - opening the new-opportunity entrypoint
    - filling the minimum create form
    - selecting toolbar filters/search inputs

    The file contents may be scaffolding/helpers only in this plan, but they must be ready for test-first execution in later plans.
  </action>
  <verify>
    <automated>test -f tests/api/test_dashboard_opportunities_api.py && test -f tests/e2e/helpers/opportunities.ts && grep -q 'def test_create_opportunity' tests/api/test_dashboard_opportunities_api.py && grep -q 'def test_dashboard_summary' tests/api/test_dashboard_opportunities_api.py && grep -q 'export async function openNewOpportunity' tests/e2e/helpers/opportunities.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/test_dashboard_opportunities_api.py` defines concrete API behavior tests for create, list, detail, archive, and dashboard summary.
    - `tests/conftest.py` exposes reusable Phase 3 fixtures rather than forcing each API test to hand-roll cookies and workspace data.
    - `tests/e2e/helpers/opportunities.ts` exports browser helpers for create and list interactions that later specs can reuse.
  </acceptance_criteria>
  <done>
    Phase 3 execution can follow TDD without inventing fixtures or helpers ad hoc inside implementation plans.
  </done>
</task>

</tasks>

<verification>
Phase 3 shared contracts and test scaffolding exist before backend or page implementation begins, and the contract surface is grep-verifiable from the repo.
</verification>

<success_criteria>
- Shared opportunity/dashboard contracts are defined in shared packages.
- Phase 3 API/browser test entrypoints exist.
- Later plans can implement against stable contracts and reusable fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-opportunities-list/03-01-SUMMARY.md`
</output>
