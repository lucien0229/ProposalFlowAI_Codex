---
phase: 07-proposal-draft-templates-rules
plan: 05
type: execute
wave: 5
depends_on:
  - "07-03"
  - "07-04"
files_modified:
  - apps/web/components/opportunities/proposal-draft-workspace.tsx
  - apps/web/components/opportunities/proposal-draft-rules-bar.tsx
  - apps/web/components/opportunities/proposal-draft-override-drawer.tsx
  - apps/web/components/opportunities/proposal-draft-status-band.tsx
  - apps/web/components/opportunities/proposal-draft-chapter-block.tsx
  - apps/web/components/opportunities/proposal-draft-version-drawer.tsx
  - apps/web/lib/proposal-draft-copy.ts
  - apps/web/lib/templates-rules-api.ts
  - apps/web/app/globals.css
  - tests/e2e/proposal-draft-workspace.spec.ts
  - tests/api/test_proposal_draft_api.py
autonomous: true
requirements:
  - PROP-01
  - PROP-02
must_haves:
  truths:
    - "Proposal Draft is the flagship workspace for this product. Editing, regenerate risk, versions, and warnings must feel launch-ready, not merely functional."
    - "The locked UI model is: top Rules Summary Bar, central chapter stage, right-side on-demand drawer. Do not regress into an always-visible dual-pane source/output layout."
    - "Template switching and override editing are opportunity-local editing flows inside Proposal Draft. They must be implemented as real interactions, not reduced to passive badges."
    - "Chapter regenerate replaces only the selected section and must explicitly protect unsaved edits. Whole-draft regenerate remains secondary and higher-risk."
    - "Execution is not successful until front-end and back-end are integrated, browser flows are green at 1024/1280/1440 widths, and all critical states are visible in-product."
  artifacts:
    - path: "apps/web/components/opportunities/proposal-draft-workspace.tsx"
      provides: "Main Proposal Draft working surface"
    - path: "apps/web/components/opportunities/proposal-draft-override-drawer.tsx"
      provides: "Opportunity-local template switch and override editing flow"
    - path: "apps/web/components/opportunities/proposal-draft-version-drawer.tsx"
      provides: "Version list, preview, and restore confirmation UX"
    - path: "tests/e2e/proposal-draft-workspace.spec.ts"
      provides: "Chapter editing, regenerate, versions, warning, and restriction browser coverage"
  key_links:
    - from: "apps/web/components/opportunities/proposal-draft-workspace.tsx"
      to: ".planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md"
      via: "layout hierarchy, state band semantics, and desktop-width constraints"
      pattern: "Rules Summary Bar|override active|entry point for opportunity override editing|Regenerate section|Versions|low confidence|billing restriction"
    - from: "apps/web/components/opportunities/proposal-draft-version-drawer.tsx"
      to: ".planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md"
      via: "restore semantics and secondary-surface placement"
      pattern: "Save version|Restore|explicit overwrite|current working draft"
---

<objective>
Turn the Proposal Draft route from state scaffolding into a production-grade editing workspace: rules summary, chapter editing, chapter regenerate, version drawer, export controls, and dual-layer warning/restriction surfaces.

This wave is complete only when users can genuinely work in the draft in the browser and the workspace visibly respects every high-risk interaction rule from the context and UI contract.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
@.planning/phases/07-proposal-draft-templates-rules/07-RESEARCH.md
@.planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
@docs/design/DESIGN.md
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
@/Users/wxm/.codex/superpowers/skills/verification-before-completion/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the editing-first Proposal Draft workspace with template/override flow, chapter actions, and version drawer</name>
  <files>apps/web/components/opportunities/proposal-draft-workspace.tsx, apps/web/components/opportunities/proposal-draft-rules-bar.tsx, apps/web/components/opportunities/proposal-draft-override-drawer.tsx, apps/web/components/opportunities/proposal-draft-chapter-block.tsx, apps/web/components/opportunities/proposal-draft-version-drawer.tsx, apps/web/lib/templates-rules-api.ts, apps/web/app/globals.css, tests/e2e/proposal-draft-workspace.spec.ts</files>
  <read_first>
    - tests/e2e/proposal-draft-workspace.spec.ts
    - apps/web/components/opportunities/lead-brief-workspace.tsx
    - apps/web/components/opportunities/discovery-workspace.tsx
    - .planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - docs/design/DESIGN.md
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - The top Rules Summary Bar is visible and keeps template, effective rules, assumptions/exclusions preview, tone, terminology summary, and `override active` in view.
    - The Rules Summary Bar lets users switch template for the current opportunity, open override editing, and clear the override without leaving Proposal Draft.
    - The chapter stage is the dominant workspace surface.
    - `Save current`, `Save version`, `Regenerate section`, `Regenerate all`, `Versions`, `Copy all`, and `Export` are all visible with the right hierarchy.
    - The right-side drawer supports version list -> preview -> restore confirmation plus opportunity-local override editing, and does not restore on selection alone.
    - Keyboard focus remains visible and stable through chapter actions and drawer interactions.
  </behavior>
  <action>
    Start from RED and build the main Proposal Draft workspace around the editing-first layout contract.

    1. Expand `tests/e2e/proposal-draft-workspace.spec.ts` to assert the final hierarchy, chapter blocks, visible rules bar, template-switch flow, override edit/clear flow, and version-drawer interactions.
    2. Implement `ProposalDraftWorkspace`, `ProposalDraftRulesBar`, `ProposalDraftOverrideDrawer`, `ProposalDraftChapterBlock`, and `ProposalDraftVersionDrawer`.
    3. Wire template switching and override edits to the real opportunity-override APIs; switching template here must mutate only the current opportunity override layer and refresh the visible effective-rules summary immediately.
    4. Make the central chapter stage visually dominant; do not let rule chrome or drawer chrome overwhelm the editing plane.
    5. Keep version preview read-only and restore explicitly confirmed.
    6. Preserve the existing shell language and do not introduce dashboard-card mosaics, report-preview framing, or mobile/tablet layouts.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/proposal-draft-workspace.spec.ts -g 'rules summary|template|override|chapter|versions|restore|drawer'</automated>
  </verify>
  <acceptance_criteria>
    - The Proposal Draft page visibly matches the locked editing-first hierarchy.
    - Users can switch template, edit or clear the opportunity-local override, and see effective-rules changes without mutating workspace baseline rules.
    - The version drawer supports list, preview, and explicit restore confirmation separately.
    - The targeted Playwright command exits `0`.
    - The UI remains credible at `1024px`, `1280px`, and `1440px`.
  </acceptance_criteria>
  <done>
    Proposal Draft is now a real working surface rather than a state shell.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finish chapter regenerate protection, export flows, and dual-layer warning/restriction states</name>
  <files>apps/web/components/opportunities/proposal-draft-workspace.tsx, apps/web/components/opportunities/proposal-draft-status-band.tsx, apps/web/lib/proposal-draft-api.ts, apps/web/lib/proposal-draft-copy.ts, apps/web/app/globals.css, tests/e2e/proposal-draft-workspace.spec.ts, tests/api/test_proposal_draft_api.py</files>
  <read_first>
    - tests/e2e/proposal-draft-workspace.spec.ts
    - tests/api/test_proposal_draft_api.py
    - apps/web/lib/proposal-draft-api.ts
    - apps/web/components/opportunities/proposal-draft-workspace.tsx
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - .planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
    - /Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/verification-before-completion/SKILL.md
  </read_first>
  <action>
    Close the remaining product-risk gaps inside Proposal Draft.

    1. Expand the browser specs to prove:
       - chapter regenerate prompts before replacing unsaved edits
       - `Regenerate all` remains present but secondary
       - page-level status band appears for `low confidence`, `rules conflict`, `missing inputs`, and `billing restriction`
       - local chapter/module markers identify affected sections without duplicating the whole warning wall
       - billing restriction keeps a visible Billing CTA (`Upgrade plan` for `trial_expired` / `inactive`, `Manage billing` for `past_due` / `canceled`)
       - `Copy all` copies the current draft as chapter-ordered plain text, shows success confirmation, and shows actionable recovery when the browser blocks clipboard write
       - export supports `Text` and `Markdown`
    2. Implement `ProposalDraftStatusBand` plus local warning markers and wire them to the real API response fields (`warnings`, `confidence_notes`, `effective_rule_summary`, restriction metadata).
    3. Implement `apps/web/lib/proposal-draft-copy.ts` and wire `Copy all` to the real workspace content with explicit result states, success feedback, and clipboard-denied recovery guidance.
    4. Keep blocked actions visibly disabled with explicit affected-action explanations for `generate`, `regenerate`, `save current`, `save-version`, `restore`, and `export`, while leaving `Copy all` available whenever a current draft exists.
    5. Map billing restriction reasons to deterministic CTA labels and destinations so the user always has a visible recovery path from the status band.
    6. Ensure chapter regenerate remains local and whole-draft regenerate uses stronger overwrite copy.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_proposal_draft_api.py -q && pnpm exec playwright test tests/e2e/proposal-draft-workspace.spec.ts -g 'regenerate|low confidence|rules conflict|restriction|billing|copy|export'</automated>
  </verify>
  <acceptance_criteria>
    - Regenerate protection and export behavior are wired end-to-end against real APIs.
    - Page-level and local warning semantics are both visible in the browser.
    - `Copy all` works with success feedback and clipboard-denied recovery states.
    - Billing restriction surfaces a visible Billing CTA while explaining the affected blocked action scope explicitly.
    - The targeted API + Playwright command exits `0`.
  </acceptance_criteria>
  <done>
    Proposal Draft now behaves like a launch-ready drafting workspace under both normal and risky conditions.
  </done>
</task>

</tasks>

<verification>
Wave 5 is complete only when Proposal Draft is fully editable, versioned, exportable, regenerate-safe, warning-aware, and browser-verified at desktop/laptop widths.
</verification>

<success_criteria>
- Proposal Draft is credible as a real customer-facing drafting workspace.
- Chapter regenerate and version recovery are explicit and safe.
- Warning, conflict, missing-input, and restriction states are both visible and actionable.
</success_criteria>
