---
phase: 07-proposal-draft-templates-rules
plan: 04
type: execute
wave: 4
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
files_modified:
  - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
  - apps/web/app/templates-rules/page.tsx
  - apps/web/lib/proposal-draft-api.ts
  - apps/web/lib/templates-rules-api.ts
  - apps/web/lib/proposal-draft-copy.ts
  - apps/web/components/opportunities/proposal-draft-workspace.tsx
  - apps/web/components/templates-rules/templates-rules-page.tsx
  - tests/e2e/proposal-draft-workspace.spec.ts
  - tests/e2e/templates-rules.spec.ts
autonomous: true
requirements:
  - RULE-01
  - PROP-01
  - PROP-02
must_haves:
  truths:
    - "This phase is not successful if the web app only renders happy-path content. Empty/loading/error/blocked/retry states must be real, browser-verifiable product states."
    - "Proposal Draft and Templates & Rules must feel like shipping web-app surfaces in the existing shell, not placeholders or spec pages."
    - "Web implementation must follow `07-UI-SPEC.md`, `docs/design/DESIGN.md`, `frontend-skill`, and `ui-ux-pro-max` for hierarchy, restraint, and dense-form quality."
    - "Browser verification is mandatory. Route scaffolding, state surfaces, and return paths must be proven in Playwright before deep polish work starts."
  artifacts:
    - path: "apps/web/lib/proposal-draft-api.ts"
      provides: "Client API boundary for Proposal Draft resource, versions, regenerate, and export"
    - path: "apps/web/lib/templates-rules-api.ts"
      provides: "Client API boundary for templates list, workspace rules, effective rules, and overrides"
    - path: "tests/e2e/proposal-draft-workspace.spec.ts"
      provides: "Proposal Draft browser-state coverage"
    - path: "tests/e2e/templates-rules.spec.ts"
      provides: "Templates & Rules browser-state coverage"
  key_links:
    - from: "apps/web/app/opportunities/[opportunityId]/[step]/page.tsx"
      to: ".planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md"
      via: "route-state hierarchy and opportunity-shell integration"
      pattern: "Proposal Draft|Rules Summary Bar|loading|blocked|retry"
    - from: "apps/web/app/templates-rules/page.tsx"
      to: ".planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md"
      via: "global-shell placement and return-to-draft expectations"
      pattern: "Templates & Rules|workspace baseline|return path|effective rules"
---

<objective>
Build the web scaffolding for both Phase 7 surfaces: real route branches, client API helpers, route-state handling, loading/error boundaries, and initial browser tests for empty/blocked/error/retry behavior.

This wave should not attempt final polish yet. It should make the routes, API calls, state blocks, and verification harness real so later UI waves can refine safely.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
@.planning/phases/07-proposal-draft-templates-rules/07-RESEARCH.md
@.planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
@docs/design/DESIGN.md
@apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
@apps/web/app/templates-rules/page.tsx
@apps/web/components/opportunities/lead-brief-workspace.tsx
@apps/web/components/opportunities/discovery-workspace.tsx
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing browser specs for Phase 7 route states and shell hierarchy</name>
  <files>tests/e2e/proposal-draft-workspace.spec.ts, tests/e2e/templates-rules.spec.ts</files>
  <read_first>
    - .planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - tests/e2e/lead-brief-workspace.spec.ts
    - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
    - apps/web/app/templates-rules/page.tsx
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Proposal Draft empty state shows `Generate draft` with no placeholder/spec copy.
    - Proposal Draft blocked state names missing Lead Brief or Discovery prerequisites.
    - Proposal Draft loading, billing-restricted, and error states preserve shell hierarchy and navigation context.
    - Templates & Rules renders in the global customer shell and not inside the opportunity stepper flow.
    - Templates & Rules empty/validation/error states remain visible in-product and preserve a return path to Proposal Draft.
    - Billing-restricted draft states keep a visible Billing CTA (`Upgrade plan` or `Manage billing`) instead of only disabled actions.
  </behavior>
  <action>
    Start from RED. Write browser specs for the route and state contract before implementing the new web surfaces.

    1. Add `tests/e2e/proposal-draft-workspace.spec.ts` covering empty, blocked, loading, billing-restricted, and error states plus the visible `Rules Summary Bar`, Billing CTA, and opportunity stepper context.
    2. Add `tests/e2e/templates-rules.spec.ts` covering global-shell placement, empty baseline state, validation/error state, and preserved return path behavior.
    3. Keep assertions literal around headings, CTA labels, and blocked-state copy so later UI work cannot hide these requirements behind ambiguous chrome.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/proposal-draft-workspace.spec.ts tests/e2e/templates-rules.spec.ts -g 'empty|blocked|loading|billing|restricted|error|validation'</automated>
  </verify>
  <acceptance_criteria>
    - Both Playwright spec files exist and assert the Phase 7 route-state contract.
    - The targeted Playwright command is red before web implementation exists.
    - The specs contain literal assertions for `Generate draft`, `Proposal Draft`, `Templates & Rules`, and rule/validation state copy.
  </acceptance_criteria>
  <done>
    Browser expectations for Phase 7 state surfaces are frozen before the web UI is built.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement route branches, client API helpers, and state-first workspace shells</name>
  <files>apps/web/app/opportunities/[opportunityId]/[step]/page.tsx, apps/web/app/templates-rules/page.tsx, apps/web/lib/proposal-draft-api.ts, apps/web/lib/templates-rules-api.ts, apps/web/lib/proposal-draft-copy.ts, apps/web/components/opportunities/proposal-draft-workspace.tsx, apps/web/components/templates-rules/templates-rules-page.tsx, tests/e2e/proposal-draft-workspace.spec.ts, tests/e2e/templates-rules.spec.ts</files>
  <read_first>
    - apps/web/app/opportunities/[opportunityId]/[step]/page.tsx
    - apps/web/app/templates-rules/page.tsx
    - apps/web/components/opportunities/discovery-workspace.tsx
    - apps/web/components/opportunities/lead-brief-workspace.tsx
    - apps/web/lib/lead-brief-api.ts
    - apps/web/lib/discovery-api.ts
    - .planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
    - tests/e2e/proposal-draft-workspace.spec.ts
    - tests/e2e/templates-rules.spec.ts
    - /Users/wxm/.codex/skills/frontend-skill/SKILL.md
    - /Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
  </read_first>
  <action>
    Build the state-first Phase 7 web scaffolding that the polished UI will later refine.

    1. Extend the opportunity step route to render a real `ProposalDraftWorkspace` branch for the `proposal_draft` segment.
    2. Replace the Phase 3 placeholder `apps/web/app/templates-rules/page.tsx` with a real customer-shell page that reads current rules/templates and preserves draft return context.
    3. Add `apps/web/lib/proposal-draft-api.ts` and `apps/web/lib/templates-rules-api.ts` mirroring the existing lead-brief/discovery client-helper pattern rather than inventing a one-off transport layer.
    4. Create minimal-but-real workspace shell components that show:
       - correct page titles and subtitles
       - state blocks for loading/empty/error/blocked/retry/restricted
       - placeholder geometry matching the final layout
       - visible route-local CTA labels
    5. Add copy helpers in `apps/web/lib/proposal-draft-copy.ts` so chapter-ordered copy payload assembly and browser result states (`success`, `permission_denied`, `unavailable`) are centralized and testable before the polished workspace lands.
    6. Keep billing-restricted state shells visibly actionable by mapping restriction reasons to a Billing CTA instead of showing disabled controls alone.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/e2e/proposal-draft-workspace.spec.ts tests/e2e/templates-rules.spec.ts -g 'empty|blocked|loading|billing|restricted|error|validation'</automated>
  </verify>
  <acceptance_criteria>
    - Proposal Draft and Templates & Rules are real routes in the shipping shell rather than placeholders.
    - Phase 7 client API helpers exist and follow the established repo pattern.
    - Billing-restricted route states expose a visible Billing CTA and `proposal-draft-copy.ts` defines explicit copy result states.
    - The targeted Playwright route-state command exits `0`.
    - The scaffolding visibly supports loading, empty, error, blocked, retry, restricted, and validation states.
  </acceptance_criteria>
  <done>
    The web routes and state surfaces are real and safe to deepen in later UI waves.
  </done>
</task>

</tasks>

<verification>
Wave 4 is complete only when both Phase 7 routes exist inside the real web shell, client API helpers are in place, and browser tests prove the route-state contract.
</verification>

<success_criteria>
- Phase 7 no longer depends on placeholder routes.
- Browser-verifiable state surfaces exist for both Proposal Draft and Templates & Rules.
- The web scaffolding is ready for dense UI/UX implementation without route ambiguity.
</success_criteria>
