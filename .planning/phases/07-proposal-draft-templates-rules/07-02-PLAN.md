---
phase: 07-proposal-draft-templates-rules
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - apps/api/alembic/versions/0007_phase7_templates_rules.py
  - apps/api/app/main.py
  - apps/api/app/product.py
  - apps/api/app/templates_rules_models.py
  - apps/api/app/templates_rules_repository.py
  - apps/api/app/templates_rules_service.py
  - apps/api/app/templates_rules_routes.py
  - tests/api/test_templates_rules_api.py
autonomous: true
requirements:
  - RULE-01
must_haves:
  truths:
    - "Rules are backend business objects, not frontend-only settings. Validation, conflict handling, and effective-rule composition must live in the application layer."
    - "Workspace baseline rules and opportunity overrides are separate sources of truth and must never silently overwrite each other."
    - "Templates & Rules is a product page, not a control-plane dump; backend responses must preserve the distinctions the UI-SPEC relies on."
    - "Execution is not successful if only happy-path saves work. Conflict, invalid-rules, and stale-write recovery must be implemented and tested."
  artifacts:
    - path: "apps/api/app/product.py"
      provides: "Mounted API composition that exposes Templates & Rules routes under the real `/api/v1` app"
    - path: "apps/api/app/templates_rules_service.py"
      provides: "Rule validation, effective-rule composition, template listing, and conflict semantics"
    - path: "tests/api/test_templates_rules_api.py"
      provides: "Rules API regression coverage"
  key_links:
    - from: "apps/api/app/templates_rules_service.py"
      to: "docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md"
      via: "rule composition and source-of-truth enforcement"
      pattern: "workspace_rule_sets|opportunity_rule_overrides|effective rules"
    - from: "apps/api/app/templates_rules_routes.py"
      to: "docs/web/ProposalFlow AI｜API 设计 v1.0.md"
      via: "public rules/template endpoints and concurrency semantics"
      pattern: "/api/v1/templates|/workspaces/current/rules|/rules/effective|/rules/override"
---

<objective>
Implement the full backend for Templates & Rules: persistence, public endpoints, validation, conflict handling, effective-rule composition, and opportunity override semantics.

This wave should leave the rules system fully test-covered and ready for web integration without building Proposal Draft resource behavior yet.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
@.planning/phases/07-proposal-draft-templates-rules/07-RESEARCH.md
@.planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
@.planning/phases/07-proposal-draft-templates-rules/07-01-PLAN.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@docs/share/ProposalFlow AI｜数据库设计 v1.0.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement template list and workspace baseline rules persistence with validation-first API behavior</name>
  <files>apps/api/alembic/versions/0007_phase7_templates_rules.py, apps/api/app/main.py, apps/api/app/product.py, apps/api/app/templates_rules_models.py, apps/api/app/templates_rules_repository.py, apps/api/app/templates_rules_service.py, apps/api/app/templates_rules_routes.py, tests/api/test_templates_rules_api.py</files>
  <read_first>
    - tests/api/test_templates_rules_api.py
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - apps/api/app/main.py
    - apps/api/app/product.py
    - apps/api/app/discovery_routes.py
    - apps/api/app/discovery_service.py
    - apps/api/app/opportunity_repository.py
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - `GET /api/v1/templates` returns the frozen MVP template set, including `development_agency`, `product_ux_agency`, and `web_delivery_proposal`.
    - `GET /api/v1/workspaces/current/rules` returns the active workspace baseline rule set or the system-default equivalent.
    - `PUT /api/v1/workspaces/current/rules` requires a concurrency field and rejects stale writes.
    - `POST /api/v1/workspaces/current/rules/validate` returns structured invalid/conflict details without mutating persistence.
    - The Templates & Rules router is mounted into the shipped product API so the endpoints are reachable from the real FastAPI app, not only defined in an isolated module.
  </behavior>
  <action>
    Implement the workspace baseline rules backbone from the failing tests.

    1. Add the Alembic migration and API-side model/repository/service/route files for template definitions and workspace baseline rules.
    2. Persist the fields fixed in the frozen docs:
       - `template_key`
       - `template_scope`
       - `tone_profile`
       - `default_assumptions`
       - `default_exclusions`
       - `preferred_terminology`
       - `banned_terminology`
       - `service_modules`
       - `section_order`
       - `required_sections`
       - `default_cta_style`
    3. Implement validation rules that reject:
       - `required_sections` missing `assumptions`
       - `required_sections` missing `exclusions`
       - overlap between `preferred_terminology` and `banned_terminology`
       - empty `section_order`
    4. Keep validation results structured so the web page can render both inline field errors and a save-time summary.
    5. Register the Templates & Rules router through the existing API composition (`product.py` / `main.py`, following current app conventions) so `/api/v1/templates` and `/api/v1/workspaces/current/rules*` are reachable in tests and dev servers.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_templates_rules_api.py -q -k 'templates or current rules or validate'</automated>
  </verify>
  <acceptance_criteria>
    - The migration and backend templates-rules modules exist.
    - The template list endpoint returns the exact MVP template keys from the docs.
    - Workspace rules read/update/validate paths honor concurrency and return structured validation results.
    - The mounted application exposes the Templates & Rules routes under the real `/api/v1` surface.
    - The targeted pytest command exits `0`.
  </acceptance_criteria>
  <done>
    The workspace baseline rules system is persisted, validated, and publicly readable.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement effective rules and opportunity override APIs with explicit source-of-truth semantics</name>
  <files>apps/api/app/templates_rules_repository.py, apps/api/app/templates_rules_service.py, apps/api/app/templates_rules_routes.py, tests/api/test_templates_rules_api.py</files>
  <read_first>
    - tests/api/test_templates_rules_api.py
    - apps/api/app/templates_rules_service.py
    - docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
    - docs/web/ProposalFlow AI｜页面流程与信息架构说明 v1.0.md
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - .planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Implement the public rule-composition contract exactly as frozen by docs and context.

    1. Add `GET /api/v1/opportunities/{opportunity_id}/rules/effective` that composes:
       - template definition
       - current workspace rule set
       - current opportunity override
       into one `effective rules` response plus `has_override`.
    2. Add `GET /api/v1/opportunities/{opportunity_id}/rules/override`, `PUT /api/v1/opportunities/{opportunity_id}/rules/override`, and `DELETE /api/v1/opportunities/{opportunity_id}/rules/override`.
    3. Require the override update path to carry an optimistic-concurrency field (`updated_at` or equivalent literal timestamp field) and reject stale writes with a recognizable conflict response.
    4. Keep override persistence minimal and opportunity-local; do not allow this path to mutate workspace baseline rows.
    5. Ensure clearing the override reverts effective rules immediately to the workspace baseline + template composite.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_templates_rules_api.py -q -k 'effective or override or conflict'</automated>
  </verify>
  <acceptance_criteria>
    - The effective-rules endpoint exists and composes template + workspace baseline + opportunity override.
    - Opportunity override create/update/delete paths exist and never mutate workspace baseline data.
    - Stale override writes return a recognizable conflict response with reload guidance or equivalent recoverable metadata.
    - The targeted pytest command exits `0`.
  </acceptance_criteria>
  <done>
    Rules composition and opportunity-local override behavior are now stable and backend-complete.
  </done>
</task>

</tasks>

<verification>
Wave 2 is complete only when the rules system is fully persisted, validated, conflict-aware, and exposes public effective-rule and override APIs that the web app can consume directly.
</verification>

<success_criteria>
- Workspace baseline rules are stored and validated on the backend.
- Effective rules and overrides are computed server-side with clear source-of-truth semantics.
- All rule conflicts and stale writes are recoverable, explicit, and test-covered.
</success_criteria>
