---
phase: 07-proposal-draft-templates-rules
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - apps/api/alembic/versions/0008_phase7_proposal_draft.py
  - apps/api/app/main.py
  - apps/api/app/product.py
  - apps/api/app/proposal_draft_models.py
  - apps/api/app/proposal_draft_repository.py
  - apps/api/app/proposal_draft_service.py
  - apps/api/app/proposal_draft_routes.py
  - tests/api/test_proposal_draft_api.py
autonomous: true
requirements:
  - PROP-01
  - PROP-02
must_haves:
  truths:
    - "Proposal Draft generation is structured-first: Lead Brief + Discovery + Template + Effective Rules. Raw intake text is not the direct generation source."
    - "Current resource, immutable versions, restore semantics, and conflict handling must match the product model already established in Phases 5 and 6, including `version_no` route semantics."
    - "Chapter-level regenerate is the primary regenerate path; it must remain local, explicit, and overwrite-aware."
    - "Execution is not successful until generate, update, save-version, restore, section regenerate, export, and blocked-action paths all work together through real APIs."
  artifacts:
    - path: "apps/api/app/proposal_draft_service.py"
      provides: "Proposal Draft gate, generation, mutate, version, section regenerate, and export behavior"
    - path: "tests/api/test_proposal_draft_api.py"
      provides: "Proposal Draft API regression coverage"
  key_links:
    - from: "apps/api/app/proposal_draft_service.py"
      to: "docs/web/ProposalFlow AI｜API 设计 v1.0.md"
      via: "generate/update/section-regenerate/version/restore/export semantics"
      pattern: "LEAD_BRIEF_REQUIRED|DISCOVERY_REQUIRED|sections/{section_key}/regenerate|format=text|format=markdown"
    - from: "apps/api/app/proposal_draft_service.py"
      to: ".planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md"
      via: "locked overwrite, section-local regenerate, and version semantics"
      pattern: "Save Current|Save Version|Regenerate section|Regenerate all|Restore"
---

<objective>
Implement the Proposal Draft backend end-to-end: persistence, current-resource model, generation gates, chapter editing, chapter regenerate, version history, restore, warnings, and export.

This wave should leave the backend ready for a real web workspace to integrate without inventing missing APIs or hidden semantics.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
@.planning/phases/07-proposal-draft-templates-rules/07-RESEARCH.md
@.planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
@.planning/phases/07-proposal-draft-templates-rules/07-02-PLAN.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@docs/share/ProposalFlow AI｜数据库设计 v1.0.md
@docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Proposal Draft current-resource, generate, update, and version-history APIs</name>
  <files>apps/api/alembic/versions/0008_phase7_proposal_draft.py, apps/api/app/main.py, apps/api/app/product.py, apps/api/app/proposal_draft_models.py, apps/api/app/proposal_draft_repository.py, apps/api/app/proposal_draft_service.py, apps/api/app/proposal_draft_routes.py, tests/api/test_proposal_draft_api.py</files>
  <read_first>
    - tests/api/test_proposal_draft_api.py
    - apps/api/app/main.py
    - apps/api/app/product.py
    - apps/api/app/discovery_routes.py
    - apps/api/app/lead_brief_service.py
    - apps/api/app/discovery_service.py
    - apps/api/app/templates_rules_service.py
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
    - docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - `POST /generate` blocks when current Lead Brief or base Discovery is unavailable.
    - `GET /proposal-draft` returns the current resource, latest version number, warnings, confidence notes, effective-rule summary, and override presence.
    - `PATCH /proposal-draft` updates the mutable current working copy only.
    - `POST /save-version`, `GET /versions`, `GET /versions/{version_no}`, and `POST /versions/{version_no}/restore` follow immutable snapshot semantics with optimistic concurrency.
    - The Proposal Draft router is mounted into the shipped product API so the endpoints are reachable from the real FastAPI app, not only defined in an isolated module.
  </behavior>
  <action>
    Build the Proposal Draft resource and generation backbone from the red tests.

    1. Add the Alembic migration plus proposal-draft model/repository/service/route files.
    2. Persist one current proposal draft per opportunity plus immutable proposal-draft versions with `unique(proposal_draft_id, version_no)` semantics.
    3. Implement `POST /generate` so it composes:
       - current Lead Brief
       - current Discovery
       - current effective rules
       - selected template
       into a structured chapter payload covering the frozen chapter set.
    4. Return explicit gate failures with the literal codes `LEAD_BRIEF_REQUIRED` and `DISCOVERY_REQUIRED` when prerequisites are missing.
    5. Keep warnings and confidence metadata explicit in the response so the web workspace can render them without heuristic inference.
    6. Register the Proposal Draft router through the existing API composition (`product.py` / `main.py`, mirroring Lead Brief and Discovery) so `/api/v1/opportunities/{opportunity_id}/proposal-draft*` is reachable in the real app.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_proposal_draft_api.py -q -k 'generate or current resource or save version or restore'</automated>
  </verify>
  <acceptance_criteria>
    - Proposal Draft migration and backend modules exist.
    - Generate, read, update, save-version, `version_no`-based version-detail, and restore endpoints work with optimistic concurrency.
    - Missing Lead Brief or Discovery prerequisites return the literal dependency error codes.
    - The mounted application exposes the Proposal Draft routes under the real `/api/v1` surface.
    - The targeted pytest command exits `0`.
  </acceptance_criteria>
  <done>
    Proposal Draft exists as a versioned backend resource with explicit generation gates and immutable history.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement section regenerate, export, and overwrite-aware conflict semantics</name>
  <files>apps/api/app/proposal_draft_service.py, apps/api/app/proposal_draft_routes.py, tests/api/test_proposal_draft_api.py</files>
  <read_first>
    - tests/api/test_proposal_draft_api.py
    - apps/api/app/proposal_draft_service.py
    - apps/api/app/templates_rules_service.py
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - .planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Finish the high-risk Proposal Draft behaviors with explicit local semantics.

    1. Implement `POST /api/v1/opportunities/{opportunity_id}/proposal-draft/sections/{section_key}/regenerate` so it replaces only the targeted section.
    2. Require the section-regenerate path to carry explicit overwrite awareness when manual edits exist on that section; do not permit silent replacement.
    3. Keep whole-draft regenerate available through the generate path or equivalent service call, but do not let it masquerade as a chapter-local operation.
    4. Implement `GET /api/v1/opportunities/{opportunity_id}/proposal-draft/export` with exact `format=text` and `format=markdown` support.
    5. Return warnings or conflict metadata that the browser can map directly to page-level status bands and local chapter markers.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_proposal_draft_api.py -q -k 'section regenerate or overwrite or export or warning'</automated>
  </verify>
  <acceptance_criteria>
    - Section regenerate exists and operates on the targeted section only.
    - Unsaved manual edits require explicit overwrite awareness before section replacement.
    - Export supports both `text` and `markdown` formats through the public API.
    - The targeted pytest command exits `0`.
  </acceptance_criteria>
  <done>
    Proposal Draft now exposes the local-regenerate and export behavior the UI depends on.
  </done>
</task>

</tasks>

<verification>
Wave 3 is complete only when Proposal Draft backend behavior is fully available: gated generation, mutable current resource, immutable versions, restore, section regenerate, warnings, and export.
</verification>

<success_criteria>
- Proposal Draft no longer has missing backend seams.
- Section regenerate and export are real APIs, not planned placeholders.
- The backend behavior is explicit enough for browser implementation to proceed without ambiguity.
</success_criteria>
