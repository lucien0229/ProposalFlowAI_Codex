---
phase: 07-proposal-draft-templates-rules
plan: 08
type: execute
wave: 8
depends_on:
  - "07-07"
files_modified:
  - tests/e2e/proposal-draft-workspace.spec.ts
  - tests/e2e/helpers/discovery.ts
  - tests/e2e/helpers/opportunity-overview.ts
  - apps/web/components/opportunities/proposal-draft-workspace.tsx
  - apps/web/lib/proposal-draft-copy.ts
  - apps/web/components/opportunities/proposal-draft-status-band.tsx
autonomous: true
gap_closure: true
requirements:
  - RULE-01
  - PROP-01
  - PROP-02
must_haves:
  truths:
    - "A user with ready Lead Brief and Discovery still sees an empty Proposal Draft until they explicitly generate it."
    - "A user missing Lead Brief or Discovery sees backend-backed blocked guidance instead of a mocked test-only state."
    - "A user can trigger a real rules-conflict state by changing live rules after draft creation, and the page shows both the status band and affected chapter warning."
    - "Playwright coverage proves these states through live app and API behavior without `page.route(...).fulfill(...)` fabricating proposal-draft payloads."
  artifacts:
    - path: "tests/e2e/proposal-draft-workspace.spec.ts"
      provides: "Non-mocked browser proof for empty, missing-input, current-draft, and rules-conflict behavior"
    - path: "tests/e2e/helpers/discovery.ts"
      provides: "Reusable setup for Proposal Draft prerequisite states without browser mocks"
    - path: "apps/web/components/opportunities/proposal-draft-workspace.tsx"
      provides: "Workspace state mapping for backend-backed warning and empty-state responses"
  key_links:
    - from: "tests/e2e/proposal-draft-workspace.spec.ts"
      to: "apps/api/app/proposal_draft_service.py"
      via: "real opportunity setup, live generate calls, and backend warning payload assertions"
      pattern: "proposal_draft: null|LEAD_BRIEF_REQUIRED|DISCOVERY_REQUIRED|RULES_CONFLICT"
    - from: "apps/web/components/opportunities/proposal-draft-workspace.tsx"
      to: ".planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md"
      via: "page-level status band plus local chapter markers, preserving the locked desktop workspace hierarchy"
      pattern: "status band|warning|empty state|Generate draft"
---

<objective>
Replace the remaining mocked Proposal Draft gap coverage with real browser proof on top of the corrected backend.

This plan closes the verifier's third gap: empty, missing-input, current-draft, and rules-conflict behavior must be exercised end-to-end through real application state, while preserving the existing desktop-only Proposal Draft UI contract.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
@.planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
@.planning/phases/07-proposal-draft-templates-rules/07-VERIFICATION.md
@.planning/phases/07-proposal-draft-templates-rules/07-05-SUMMARY.md
@.planning/phases/07-proposal-draft-templates-rules/07-06-SUMMARY.md
@docs/design/DESIGN.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
@/Users/wxm/.codex/superpowers/skills/verification-before-completion/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace mocked Proposal Draft gap states with backend-backed Playwright coverage</name>
  <files>tests/e2e/proposal-draft-workspace.spec.ts, tests/e2e/helpers/discovery.ts, tests/e2e/helpers/opportunity-overview.ts, apps/web/components/opportunities/proposal-draft-workspace.tsx, apps/web/lib/proposal-draft-copy.ts, apps/web/components/opportunities/proposal-draft-status-band.tsx</files>
  <read_first>
    - .planning/phases/07-proposal-draft-templates-rules/07-VERIFICATION.md
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - .planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
    - .planning/phases/07-proposal-draft-templates-rules/07-05-SUMMARY.md
    - .planning/phases/07-proposal-draft-templates-rules/07-06-SUMMARY.md
    - tests/e2e/proposal-draft-workspace.spec.ts
    - tests/e2e/helpers/discovery.ts
    - tests/e2e/helpers/opportunity-overview.ts
    - apps/web/components/opportunities/proposal-draft-workspace.tsx
    - apps/web/lib/proposal-draft-copy.ts
    - apps/web/components/opportunities/proposal-draft-status-band.tsx
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
    - /Users/wxm/.codex/superpowers/skills/verification-before-completion/SKILL.md
  </read_first>
  <behavior>
    - The empty Proposal Draft state is reached through a real opportunity with ready Lead Brief and ready Discovery but no current Proposal Draft yet.
    - Lead Brief missing and Discovery missing blocked flows are exercised through the real backend without faking `proposal_draft: null` responses.
    - A real rules-conflict scenario is created by changing live rules after the draft exists, and the page shows both the page-level status band and the affected chapter warning.
    - No `page.route(...).fulfill(...)` remains for `proposal_draft: null`, `LEAD_BRIEF_REQUIRED`, `DISCOVERY_REQUIRED`, or `RULES_CONFLICT` proposal-draft flows.
  </behavior>
  <action>
    Rebuild the Proposal Draft browser proof around real state transitions, following D-11 for hierarchy and D-25 through D-28 for notice visibility.

    1. Remove the request-mock shortcuts that currently fabricate `proposal_draft: null` and `RULES_CONFLICT` in `tests/e2e/proposal-draft-workspace.spec.ts`.
    2. Add or extend helper setup so the spec can create these real states:
       - opportunity exists, Lead Brief absent, Proposal Draft unopened
       - opportunity has Lead Brief, Discovery not ready, Proposal Draft unopened
       - opportunity has ready Lead Brief and ready Discovery, Proposal Draft unopened
       - opportunity has a generated draft, then live rules change so the saved `assumptions` or `exclusions` chapters conflict with the current effective rules
    3. Keep the browser assertions focused on the shipped UX:
       - empty state copy and Generate Draft CTA
       - blocked status-band copy and correct navigation CTA for missing Lead Brief or Discovery
       - rules-conflict status-band copy plus local chapter marker in `Assumptions` or `Exclusions`
    4. If the live backend payloads reveal small UI-mapping gaps, adjust `proposal-draft-workspace.tsx`, `proposal-draft-copy.ts`, or `proposal-draft-status-band.tsx` without altering the locked desktop hierarchy, drawer model, or status-band contract.
    5. Do not broaden scope into mobile/tablet, new billing work, or unrelated Proposal Draft redesign. This is a proof-and-wiring gap closure only.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_templates_rules_api.py tests/api/test_proposal_draft_api.py -q && pnpm exec playwright test tests/e2e/proposal-draft-workspace.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/proposal-draft-workspace.spec.ts` proves empty, Lead Brief missing, Discovery missing, and rules-conflict behavior through real backend state instead of response fabrication.
    - The Proposal Draft page still renders the locked desktop workspace hierarchy and notice model from `07-UI-SPEC.md`.
    - No proposal-draft request mocking remains for `proposal_draft: null`, missing-input gate responses, or `RULES_CONFLICT` warnings.
    - `PYTHONPATH=apps/api pytest tests/api/test_templates_rules_api.py tests/api/test_proposal_draft_api.py -q && pnpm exec playwright test tests/e2e/proposal-draft-workspace.spec.ts` exits `0`.
  </acceptance_criteria>
  <done>
    Proposal Draft gap states are now proven in-browser against the real backend instead of mock-only coverage.
  </done>
</task>

</tasks>

<verification>
Wave 8 is complete only when Proposal Draft empty, dependency-blocked, current-draft, and rules-conflict states are demonstrated end-to-end with no request-mock fabrication of proposal-draft payloads.
</verification>

<success_criteria>
- The verifier's mock-only browser gap is closed with real Playwright coverage.
- Proposal Draft status-band and chapter-warning behavior is backed by actual API payloads.
- The Phase 7 desktop web contract remains intact while the evidence for it becomes real.
</success_criteria>

<output>
After completion, create `.planning/phases/07-proposal-draft-templates-rules/07-08-SUMMARY.md`
</output>
