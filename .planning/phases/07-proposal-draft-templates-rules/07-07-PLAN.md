---
phase: 07-proposal-draft-templates-rules
plan: 07
type: execute
wave: 7
depends_on:
  - "07-02"
  - "07-03"
  - "07-06"
files_modified:
  - apps/api/app/proposal_draft_service.py
  - apps/api/app/proposal_draft_routes.py
  - apps/api/app/templates_rules_service.py
  - tests/api/test_proposal_draft_api.py
  - tests/api/test_templates_rules_api.py
autonomous: true
gap_closure: true
requirements:
  - RULE-01
  - PROP-01
  - PROP-02
must_haves:
  truths:
    - "Proposal Draft stays empty until generation succeeds; visiting the page must not silently create a current draft or version history."
    - "Proposal Draft generation remains blocked by the real Lead Brief and Discovery prerequisites even after the workspace has been opened."
    - "Proposal Draft content, export output, and effective-rule summaries are derived from the live template plus workspace baseline plus opt-in opportunity override stack."
    - "Rules-conflict and missing-input/current-draft notices are emitted by the backend as real proposal-draft payloads, not invented in browser mocks."
  artifacts:
    - path: "apps/api/app/proposal_draft_service.py"
      provides: "Truthful proposal-draft workspace, generation, save/version, regenerate, export, and warning semantics"
    - path: "apps/api/app/templates_rules_service.py"
      provides: "Opt-in opportunity override behavior and live effective-rule composition"
    - path: "tests/api/test_proposal_draft_api.py"
      provides: "Regression proof for empty current draft, dependency gating, live rule composition, and warning payloads"
    - path: "tests/api/test_templates_rules_api.py"
      provides: "Regression proof that overrides are inactive until explicitly saved"
  key_links:
    - from: "apps/api/app/proposal_draft_service.py"
      to: "apps/api/app/templates_rules_service.py"
      via: "live effective-rule composition in generate, save, version, regenerate, and export flows"
      pattern: "build_effective_rule_summary|get_effective_rules_payload"
    - from: "apps/api/app/proposal_draft_service.py"
      to: ".planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md"
      via: "locked rule layering, structured-first generation, and two-layer warning semantics"
      pattern: "D-05|D-08|D-18|D-25|D-26|D-27|D-28"
---

<objective>
Close the Phase 7 backend truth gaps so Proposal Draft is actually gated, actually rule-constrained, and actually warning-capable under the real API.

This plan fixes the verifier's first two blockers and the backend half of the third: GET must stop seeding drafts, opportunity overrides must become opt-in, and all proposal-draft flows must use live effective rules plus real warning payloads.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
@.planning/phases/07-proposal-draft-templates-rules/07-RESEARCH.md
@.planning/phases/07-proposal-draft-templates-rules/07-VERIFICATION.md
@.planning/phases/07-proposal-draft-templates-rules/07-02-SUMMARY.md
@.planning/phases/07-proposal-draft-templates-rules/07-03-SUMMARY.md
@docs/web/ProposalFlow AI｜API 设计 v1.0.md
@docs/share/ProposalFlow AI｜系统架构与技术方案 v1.0.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make Proposal Draft backend truthfully gated, live-rule-driven, and warning-capable</name>
  <files>apps/api/app/proposal_draft_service.py, apps/api/app/proposal_draft_routes.py, apps/api/app/templates_rules_service.py, tests/api/test_proposal_draft_api.py, tests/api/test_templates_rules_api.py</files>
  <read_first>
    - .planning/phases/07-proposal-draft-templates-rules/07-VERIFICATION.md
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - .planning/phases/07-proposal-draft-templates-rules/07-02-SUMMARY.md
    - .planning/phases/07-proposal-draft-templates-rules/07-03-SUMMARY.md
    - apps/api/app/proposal_draft_service.py
    - apps/api/app/proposal_draft_routes.py
    - apps/api/app/templates_rules_service.py
    - tests/api/test_proposal_draft_api.py
    - tests/api/test_templates_rules_api.py
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - `GET /api/v1/opportunities/{opportunity_id}/proposal-draft` returns `proposal_draft: null` and `versions: []` when no generation has succeeded yet.
    - Visiting the Proposal Draft route never weakens `PROP-01`; `POST /generate` still returns literal `LEAD_BRIEF_REQUIRED` or `DISCOVERY_REQUIRED` errors until the real prerequisites exist.
    - Proposal Draft generate, save current, save version, version detail, section regenerate, and export use live effective rules and live assumption/exclusion text rather than seeded defaults.
    - Opportunity overrides are absent by default and become active only after an explicit override save.
    - When current draft assumptions or exclusions drift from the live effective rules, the API emits a real `RULES_CONFLICT` warning and affected chapter warnings per the Phase 7 notice contract.
  </behavior>
  <action>
    Fix the backend seams called out in `07-VERIFICATION.md`, following D-05/D-08/D-18 for rule composition and D-25 through D-28 for notice behavior.

    1. Rewrite the API regression files first so they assert the real gap-closure contract:
       - untouched Proposal Draft GET returns `proposal_draft: null` and no seeded versions
       - repeated GETs do not create backend rows
       - generate-after-GET still blocks on missing Lead Brief or Discovery
       - live baseline-rule edits and explicit opportunity overrides change generated/exported assumptions, exclusions, template selection, and effective-rule summaries
       - opportunity override GET returns `override: null` / inactive semantics until the user saves one
       - rules-conflict warnings come from backend state, not canned defaults
    2. Remove read-path seeding from `proposal_draft_service.py`. Only the real generate path may create the current Proposal Draft resource. `save current`, `save-version`, `restore`, `regenerate`, and `export` must require an existing current draft instead of silently creating one.
    3. Replace `_seed_effective_rule_summary()` and other hardcoded rule text usage with live composition from `templates_rules_service.py`, including generate, save current, save version, version detail, section regenerate, and export.
    4. Change `templates_rules_service.py` so opportunity overrides are opt-in user state:
       - do not auto-create an active override on read
       - create or activate the override row on explicit `PUT`
       - keep `DELETE` idempotent and baseline-restoring even when no active override exists
    5. Add real Proposal Draft warning semantics for missing-input/current-draft and rules-conflict states. The minimal concrete conflict rule is: if the live effective assumptions or exclusions no longer match the current draft's `assumptions` or `exclusions` sections, the workspace payload must surface a `RULES_CONFLICT` warning plus affected chapter warnings without auto-rewriting the draft.
    6. Preserve the existing desktop-only web contract and response vocabulary. Do not expand scope into mobile/tablet or new rule-management surfaces.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_templates_rules_api.py tests/api/test_proposal_draft_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Initial Proposal Draft GET no longer seeds a current draft or version history.
    - Generate remains blocked after prior GET visits until current Lead Brief and ready Discovery exist.
    - Proposal Draft assumptions, exclusions, template key, and effective-rule summary come from live effective rules in generate/save/version/regenerate/export flows.
    - Opportunity overrides are inactive by default and only become active after explicit user save.
    - The API returns real `RULES_CONFLICT` and missing-input/current-draft warning payloads suitable for page-level and chapter-level UI notices.
    - `PYTHONPATH=apps/api pytest tests/api/test_templates_rules_api.py tests/api/test_proposal_draft_api.py -q` exits `0`.
  </acceptance_criteria>
  <done>
    Proposal Draft backend semantics match the roadmap truth instead of seeded placeholder behavior.
  </done>
</task>

</tasks>

<verification>
Wave 7 is complete only when the real API keeps Proposal Draft empty until generation, keeps `PROP-01` intact after page visits, composes live rules into all draft flows, and emits backend-backed warning payloads for the browser to render.
</verification>

<success_criteria>
- Proposal Draft no longer seeds itself on GET or on non-generate actions.
- Live template, workspace baseline, and explicit opportunity override data drive draft content and exports.
- Backend warning payloads exist for the empty/missing-input/rules-conflict states that were previously mock-only.
</success_criteria>

<output>
After completion, create `.planning/phases/07-proposal-draft-templates-rules/07-07-SUMMARY.md`
</output>
