---
phase: 07-proposal-draft-templates-rules
plan: 01
type: execute
wave: 1
depends_on:
  - "06"
files_modified:
  - tests/api/test_templates_rules_api.py
  - tests/api/test_proposal_draft_api.py
  - tests/shared/phase7-contracts.assert.ts
  - packages/shared-types/index.ts
  - packages/shared-schemas/index.ts
  - packages/shared-config/index.ts
autonomous: true
requirements:
  - RULE-01
  - PROP-01
  - PROP-02
must_haves:
  truths:
    - "Phase 7 starts from failing contract coverage. No production code should land before the public API, DTOs, and route builders are frozen in red tests."
    - "UI/UX implementation later must follow `.planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md`, `docs/design/DESIGN.md`, `frontend-skill`, and `ui-ux-pro-max`; this wave must preserve those seams instead of inventing alternate vocabularies."
    - "TDD is mandatory: failing tests first, then minimal implementation. This applies to API contracts, route helpers, and browser-facing response semantics."
    - "This product remains desktop/laptop web only. No mobile/tablet contract, no touch-first gestures, and no alternate route/UI surface should leak into the phase plan."
  artifacts:
    - path: "tests/api/test_templates_rules_api.py"
      provides: "Canonical rules/template API contract coverage, including validation and override conflict semantics"
    - path: "tests/api/test_proposal_draft_api.py"
      provides: "Canonical proposal draft API contract coverage, including generation gate, chapter regenerate, versioning, and export semantics"
    - path: "tests/shared/phase7-contracts.assert.ts"
      provides: "Focused shared-contract assertions for Phase 7 route builders, literal path segments, and DTO alignment"
    - path: "packages/shared-config/index.ts"
      provides: "Canonical Phase 7 web/API route builders and templates"
  key_links:
    - from: "tests/api/test_proposal_draft_api.py"
      to: ".planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md"
      via: "locked generate gate, chapter regenerate overwrite awareness, and export restrictions"
      pattern: "LEAD_BRIEF_REQUIRED|DISCOVERY_REQUIRED|Regenerate section|format=markdown|effective_rule_summary"
    - from: "tests/api/test_templates_rules_api.py"
      to: "docs/web/ProposalFlow AI｜API 设计 v1.0.md"
      via: "workspace rule set, effective rules, and opportunity override contract"
      pattern: "/templates|/workspaces/current/rules|/rules/effective|/rules/override"
---

<objective>
Freeze the public Phase 7 contract before implementation by making the rules/template endpoints, proposal-draft endpoints, shared DTOs, and route helpers explicit in failing tests and shared packages.

This wave does not build the real backend or UI yet. It removes ambiguity so later waves can implement without reopening source-of-truth questions.
</objective>

<execution_context>
@/Users/wxm/.codex/get-shit-done/workflows/execute-plan.md
@/Users/wxm/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
@.planning/phases/07-proposal-draft-templates-rules/07-RESEARCH.md
@.planning/phases/07-proposal-draft-templates-rules/07-UI-SPEC.md
@/Users/wxm/.codex/skills/frontend-skill/SKILL.md
@/Users/wxm/.codex/skills/ui-ux-pro-max/SKILL.md
@/Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing API contract tests for Templates & Rules and Proposal Draft</name>
  <files>tests/api/test_templates_rules_api.py, tests/api/test_proposal_draft_api.py</files>
  <read_first>
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - docs/web/ProposalFlow AI｜页面清单与页面级需求 v1.0.md
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - .planning/phases/07-proposal-draft-templates-rules/07-RESEARCH.md
    - packages/shared-config/index.ts
    - packages/shared-types/index.ts
    - tests/api/test_lead_brief_api.py
    - tests/api/test_discovery_api.py
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <behavior>
    - Test `GET /api/v1/templates`, `GET/PUT /api/v1/workspaces/current/rules`, `POST /api/v1/workspaces/current/rules/validate`, `GET /api/v1/opportunities/{id}/rules/effective`, `GET/PUT/DELETE /api/v1/opportunities/{id}/rules/override`.
    - Test rules validation catches `required_sections` omissions for `assumptions` or `exclusions`.
    - Test rules validation catches overlap between `preferred_terminology` and `banned_terminology`.
    - Test `GET /api/v1/opportunities/{id}/proposal-draft`, `POST /generate`, `PATCH`, `POST /save-version`, `GET /versions`, `GET /versions/{version_no}`, `POST /versions/{version_no}/restore`, `POST /sections/{section_key}/regenerate`, and `GET /export`.
    - Test generation gate returns `LEAD_BRIEF_REQUIRED` and `DISCOVERY_REQUIRED` failures when prerequisites are missing.
    - Test chapter regenerate requires explicit overwrite awareness and export supports `format=text` and `format=markdown`.
  </behavior>
  <action>
    Start from RED. Create the Phase 7 API contract tests before any backend implementation exists.

    1. Add `tests/api/test_templates_rules_api.py` that hard-codes the public rules/template endpoints, payload shapes, validation errors, concurrency expectations, and source-of-truth semantics (`workspace_rule_sets` + `opportunity_rule_overrides` -> `effective rules`).
    2. Add `tests/api/test_proposal_draft_api.py` that hard-codes the public proposal-draft endpoints, generation prerequisites, section-regenerate overwrite semantics, version history semantics, and export contract.
    3. Freeze version semantics to the existing Phase 5 / Phase 6 convention: list/detail/restore paths use `{version_no}` and request bodies carry `version_no`, never a new opaque `version_id` contract.
    4. Keep error-code assertions literal: `LEAD_BRIEF_REQUIRED`, `DISCOVERY_REQUIRED`, recognizable rule-conflict failures, and proposal-draft conflict/reload guidance.
    5. Make browser-facing warnings explicit in assertions so later UI work cannot downgrade them to generic errors or hidden toasts.
  </action>
  <verify>
    <automated>PYTHONPATH=apps/api pytest tests/api/test_templates_rules_api.py tests/api/test_proposal_draft_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/api/test_templates_rules_api.py` exists and asserts the exact rules/template endpoints plus conflict validation semantics.
    - `tests/api/test_proposal_draft_api.py` exists and asserts the exact proposal-draft endpoints plus gate, regenerate, `version_no`-based version history, restore, and export semantics.
    - The targeted pytest command fails red before backend implementation exists.
    - The tests contain the literal strings `LEAD_BRIEF_REQUIRED`, `DISCOVERY_REQUIRED`, `effective_rule_summary`, `format=markdown`, and `Regenerate section`.
  </acceptance_criteria>
  <done>
    The public Phase 7 contract is frozen in failing tests before production code starts.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend shared Phase 7 types, schemas, route helpers, and focused contract assertions</name>
  <files>packages/shared-types/index.ts, packages/shared-schemas/index.ts, packages/shared-config/index.ts, tests/shared/phase7-contracts.assert.ts</files>
  <read_first>
    - packages/shared-types/index.ts
    - packages/shared-schemas/index.ts
    - packages/shared-config/index.ts
    - docs/web/ProposalFlow AI｜API 设计 v1.0.md
    - docs/share/ProposalFlow AI｜数据库设计 v1.0.md
    - .planning/phases/07-proposal-draft-templates-rules/07-CONTEXT.md
    - tests/api/test_templates_rules_api.py
    - tests/api/test_proposal_draft_api.py
    - apps/web/lib/discovery-api.ts
    - apps/web/lib/lead-brief-api.ts
    - /Users/wxm/.codex/superpowers/skills/test-driven-development/SKILL.md
  </read_first>
  <action>
    Add the complete shared contract layer that later backend and web waves will consume.

    1. Extend `packages/shared-types/index.ts` with exact rule/template shapes:
       - `TemplateDefinition`
       - `WorkspaceRuleSet`
       - `WorkspaceRuleSetResponse`
       - `WorkspaceRuleSetUpdateRequest`
       - `WorkspaceRuleValidationResponse`
       - `OpportunityRuleOverride`
       - `OpportunityRuleOverrideResponse`
       - `EffectiveRuleSummary`
    2. Extend `packages/shared-types/index.ts` with exact proposal-draft shapes:
       - `ProposalDraftSectionKey`
       - `ProposalDraftSection`
       - `ProposalDraftSections`
       - `ProposalDraftCurrentResource`
       - `ProposalDraftVersion`
       - `ProposalDraftCurrentResourceResponse`
       - `ProposalDraftVersionListResponse`
       - `ProposalDraftVersionDetailResponse`
       - `ProposalDraftSaveCurrentRequest`
       - `ProposalDraftSaveVersionRequest`
       - `ProposalDraftRestoreRequest`
       - `ProposalDraftGenerateRequest`
       - `ProposalDraftGenerateResponse`
       - `ProposalDraftSectionRegenerateRequest`
       - `ProposalDraftConflictResponse`
       - `ProposalDraftExportFormat`
    3. Mirror those additions in `packages/shared-schemas/index.ts` with strict schemas, keeping field names and nested structures identical to the shared types.
    4. Extend `packages/shared-config/index.ts` with exact builders and API path constants for:
       - templates list
       - current workspace rules read/update/validate
       - opportunity effective rules
       - opportunity rules override read/update/delete
       - proposal-draft read/generate/update
       - proposal-draft section regenerate
       - proposal-draft save-version / versions / version detail / restore / export
    5. Keep Phase 7 version routes aligned with the shipped Lead Brief / Discovery pattern: `versions/{version_no}` for detail and restore, `latest_version_no` on current-resource payloads, and no parallel `version_id` vocabulary.
    6. Add `tests/shared/phase7-contracts.assert.ts` that imports the shared route builders/types and asserts the literal public path segments, `proposal-draft` route names, `version_no` path usage, and the existence of the required DTO exports.
    7. Use the exact public path segment `proposal-draft` and keep section regenerate keyed by `{section_key}` rather than by array index or ad hoc route naming.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit tests/shared/phase7-contracts.assert.ts && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - Shared Phase 7 types exist for both the rules system and the proposal-draft resource.
    - Shared schemas exist with matching names and strict field validation.
    - Shared config exports canonical builders for all Phase 7 public endpoints with the exact `proposal-draft`, `templates-rules`, and `version_no` vocabulary.
    - `tests/shared/phase7-contracts.assert.ts` exists and compiles as a focused guard against path/DTO drift.
    - `pnpm exec tsc --noEmit tests/shared/phase7-contracts.assert.ts && pnpm lint` exits successfully after the shared contract files are updated.
  </acceptance_criteria>
  <done>
    The shared Phase 7 contract surface is explicit and ready for backend and web implementation.
  </done>
</task>

</tasks>

<verification>
Wave 1 is complete only when failing contract tests exist for the public rules and proposal-draft APIs, and the shared DTO/route-helper layer matches those contracts exactly.
</verification>

<success_criteria>
- Public route drift is impossible to miss.
- Phase 7 contract names and shapes are frozen before implementation.
- The plan preserves TDD, web-only scope, and later UI/UX skill enforcement.
</success_criteria>
